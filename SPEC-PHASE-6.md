# SPEC — فاز ۶: پیشنهاد به مشتری (تب ۴)

> پیش‌نیاز: فاز ۵ ✅ (انتخاب نهایی قفل‌شده). مرجع بصری: `ProposalTab` در `mockups/inquiry-detail-mockup.jsx`.
> این مرحله در اختیار **کارشناس فروش** است (بعد از قفل تب ۳ توسط مدیریت).

## هدف فاز ۶

1. **پیشنهاد مالی و پیشنهاد فنی** به‌عنوان دو Entity کاملاً مستقل (نسخه‌بندی، وضعیت، ارسال جدا از هم)
2. قیمت پایه/مشخصات فنی هر قلم **فقط‌خواندنی** از خروجی تب ۳ (قفل‌شده) نمایش داده می‌شه
3. نسخه فعلی هرکدوم قابل ویرایش تا وقتی ارسال نشده (`sent_at IS NULL`)؛ بعد از «ارسال شد» قفل می‌شه و «اصلاح» یک نسخه جدید (version+1) می‌سازه — نسخه قبلی به‌عنوان تاریخچه (`status='superseded'`) باقی می‌مونه
4. ترم تحویل منتخب پیشنهاد مالی (از بین گزینه‌های تب ۳) + زمان تحویل قابل‌ویرایش؛ زمان تحویل پیشنهاد فنی **همگام و فقط‌خواندنی** از همون مقدار پیشنهاد مالی نسخه فعلی
5. آپلود دستی فایل خروجی (PDF/Word) برای هرکدوم — طبق تصمیم شما، بدون تولید خودکار سند
6. دکمه «ارسال شد» **مستقل** برای مالی و فنی (طبق سند طراحی، برخلاف دکمه مشترک mockup)

## ⚠️ یک انحراف حساب‌شده از mockup/اسکیما (تأییدشده توسط شما)

**تک‌ارزی بودن پیشنهاد مالی**: `financial_proposals.currency_code` طبق اسکیما یک ارز واحده، ولی آفرهای منتخب اقلام مختلف یک استعلام ممکنه ارز متفاوت داشته باشن. رفتار انتخابی شما: **هشدار غیرمسدودکننده** — اگه ارز آفر منتخب قلمی با `currency_code` پیشنهاد فرق داشت، ذخیره/ارسال مسدود نمی‌شه، فقط در پاسخ API و در UI یک هشدار زرد نمایش داده می‌شه («این قلم با ارز {X} قیمت‌گذاری شده، نه {ارز پیشنهاد}») و کارشناس فروش خودش `final_sale_price` رو دستی اصلاح می‌کنه.

## بک‌اند

مدل‌های Prisma جدید (جدول‌هاشون در baseline هست، Migration جدید لازم نیست): `FinancialProposal`, `FinancialProposalItem`, `TechnicalProposal`, `TechnicalProposalItem`.

`proposal_number` بدون شمارنده جدا تولید می‌شه: `{internalNumber}-FIN-v{version}` / `{internalNumber}-TEC-v{version}`.

### ProposalModule

| Endpoint | دسترسی | توضیح |
|---|---|---|
| `GET /inquiries/:id/proposal` | `proposal.view` | baseline فقط‌خواندنی (از تب ۳) + نسخه فعلی مالی با اقلام + نسخه فعلی فنی با اقلام + فهرست تاریخچه هر دو + هشدارهای ارز؛ اگه نسخه فعلی وجود نداره، خودکار نسخه ۱ Draft (seed از baseline) ساخته می‌شه |
| `PUT /inquiries/:id/proposal/financial` | `proposal.edit` | ذخیره Draft فعلی (نسخه با `sent_at IS NULL`): ترم/روز تحویل، ارز، شرایط پرداخت، اعتبار پیشنهاد، یادداشت مذاکره، `fileUrl`، و per-item `markupPercent`/`finalSalePrice` — رد می‌شه اگه نسخه فعلی already ارسال شده |
| `POST /inquiries/:id/proposal/financial/send` | `proposal.send` | `sent_at = now()` روی نسخه فعلی؛ activity با tag=`stage_completed` |
| `POST /inquiries/:id/proposal/financial/revise` | `proposal.edit` | فقط اگه نسخه فعلی ارسال‌شده باشه: نسخه فعلی → `status='superseded'`, نسخه جدید (version+1, `status='current'`, `sent_at=null`) با کپی مقادیر نسخه قبل ساخته می‌شه |
| `PUT /inquiries/:id/proposal/technical` | `proposal.edit` | مشابه مالی؛ `deliveryTimeEstimateDays` سمت سرور همیشه از نسخه فعلی مالی بازنویسی می‌شه (فیلد ارسالی از کلاینت نادیده گرفته می‌شه) |
| `POST /inquiries/:id/proposal/technical/send` | `proposal.send` | مشابه مالی |
| `POST /inquiries/:id/proposal/technical/revise` | `proposal.edit` | مشابه مالی |

**قوانین کسب‌وکاری:**
- پیش‌شرط کل ماژول: `inquiries.selection_locked_at` باید پر باشه (۴۰۰ در غیر این صورت)
- هر Endpoint نوشتنی فقط روی «نسخه فعلی و ارسال‌نشده» عمل می‌کنه؛ اگه ارسال شده، فقط `revise` مجازه
- `purchase_price` هر قلم پیشنهاد مالی = فی مؤثر همون قلم (با استفاده مجدد از `computeEffectiveUnitPrice` فاز ۵) در لحظه ذخیره — snapshot، نه لینک زنده
- ذخیره اولیه نسخه ۱: `markupPercent`/`finalSalePrice` از baseline (`inquiry_items`) کپی می‌شه؛ `chosenDeliveryTerm`/`deliveryDays` پیش‌فرض از ارزون‌ترین گزینه تب ۳؛ `currencyCode` پیش‌فرض = پرتکرارترین ارز بین آفرهای منتخب
- وقتی نسخه فعلی مالی save/revise می‌شه و مقدار deliveryDays/term تغییر می‌کنه، اگه نسخه فعلی فنی هنوز Draft باشه (`sent_at IS NULL`)، `delivery_time_estimate_days`‌ش خودکار به‌روز می‌شه
- هشدار ارز per-item در پاسخ `GET`/`PUT` مالی برمی‌گرده؛ چیزی رو مسدود نمی‌کنه
- activity فقط روی `send` هر کدوم ثبت می‌شه (نه هر ذخیره Draft)

### دسترسی جدید (`permission-catalog.ts`)

ماژول `proposal`: `proposal.view`، `proposal.edit`، `proposal.send` — پیش‌فرض کامل به فروش و مدیریت، فقط `view` به بازرگانی.

## فرانت‌اند — تب ۴ (مطابق mockup)

- گیت ورودی: اگه `selection_locked_at` خالیه، پیام «ابتدا باید مرحله انتخاب نهایی توسط مدیریت قفل بشه» به‌جای محتوای تب
- بنر سبز قفل تب ۳ + بنر یادداشت مدیر (`manager_note_to_sales`) بالای تب
- کارت baseline فقط‌خواندنی: قیمت پایه هر قلم + گزینه‌های ترم تحویل تب ۳
- دو کارت مستقل کنار هم (مالی/فنی)، هرکدوم:
  - نشان نسخه فعلی + وضعیت (Draft/ارسال‌شده)
  - فیلدهای فرم مطابق mockup؛ همه در حالت ارسال‌شده `disabled`
  - ردیف هر قلم با هشدار ارز (زرد) در صورت Mismatch
  - دکمه «آپلود فایل» (به‌جای «تولید فایل») + دکمه «تاریخچه نسخه‌ها (N)» (Modal فهرست فقط‌خواندنی نسخه‌های قبلی)
  - در حالت Draft: دکمه «ذخیره» + دکمه «فایل‌ها ارسال شد» (مستقل از کارت دیگر)
  - در حالت ارسال‌شده: پیام سبز + دکمه «اصلاح (ساخت نسخه جدید)»
- گیت دسترسی: بدون `proposal.edit` فرم read-only؛ بدون `proposal.send` دکمه‌های ارسال/اصلاح مخفی

## خارج از اسکوپ فاز ۶ (به‌صراحت)

- تولید خودکار PDF/Word — آپلود دستی جایگزینش شد (طبق تصمیم شما)
- تب نتیجه نهایی برد/باخت (`OutcomeTab`، مرحله ۷) — فاز ۷
- تبدیل نرخ ارز برای هشدار Mismatch — فقط نمایش هشدار، بدون محاسبه تبدیل

## تست‌ها

- **Jest**: تولید خودکار نسخه ۱ (seed از baseline)، رد ذخیره روی نسخه ارسال‌شده، `revise` نسخه جدید می‌سازه و قبلی را `superseded` می‌کنه، همگام‌سازی `deliveryTimeEstimateDays` فنی از مالی، هشدار ارز non-blocking، گیت `selection_locked_at`
- **E2E زنده**: قفل تب ۳ → ورود به تب ۴ (seed خودکار) → ذخیره Draft مالی+فنی → ارسال مالی (فنی هنوز Draft) → تغییر ترم مالی و بررسی همگام‌سازی فنی → ارسال فنی → اصلاح مالی (نسخه ۲) → پاک‌سازی

## Definition of Done

- [ ] GET با seed خودکار نسخه ۱ + baseline فقط‌خواندنی + هشدار ارز
- [ ] PUT/send/revise مستقل برای مالی و فنی
- [ ] همگام‌سازی زمان تحویل فنی از مالی
- [ ] گیت `selection_locked_at` روی کل ماژول
- [ ] تب ۴ مطابق mockup (دو کارت مستقل، آپلود دستی، تاریخچه نسخه‌ها)
- [ ] Jest سبز + build/lint + E2E زنده + تست بصری
