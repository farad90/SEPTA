# طراحی دیتابیس ERP پولاد تجهیز آپادانا

> دیتابیس: PostgreSQL | بک‌اند: Node.js
> این سند هم‌زمان با فایل `erp-schema.sql` پیش می‌ره و توضیح روابط/تصمیمات طراحی رو نگه می‌داره.

**تصمیمات کلی طراحی:**
- کلید اصلی همه جداول: `UUID` (با `gen_random_uuid()`) — به‌جز `item_catalog` و `currencies` که از کد طبیعی (Natural Key) به‌عنوان PK استفاده می‌کنن چون خودشون یکتا و معنادارن
- نام جداول و ستون‌ها: `snake_case` (استاندارد PostgreSQL)
- همه جداول اصلی دارای `created_at` / `updated_at`
- برای جلوگیری از ثبت تکراری نام شرکت/کالا، از `pg_trgm` برای جستجوی فازی (Similarity Search) استفاده می‌شه — باید Extension مربوطه هم نصب بشه (`CREATE EXTENSION pg_trgm`)

---

## دامنه ۱: پایه/پشتیبان ✅

### users / permissions / permission_groups / permission_group_items — سیستم دسترسی (نسخه ۲، اصلاح کامل)

⚠️ **این بخش کاملاً بازطراحی شد** — به‌جای نقش‌های ثابت (کارشناس/مدیر × حوزه)، یک سیستم RBAC واقعی با دسترسی‌های ریز:

- `permissions`: فهرست تک‌تک دسترسی‌های سیستم، دسته‌بندی‌شده بر اساس `module` (مثلاً «استعلام‌ها» می‌تونه ده‌ها دسترسی مجزا داشته باشه — ثبت، ویرایش، دیدن قیمت، تأیید تخفیف، انتخاب آفر، صدور فاکتور و...). هرکدوم یک چک‌باکس در UI هست
- `permissions.supports_limit`: برخی دسترسی‌ها (مثل «تأیید مبلغ قرارداد») می‌تونن سقف عددی داشته باشن
- `permission_groups`: **گروه‌های دسترسی** — چندتا از پیش تعریف‌شده (`is_default=true`: بازرگانی، مالی، فروش، مدیریت) + هر تعداد گروه سفارشی که مدیر بسازه، با هر ترکیبی از دسترسی‌ها
- `permission_group_items`: کدوم دسترسی‌ها در کدوم گروه فعالن؛ `limit_value` برای دسترسی‌هایی که `supports_limit=true` دارن
- `users.permission_group_id`: هر کاربر **دقیقاً به یک گروه دسترسی** وصل می‌شه (نه چند نقش هم‌زمان) — مدیر این اتصال رو انجام می‌ده

### users — اطلاعات هویتی الزامی و مدارک

- `users` فیلدهای هویتی الزامی گرفت: `birth_date`، `national_id` (کد ملی)، `birth_certificate_no` (شماره شناسنامه)، `address`، `profile_photo_url`
- `user_identity_documents`: تصاویر مدارک هویتی — چون شناسنامه چند صفحه داره، `document_type='birth_certificate_page'` با `page_number` مشخص می‌شه؛ کارت ملی (`document_type='national_id_card'`) معمولاً یک یا دو تصویر (رو/پشت) داره

### بازیابی رمز عبور (خودسرویس) + ریست پسورد توسط مدیر ✅ 🆕

⚠️ ستون‌های `password_reset_token`/`password_reset_token_expires_at` از فاز ۱ (Migration `0002_auth_fields`) در جدول موجود بودن ولی هیچ‌وقت خونده/نوشته نمی‌شدن — یک قابلیت نیمه‌کاره که این فاز کامل شد؛ **بدون تغییر Schema جدید**، همون دو ستون به کار گرفته شدن.

- **فراموشی رمز عبور (`POST /auth/forgot-password` → `POST /auth/reset-password`)**: با `identifier` (ایمیل/موبایل) کاربر پیدا می‌شه؛ اگه فعال و تأییدشده باشه و ایمیل داشته باشه، یک توکن تصادفی ۳۲بایتی ساخته می‌شه — فقط **هش SHA-256 اون در `password_reset_token` ذخیره می‌شه** (دقیقاً هم‌الگوی `refresh_tokens.token_hash`)، با انقضای ۶۰ دقیقه‌ای. لینک حاوی توکن خام از طریق `MailService` موجود (زیرساخت فاز ۴) ایمیل می‌شه. ⚠️ **پاسخ endpoint همیشه یک پیام عمومی یکسانه** — چه کاربری با اون ایمیل پیدا بشه چه نه — تا از User Enumeration جلوگیری بشه؛ endpoint دوم (`reset-password`) توکن رو با `newPassword` عوض می‌کنه، بعد از موفقیت همه‌ی `refresh_tokens` فعال اون کاربر باطل می‌شن (خروج اجباری از همه دستگاه‌ها) — توکن هم تک‌مصرفه (بعد از استفاده `null` می‌شه)
- ⚠️ **SMTP اختیاریه (طبق طراحی موجود `MailService`)** — اگه تنظیم نشده باشه (dev/local)، لینک بازیابی به‌جای ایمیل در لاگ سرور نوشته می‌شه (`Logger.warn`) تا در محیط توسعه هم قابل استفاده باشه؛ در production باید SMTP تنظیم بشه وگرنه این قابلیت عملاً غیرفعاله
- **ریست پسورد توسط مدیر (`POST /users/:id/reset-password`)**: پشت همون Guard سطح کلاس `users.manage` (بدون کلید دسترسی جدید) — مدیر مستقیماً رمز عبور جدید کاربر رو تعیین می‌کنه (هم‌الگوی `initialPassword` در ساخت کاربر)؛ این هم بعد از تغییر همه نشست‌های فعال اون کاربر رو باطل می‌کنه. پاسخ به این سوال که «اگه مدیر سیستم پسوردش یادش بره چیکار کنه؟» → از همون فلوی «فراموشی رمز عبور» عمومی استفاده می‌کنه (هیچ مسیر ویژه‌ای برای ادمین لازم نبود)؛ اگه مدیر دیگری هم در گروه «مدیریت» باشه، می‌تونه از پنل کاربران پسورد مدیر قفل‌شده رو مستقیم ریست کنه

### business_partners / partner_contacts

- `business_partners` هم مشتری هم تأمین‌کننده رو در یک جدول واحد نگه می‌داره (`partner_type`)
- ⚠️ فیلدهای `province`/`city`/`postal_code`/`phone`/`email`/`national_id`/`registration_number` مخصوص شرکت‌های داخلی (ایرانی) هستن — در سطح UI فقط وقتی `country = 'ایران'` نمایش داده می‌شن، ولی در دیتابیس همیشه nullable و در دسترسن (برای هر کشوری که این اطلاعات معنا داشته باشه)
- `notes`: یادداشت/ملاحظات آزاد درباره شرکت
- هر Partner می‌تونه چند رابط (`partner_contacts`) با نقش‌های مختلف (فنی/مالی/بازرگانی) داشته باشه، و هرکدوم قابل ویرایش/حذف
- `partner_contacts.level`: به‌جای فیلد آزاد «سمت»، یک سطح محدود (کارشناس/مدیر/مدیرعامل) — منسجم با ساختار سطوح سازمانی که برای کاربران داخلی هم استفاده کردیم
- `partner_contacts.mobile`: شماره موبایل، جدا از `phone` (تلفن ثابت)
- دسترسی بر اساس `partner_type` در سطح اپلیکیشن کنترل می‌شه (نه در دیتابیس): کارشناس فروش فقط `customer`، کارشناس بازرگانی فقط `supplier`/`freight_forwarder` می‌بینه

### فیلدهای دوزبانه (فاز ۳۷) ✅

⚠️ پیش‌نیاز پیشنهاد/پیش‌فاکتور دوزبانه (فاز ۳۸) — مشتری خارجی نیاز به نسخه انگلیسی نام/آدرس شرکت و نام رابط داره.

- `business_partners.company_name_en` / `address_en` و `partner_contacts.contact_name_en`: همه nullable، بدون تغییر ساختاری دیگه
- ⚠️ **تصمیم صریح کاربر**: تلفن/ایمیل دوزبانه نمی‌شن (این‌ها ذاتاً بدون زبان‌ان) — فقط فیلدهای متنی (نام/آدرس) نسخه انگلیسی گرفتن
- فرانت (`PartnersPage.tsx`): هر فیلد اصلی (نام شرکت، آدرس، نام رابط) یک فیلد انگلیسی اختیاری کنارش/زیرش داره؛ در حالت نمایش هم به همون شکل (فارسی + انگلیسی جدا) نشون داده می‌شه

### item_catalog

- کلید اصلی، خود `item_code` هست (متن، نه UUID) چون کارشناسان مستقیماً باهاش کار می‌کنن
- بدون دسته‌بندی — لیست تخت طبق تصمیم قبلی

⚠️ **Migration 0005_feedback_pack**: `part_number` به‌عنوان فیلد اصلی کالا اضافه شد (`NOT NULL`، با بک‌فیل از `item_code` موجود، چون کد قبلی در واقع پارت نامبر بوده) + ایندکس فازی `idx_item_catalog_part_number`. همراهش دو جدول پشتیبان: `catalog_counters` (شمارنده اتمیک تک‌ردیفه برای تولید کد سیستمی `ITM-NNNNNN`؛ کد دستی هم مجازه) و `measurement_units` (فهرست واحدهای اندازه‌گیری از پیش تعریف‌شده، ۱۵ ردیف Seed، افزودنی با دسترسی `catalog.manage_units`).

### item_catalog_documents — پیوست فایل در سطح کالا (فاز ۲۵) ✅

⚠️ اولین جدول پیوستی مستقل برای کاتالوگ کالا — تا این فاز اصلاً پیوستی برای کالا وجود نداشت.

- الگوی دقیقاً هم‌شکل `inquiry_item_documents` (دامنه ۲) با یک تفاوت ساختاری مهم: چون PK جدول `item_catalog` یک کلید طبیعی (`item_code VARCHAR(100)`) هست نه UUID، ستون FK این جدول هم `VARCHAR(100) REFERENCES item_catalog(item_code)` است، نه `UUID`
- `ON DELETE CASCADE` — حذف یک کالا (که خودش نادره چون `inquiry_items` جلوش رو با FK می‌گیره) پیوست‌هاش رو هم پاک می‌کنه
- دسترسی افزودن/حذف پیوست از پرمیژن موجود `catalog.create` استفاده می‌کنه (نه کلید جدا) — دقیقاً هم‌الگوی `inquiry_item_documents` که از `inquiry.edit` دامنهٔ مادر استفاده می‌کنه؛ مشاهدهٔ پیوست‌های موجود برای هر کاربر با `catalog.view` آزاده

### Import/Export اکسل کاتالوگ کالا (فاز ۲۵) — بدون تغییر ساختار دیتابیس

⚠️ این قابلیت هیچ جدول جدیدی نیاز نداشت — فقط منطق اپلیکیشنه (Parse/Generate با `exceljs` در بک‌اند):

- **Import فقط ایجاد می‌کنه، هرگز بروزرسانی نمی‌کنه** (تصمیم صریح کاربر) — اگه `item_code` یک ردیف از قبل در کاتالوگ باشه، همون ردیف با پیام خطا رد می‌شه، نه Upsert؛ برای اصلاح کالای موجود باید از فرم ویرایش تکی استفاده بشه
- **مدل خطا: ثبت جزئی + گزارش**، نه Atomic — هر ردیف مستقل پردازش می‌شه؛ ردیف‌های سالم ثبت می‌شن، ردیف‌های مشکل‌دار (شرح/پارت‌نامبر خالی یا کد تکراری) رد می‌شن بدون توقف بقیهٔ فایل؛ خروجی یک گزارش `{totalRows, createdCount, failedCount, errors:[{row, itemCode?, message}]}` است
- ستون `default_measurement_unit` در اکسل متن آزاده و با جدول `measurement_units` همگام‌سازی خودکار نمی‌شه (چون خود این ستون در DB هم هیچ FK/CHECK نداره — فقط UI فرم دستی از `measurement_units` به‌عنوان پیشنهاد استفاده می‌کنه)
- دسترسی: `catalog.import` (کلید جدید، پرمیژن مجزا چون یک نوشتار دسته‌جمعیه با شعاع اثر بیشتر از افزودن یک کالای تکی) — به همون گروه‌های پیش‌فرضی داده می‌شه که `catalog.manage_units` رو دارن. Export فقط با `catalog.view` (همون دیتایی که کاربر با View می‌بینه، خطر جدیدی نداره)

### currencies / exchange_rates

- `exchange_rates` نرخ روزانه هر ارز به ریال رو نگه می‌داره؛ `UNIQUE(currency_code, rate_date)` تضمین می‌کنه هر ارز در هر روز فقط یک نرخ داشته باشه
- منبع دریافت خودکار نرخ (فیلد `source`) هنوز مشخص نشده — **سوال باز برای فاز پیاده‌سازی**

### our_entities — نکته ساختاری مهم

⚠️ هر استعلام قیمت از تأمین‌کننده و هر خرید نهایی **باید از طریق یکی از شرکت‌های خود گروه** (نه مستقیماً توسط پولاد تجهیز آپادانا ایران) انجام بشه — مثلاً شعبه ایتالیا، ترکیه، لهستان یا دفتر مرکزی ایران. این با `business_partners` (که طرف‌های تجاری بیرونی مثل مشتری/تأمین‌کننده رو نگه می‌داره) کاملاً فرق داره — `our_entities` خودمون هستیم، نه بیرونی.

- `supplier_rfqs.our_entity_id` (دامنه ۳): مشخص می‌کنه استعلام از طریق کدوم شرکت ما ارسال شده
- `purchase_orders.our_entity_id` (دامنه ۵): مشخص می‌کنه خرید نهایی از طریق کدوم شرکت ما صادر می‌شه — معمولاً باید همون شرکت RFQ اولیه باشه، ولی چون قانون کسب‌وکاری نرمه (نه محدودیت سخت)، در سطح اپلیکیشن به‌عنوان مقدار پیش‌فرض/هشدار پیاده‌سازی می‌شه، نه Constraint دیتابیس

**📌 یادداشت قدیمی (قبل از فاز ۳۶):** نیاز به یک پنل مدیریتی جدا برای `our_entities` بود که بتونه شرکت‌های گروه رو تعریف/ویرایش کنه؛ فاز ۳۶ همین بخش سربرگ/تماس رو پیاده کرد (زیر). حساب‌های مالی/Ledger هرکدوم همچنان خارج از اسکوپه، به‌عنوان یادداشت آینده باقی می‌مونه.

### پنل مدیریتی «شرکت‌های ما» (فاز ۳۶) ✅

⚠️ ۴ شرکت seed‌شده (پولاد تجهیز آپادانا، General Trading srl، Landa Controls، Pasifik Global Makina) دیگه فقط از طریق کد قابل ویرایش نیستن — `prisma/seed.ts` → `upsertOurEntities()` حالا با `update: {}` اجرا می‌شه (فقط رکورد جدید در صورت نبود create می‌کنه)، تا اجرای مجدد seed ویرایش‌های ادمین رو پاک نکنه.

- Migration `0022_our_entities_admin_fields`: شش ستون nullable روی `our_entities` اضافه شد — `entity_name_en` (برای پیشنهاد/پیش‌فاکتور دوزبانه، فاز ۳۷)، `address`, `phone`, `email`, `website`, `logo_url` (سربرگ/تماس).
- ماژول بک‌اند جدید `OurEntitiesModule` (به‌جای افزودن به `RfqsModule` قدیمی — `listOurEntities()` قبلی از `RfqsController`/`RfqsService` حذف و اینجا بازسازی شد):
  - `GET /our-entities` — بدون تغییر نسبت به قبل: عمومی برای هر کاربر لاگین‌شده، فقط رکوردهای `status='active'` (مصرف‌کننده: دراپ‌داون‌های RFQ/PO)
  - `GET/POST/PATCH /our-entities-admin[...]` — پشت پرمیژن جدید `our_entities.manage` (پیش‌فرض فقط مدیریت، طبق `ALL_PERMISSION_KEYS`)؛ `GET` بدون فیلتر وضعیت (فعال+غیرفعال) برای پنل مدیریتی
- `short_code` هنوز `UNIQUE` است؛ create/update هر دو قبل از نوشتن یک چک صریح تکراری‌بودن کد انجام می‌دن (پیام خطای فارسی، نه صرفاً خطای DB constraint)
- فرانت: صفحه‌ی جدید `/our-entities` (زیر «تنظیمات»، آیکون `Landmark`، فقط `our_entities.manage`) با همون الگوی بصری لیست/جزئیات/فرم `PartnersPage.tsx` — آپلود لوگو از همون `uploadFile()` مشترک استفاده می‌کنه، بدون endpoint آپلود جدید

### site_settings — تنظیمات سراسری سامانه (فاز ۴۱-ب) ✅ 🆕

⚠️ سه قابلیت مستقل جدید بعد از فاز ۴۰: تنظیمات ظاهری شخصی (پالت رنگی/دارک‌مود/سایز فونت)، پس‌زمینه صفحه ورود، و پیام اعلامی مدیر — هرکدوم مستقل پیاده‌سازی شدن.

- **تنظیمات ظاهری شخصی (پالت رنگی/دارک‌مود/سایز فونت)**: طبق تصمیم صریح کاربر، **کاملاً سمت فرانت و بدون دیتابیس** — فقط در `localStorage` مرورگر (کلید `septa:appearance`) ذخیره می‌شه، نه یک ستون/جدول. رنگ‌های Tailwind به متغیرهای CSS (`--color-*`) تبدیل شدن تا سوییچ پالت/دارک‌مود با تغییر یک attribute (`data-palette`) و کلاس (`.dark`) روی `<html>` انجام بشه، بدون دست‌زدن به صدها کامپوننت موجود.
- **`site_settings`**: جدول تک‌ردیفی (singleton، دقیقاً الگوی `catalog_counters`) برای تنظیمات سراسری سامانه که مدیر تعیین می‌کنه — فعلاً فقط `login_background_url` (تصویر پس‌زمینه صفحه ورود). `updated_by`/`updated_at` برای ردیابی آخرین ویرایش
- ⚠️ **مسیر عمومی بدون احراز هویت**: چون تصویر پس‌زمینه باید **قبل از لاگین** روی صفحه ورود نمایش داده بشه، یک کنترلر کاملاً جدا و بدون `JwtAuthGuard` لازم بود (`GET /public/login-background`) — برخلاف `FilesController` موجود که در سطح کلاس Guard داره. برای جلوگیری از خطر Path Traversal، این مسیر هیچ پارامتری از کلاینت نمی‌گیره؛ مسیر فایل واقعی همیشه سمت سرور از روی همون یک ردیف `site_settings` خونده می‌شه
- دسترسی مدیریت (`PUT /site-settings/login-background`) پشت مجوز جدید `site_settings.manage` (پیش‌فرض فقط گروه «مدیریت»)
- پیام اعلامی مدیر (Broadcast) — نگاه کنید به بخش `broadcast_messages` (دامنه ۹)

---

## دامنه ۲: استعلام ✅

### inquiries

- ⚠️ **دو شماره جدا**: `internal_number` به‌صورت خودکار توسط سیستم تولید می‌شه (برای پیگیری داخلی)، ولی `inquiry_number` توسط کارشناس وارد می‌شه — چون این شماره‌ای‌ست که طرف مشتری/مناقصه‌گزار به استعلام خودش داده (مرجع خارجی)، نه یک شناسه داخلی
- `buyer_id` به `business_partners` وصله (فقط رکوردهایی با `partner_type IN ('customer','both')` — این محدودیت در سطح اپلیکیشن چک می‌شه، نه Constraint دیتابیس، چون Postgres native نمی‌تونه شرط روی جدول دیگه بذاره)
- `sales_expert_id` ≠ `created_by_user_id` وقتی مدیر ثبت کرده ولی مسئولیت رو به کارشناس فروش واگذار کرده
- `status` شامل حالت `suspended` هم هست (برای مواردی که پروژه مشتری موقتاً متوقف شده، جدا از won/lost)
- تعداد اقلام (`item_count`) ستون ذخیره‌شده نیست — از طریق `COUNT(*)` روی `inquiry_items` محاسبه می‌شه

### inquiry_items

- `UNIQUE(inquiry_id, row_index)` تضمین می‌کنه ردیف‌ها در هر استعلام یکتا باشن
- `item_code` به `item_catalog` وصله — یعنی قبل از ثبت قلم در استعلام، باید کالا در کاتالوگ موجود باشه
- ستون `selected_offer_id` (آفر منتخب تأمین‌کننده برای این قلم) بعد از ساخت جدول `supplier_offers` در دامنه ۳ با `ALTER TABLE` اضافه می‌شه، چون در Postgres نمی‌شه به جدولی که هنوز وجود نداره FK زد

### inquiry_item_documents

- مستندات پیوستی هر قلم (نقشه/کاتالوگ) به‌صورت جدول جدا (نه آرایه در خود `inquiry_items`) تا امکان چند فایل برای هر قلم باشه

### inquiry_documents — پیوست‌های کلی سطح استعلام (فاز ۴۱) ✅ 🆕

⚠️ جدا از `inquiry_item_documents` — برای فایل‌هایی که به کل پرونده مربوطن (نه یک ردیف کالای خاص)، مثلاً یک قرارداد یا مشخصات فنی کلی پروژه. کاربر صریحاً درخواست کرد که این نوع پیوست از پیوست ردیف‌ها مجزا باشه.

- هم‌الگوی دقیق `inquiry_item_documents` (فقط FK به‌جای `inquiry_item_id` روی `inquiry_id` می‌شینه) — همون سبک ساده‌ی فایل+نام+آپلودکننده
- **Drag & Drop**: هم در فرم ثبت استعلام جدید (فایل‌ها قبل از ایجاد پرونده در state محلی جمع می‌شن، بعد از ثبت به پرونده‌ی تازه‌ساخته وصل می‌شن — دقیقاً هم‌الگوی پیوست پیش‌ثبت ردیف‌ها که از قبل وجود داشت) و هم در تب اطلاعات پرونده‌ی موجود (همیشه قابل‌مشاهده/آپلود، نه فقط در حالت ویرایش، چون آپلود فایل نیازی به «ویرایش فیلدهای متنی» نداره) فعاله
- کامپوننت مشترک جدید `FileDropZone` (`apps/web/src/components/ui/FileDropZone.tsx`) این قابلیت رو پیاده می‌کنه — الگوی `dragCounter` که از قبل در `ChatPage.tsx` بود، دور تابع مشترک `uploadFile()` پیچیده شده؛ در فازهای ۴۲/۴۹ هم مجدداً استفاده شد (پیوست ردیف/کل آفر تأمین‌کننده)

### inquiry_discussions — فید ترکیبی چت + لاگ فعالیت ✅ (نسخه ۲)

⚠️ **این جدول دو نوع محتوا داره** (`entry_type`):
- `message`: چت آزاد بین افراد درگیر در پرونده
- `activity`: لاگ خودکار سیستم برای هر اقدام مهم روی پرونده (تکمیل یک مرحله، بارگذاری فایل، تغییر وضعیت، تأیید/قفل شدن یک بخش و...) — همراه با نام عامل اون اقدام (`author_id`)

- گفتگو/فعالیت در سطح کل استعلام (نه هر RFQ جدا) — طبق تصمیم قبلی
- `mentioned_user_id` برای جلب توجه یک کارشناس خاص (مثلاً وقتی کارشناس بازرگانی از فروش سوال فنی می‌پرسه)
- `metadata` (JSONB): جزئیات ساختاریافته برای ورودی‌های `activity` (مثلاً نام فایل، ماژول مبدأ) — برای نمایش آیکون/فرمت مناسب در UI
- ⚠️ **نکته معماری Cross-Cutting**: هر ماژولی که روی این پرونده کار می‌کنه (RFQ، انتخاب نهایی، پیشنهاد، نتیجه، سفارش، PO، حمل، تسویه) باید هنگام اقدامات مهم (نه هر تغییر جزئی فرم) یک رکورد `activity` اینجا درج کنه. این منطق در سطح اپلیکیشن (مثلاً یک Middleware/Service مشترک) پیاده می‌شه، نه در منطق یک ماژول خاص — تا نیازی به تکرار کد در همه ماژول‌ها نباشه

### comment_text_restricted — محرمانگی اطلاعات بازرگانی از دید فروش (فاز ۳۴) ✅ 🆕

⚠️ بازخورد کاربر: فروش باید بتونه مرحله‌ی فعلی استعلام رو ببینه ولی نه جزئیات بازرگانی (اسم تأمین‌کننده، مبلغ) داخل فید گفتگو/لاگ.

- `ActivityLogService.log()` یک پارامتر اختیاری `restrictedText` می‌گیره؛ نقاط تولید لاگ در `rfqs.service.ts` (ثبت RFQ، ارسال ایمیل، ثبت/اصلاح آفر، بدون‌پاسخ خودکار) و `selection.service.ts` (انتخاب آفر) یک نسخه‌ی عمومی هم‌زمان با نسخه‌ی کامل پاس می‌دن — مثلاً به‌جای «استعلام … از طریق «General Trading srl» برای «Siemens AG» ثبت شد» → «استعلام با ۲ قلم برای ۱ تأمین‌کننده ثبت شد»
- مجوز جدید `inquiry.view_commercial_details` (ماژول `inquiry`، پیش‌فرض فقط بازرگانی+مدیریت). `InquiriesService.listDiscussions()` چک می‌کنه کاربر این مجوز رو داره یا نه؛ اگه نداره، برای هر ردیفی که `comment_text_restricted` پر داره، همون رو به‌جای `comment_text` کامل برمی‌گردونه — فیلتر در سطح سرویس انجام می‌شه (نه دیتابیس)، چون بستگی به کاربر درخواست‌کننده داره
- تب‌های RFQ/انتخاب نهایی/PO خودشون از قبل با `rfq.view`/`selection.view`/`po.view` گیت شدن (فروش این‌ها رو نداره) — این فاز فقط فید گفتگو/لاگ رو که در همه‌ی تب‌ها مشترکه فیلتر می‌کنه
- ⚠️ داده‌ی قبل از این فاز `comment_text_restricted = NULL` داره (چون هنوز نسخه‌ی محدود تولید نمی‌شد) — یعنی رکوردهای قدیمی همچنان کامل نمایش داده می‌شن؛ فقط لاگ‌های تولیدشده بعد از این فاز محدود می‌شن

---

## دامنه ۳: تأمین ✅

### supplier_rfqs / rfq_items

- یک RFQ = یک تأمین‌کننده + مجموعه‌ای از ردیف‌های استعلام (`rfq_items` رابطه چندبه‌چند با `inquiry_items`)
- اگر چند تأمین‌کننده هدف باشن، چند رکورد `supplier_rfqs` جدا ساخته می‌شه
- ⚠️ `our_entity_id` الزامیه — هر RFQ باید مشخص کنه از طریق کدوم شرکت گروه ما ارسال می‌شه (نگاه کنید به بخش `our_entities`)
- وضعیت پیش‌فرض `awaiting_response` (در انتظار پاسخ). کارشناس بازرگانی به‌صورت دستی پاسخ تأمین‌کننده رو ثبت می‌کنه: یا «سوال فنی داشت» (وضعیت → `technical_question` + درج خودکار در `inquiry_discussions` با `tag='technical_question'` و `source_rfq_id`) یا «پیشنهاد قیمت داد» (وضعیت → `offer_received` + ثبت `supplier_offers`)
- `response_due_date` برای پیاده‌سازی قانون خودکار «بدون پاسخ» — یک Job زمان‌بندی‌شده (Cron) در بک‌اند باید وضعیت رو بعد از این تاریخ به `no_response` تغییر بده (فقط اگه هنوز `awaiting_response` مونده باشه)
- `email_subject` در سطح اپلیکیشن پیش‌فرض روی `inquiries.internal_number` تنظیم می‌شه

### supplier_offers / supplier_offer_items / supplier_offer_documents

- ⚠️ **ثبت خودکار زمان پاسخ**: `received_at` یک `TIMESTAMPTZ` با `DEFAULT now()` است — یعنی لحظه‌ای که کارشناس بازرگانی پاسخ رو در سیستم ثبت می‌کنه، خودکار و بدون دخالت دستی ذخیره می‌شه. این فیلد (نه `offer_date` که تاریخ روی سند خود تأمین‌کننده‌ست) مبنای گزارش «میانگین سرعت پاسخ‌دهی هر تأمین‌کننده» است: `AVG(supplier_offers.received_at - supplier_rfqs.sent_date)`
- به همین ترتیب، برای مسیر «سوال فنی»، `inquiry_discussions.created_at` (که خودش خودکاره) معیار زمان پاسخ برای اون مسیره
- **جمع کل پیش‌فاکتور تأمین‌کننده**: `vat_applicable` + `vat_rate_percent` + `other_costs` (حمل/بسته‌بندی) روی خود آفر ذخیره می‌شن؛ جمع کل (ارزش کالاها + VAT + سایر هزینه‌ها) در سطح اپلیکیشن محاسبه و در UI زنده نمایش داده می‌شه، نه یک ستون ذخیره‌شده (چون از ضرب قیمت×تعداد هر قلم مشتق می‌شه و تغییرپذیره تا قبل از قفل‌شدن آفر)
- **اطلاعات فنی هر قلم** (`part_number`, `technical_specs`, `datasheet_url`, `country_of_origin`) همزمان با قیمت توسط کارشناس بازرگانی ثبت می‌شه — چون بعداً مستقیماً برای تهیه «پیشنهاد فنی» (دامنه ۴) استفاده می‌شه، بدون نیاز به هماهنگی مجدد با تأمین‌کننده. `country_of_origin` ممکنه با کشور خود تأمین‌کننده فرق داشته باشه (مثلاً توزیع‌کننده هلندی، کالای ساخت آلمان)
- `general_remarks` روی خود آفر برای ملاحظات کلی که به قلم خاصی مربوط نیست (مثلاً شرایط بسته‌بندی کلی، گارانتی عمومی)
- هر آفر می‌تونه شامل چند قلم باشه (`supplier_offer_items`)، هر قلم با قیمت/زمان تحویل/مشخصات فنی مستقل
- `UNIQUE(offer_id, inquiry_item_id)` از ثبت تکراری قیمت برای یک قلم در یک آفر جلوگیری می‌کنه

### inquiry_items.selected_offer_item_id / selection_notes / markup_percent / final_sale_price / inquiries.final_delivery_days / selection_locked_at

⚠️ **این یک مرحله مجزا و مخصوص مدیریت/مدیر فروش است** — جدا از مرحله ثبت آفر توسط کارشناس بازرگانی (که در بالا اومد). کارشناس بازرگانی فقط آفرها رو با جزئیات ثبت می‌کنه؛ مدیریت در یک مرحله بعدی همه‌ی آفرهای دریافتی برای هر قلم رو می‌بینه، مقایسه می‌کنه و تصمیم می‌گیره:

- انتخاب آفر نهایی **در سطح هر قلم** انجام می‌شه (نه کل استعلام) — چون ممکنه قلم ۱ از تأمین‌کننده A و قلم ۲ از B انتخاب بشه
- `selection_notes` معیارهای کیفی (سابقه همکاری، کیفیت، ریسک ارزی) رو به‌صورت متنی نگه می‌داره
- `markup_percent` و `final_sale_price` مستقیماً روی `inquiry_items` ثبت می‌شه (نه در دامنه پیشنهاد) چون این تصمیم مدیریتیه؛ `financial_proposal_items` (دامنه ۴) در زمان تولید پیشنهاد، این مقادیر رو **کپی/اسنپ‌شات** می‌کنه
- ⚠️ **فاز ۳۵-الف**: `supplier_offers.distribute_costs` با دو سوییچ مستقل جایگزین شد — `distribute_vat` و `distribute_other_costs` — چون ممکنه مدیریت بخواد فقط VAT توزیع بشه بدون سایر هزینه‌ها (یا برعکس). هرکدوم فعال باشه، سهم متناظرش (VAT یا سایر هزینه‌های آفر) به نسبت قیمت بین اقلام همون آفر توزیع می‌شه و به فی مؤثر هر قلم (مبنای اعمال Markup) اضافه می‌شه — محاسبه در سطح اپلیکیشن انجام می‌شه، نه ستون ذخیره‌شده. ستون قدیمی `distribute_costs` طبق قاعده‌ی Rollback-پذیری حذف نشد ولی دیگه نه خونده می‌شه نه نوشته می‌شه
- `inquiries.manager_note_to_sales`: یادداشت آزاد مدیر خطاب به کارشناس فروش (مثلاً ملاحظات خاص گارانتی/شرایط پرداخت)؛ در سطح اپلیکیشن فقط بعد از `selection_locked_at` به کارشناس فروش نمایش داده می‌شه
- `inquiries.selection_locked_at` / `selection_locked_by`: قفل‌شدن این مرحله. **قانون کسب‌وکاری مهم**: تولید پیشنهاد مالی/فنی (دامنه ۴) در سطح اپلیکیشن فقط بعد از پر شدن `selection_locked_at` مجاز است — یعنی کارشناس فروش تا وقتی مدیریت این مرحله رو قفل نکرده، دسترسی به تولید فایل پیشنهاد نداره
- `inquiry_delivery_options`: ⚠️ به‌جای یک ترم تحویل ثابت، مدیریت می‌تونه **هم‌زمان چند ترم** (مثلاً هم EXW هم CPT هم DDP) به مشتری پیشنهاد بده — هرکدوم با `extra_cost` (هزینه اضافه نسبت به قیمت پایه) و `delivery_days` مخصوص خودش، چون مسئولیت گمرک/بیمه/حمل در هر Incoterm فرق می‌کنه و همین باعث تفاوت هزینه و زمان می‌شه. `UNIQUE(inquiry_id, delivery_term)` از ثبت تکراری همون ترم جلوگیری می‌کنه

---

## دامنه ۴: پیشنهاد به مشتری + نتیجه استعلام ✅

### financial_proposals / technical_proposals

- دو Entity کاملاً مستقل با نسخه‌بندی (`version`) و وضعیت (`status: current/superseded`) جدا از هم — چون ممکنه در زمان‌های متفاوت ارسال بشن
- `UNIQUE(inquiry_id, version)` روی هر دو جدول تاریخچه نسخه‌ها رو تضمین می‌کنه
- ⚠️ **انتخاب ترم تحویل در سطح هر نسخه**: کارشناس فروش برای هر نسخه پیشنهاد مالی مشخص می‌کنه از بین گزینه‌های `inquiry_delivery_options` کدوم ترم (EXW/CPT/DDP/...) رو می‌خواد آفر بده؛ `financial_proposals.chosen_delivery_term` و `delivery_days` همون ترم رو ذخیره می‌کنه و در فایل پیشنهاد قید می‌شه. `delivery_days` پیش‌فرض از `inquiry_delivery_options` میاد ولی **قابل ویرایش دستیه** (نه فقط قیمت‌ها) — چون مذاکره ممکنه روی زمان تحویل هم اثر بذاره، نه فقط قیمت. `technical_proposals.delivery_time_estimate_days` باید با همین عدد هماهنگ بمونه
- ⚠️ **مسیر ثبت قیمت نهاییِ مذاکره‌شده (اصلاح‌شده در فاز ۳۵-ج)**: `final_sale_price` روی `financial_proposal_items` مستقیماً توسط کارشناس فروش ویرایش می‌شه — **دیگه فیلد ورودی `markup_percent` در فرم ویرایش وجود نداره** (خود ستون در دیتابیس برای سازگاری تاریخی باقی مونده، فقط موقع بذر اولیه‌ی نسخه ۱ از `inquiry_items.markup_percent` کپی می‌شه، بعدش دیگه از UI قابل تغییر نیست). قیمت پایه (`inquiry_items.final_sale_price` که مدیریت در مرحله انتخاب نهایی تعیین کرده) هیچ‌وقت تغییر نمی‌کنه؛ اگه مذاکره باعث تغییر قیمت بشه، یک **نسخه جدید** از `financial_proposals` ساخته می‌شه و قیمت جدید در `financial_proposal_items` همون نسخه ثبت می‌شه. به این ترتیب تاریخچه کامل مذاکره (کدوم قیمت در کدوم نسخه پیشنهاد شده) قابل بازیابیه، و وقتی سفارش نهایی (دامنه ۵) ثبت می‌شه، از قیمت **آخرین نسخه پذیرفته‌شده** استفاده می‌کنه، نه از قیمت پایه مدیریت
- `sent_at` (روی هر دو جدول): وقتی کارشناس فروش دکمه «فایل‌ها ارسال شد» رو می‌زنه، این فیلد پر می‌شه و فیلدهای اون نسخه (قیمت، ترم، زمان تحویل) در سطح اپلیکیشن قفل می‌شن. اگه بعداً نیاز به اصلاح بود (مثلاً مشتری چانه زد)، کارشناس فروش دکمه «اصلاح» رو می‌زنه که یک **نسخه جدید** (version+1) با `sent_at IS NULL` می‌سازه — نسخه قبلی با `status='superseded'` و `sent_at` پرش به‌عنوان تاریخچه باقی می‌مونه

### پنهان‌سازی قیمت خرید + گردش تأیید کاهش قیمت (فاز ۳۵-ب و ۳۵-ج) ✅ 🆕

⚠️ بازخورد کاربر: «در قسمت پیشنهاد به مشتری نباید قیمت خرید دیده شود» + «اگر قیمت اصلاح‌شده کمتر از قیمت تعیین‌شده توسط مدیر باشد باید تأیید مدیر گرفته شود».

- **پنهان‌سازی قیمت خرید (فاز ۳۵-ب)**: `financial_proposal_items.purchase_price` و `effectivePrice` (فی مؤثر مرحله انتخاب) دیگه هیچ‌وقت در پاسخ API تب «پیشنهاد به مشتری» برنمی‌گردن — نه در `formatFinancial()` نه در `baselineItems`. خود ستون‌ها در دیتابیس دست‌نخورده می‌مونن (فقط برای محاسبه‌ی داخلی سرور استفاده می‌شن)، فقط از پاسخ حذف شدن. UI به‌جاش فقط `inquiry_items.final_sale_price` (قیمت پایه‌ی تعیین‌شده‌ی مدیریت، نه قیمت خرید) رو به‌عنوان مرجع فقط‌خواندنی نشون می‌ده
- **گردش تأیید کاهش قیمت (فاز ۳۵-ج)**: وقتی کارشناس فروش هنگام ذخیره‌ی پیشنهاد مالی، `final_sale_price` ای کمتر از `inquiry_items.final_sale_price` (خط پایه) وارد می‌کنه، مقدار **مستقیماً اعمال نمی‌شه** — به‌جاش یک رکورد در `financial_proposal_price_change_requests` ساخته/به‌روزرسانی می‌شه (هم‌ساختار `shipment_edit_requests` فاز ۲۷) و به همه‌ی دارندگان مجوز جدید `proposal.approve_price_reduction` (پیش‌فرض فقط مدیریت) یک اعلان قابل‌اقدام (`notifications.actions` با دکمه‌های تأیید/رد) می‌ره. تا زمان تصمیم، `financial_proposal_items.final_sale_price` قبلی دست‌نخورده می‌مونه و UI یک نشان «در انتظار تأیید مدیر» کنار همون ردیف نشون می‌ده. تأیید → `final_sale_price` واقعاً به مقدار درخواستی به‌روزرسانی می‌شه؛ رد → هیچ تغییری اعمال نمی‌شه. اگه بعداً کارشناس فروش قیمتی در سطح یا بالای خط پایه وارد کنه، هر درخواست معلق همون قلم خودکار رد می‌شه (چون دیگه لازم نیست)
- **دکمه انصراف**: چون فرم قیمت‌گذاری این فاز به الگوی «بافر محلی + دکمه ثبت صریح» (فاز ۳۲) پیوست، یک دکمه‌ی «انصراف» هم کنار «ذخیره» اضافه شد که فقط state محلی فرم رو به آخرین مقدار سرور برمی‌گردونه — بدون تماس شبکه

### تولید سند PDF/Word پیشنهاد مالی/فنی (فاز ۳۸) ✅ 🆕

⚠️ بازخورد کاربر (فرمت دقیق سند): سربرگ شامل لوگوی شرکت ما + مشخصات فروشنده/خریدار + شماره/تاریخ/اعتبار پیش‌فاکتور + کارشناس استعلام + شماره استعلام؛ جدولی با ردیف/پارت‌نامبر/شرح/برند/کشور سازنده/مقدار/قیمت واحد/قیمت کل/زمان تحویل؛ جمع کل زیر جدول + مشخصات کلی (ترم تحویل و...)؛ فوتر با آدرس/کدپستی/شماره ثبت/ایمیل/تلفن شرکت ما. پیشنهاد فنی همون قالب رو می‌گیره **بدون ستون‌های قیمت** (تفسیر: متن کاربر در جمله‌ی آخر «برای پیشنهاد مالی هم …» رو با توجه به تکرار عبارت و هم‌راستایی با طراحی از پیش موجود `technical_proposals`/فاز ۳۵-ب، معادل «پیشنهاد فنی» در نظر گرفتیم).

- **`our_entities.postal_code` / `registration_number`** (Migration `0024_our_entities_footer_fields`): دو ستون nullable جدید، مخصوص فوتر سند — قبلاً در فاز ۳۶ فقط `address`/`phone`/`email`/`website`/`logo_url` اضافه شده بود، کدپستی و شماره ثبت برای فوتر رسمی سند کم بودن
- **`financial_proposals.our_entity_id` / `technical_proposals.our_entity_id`** (Migration `0025_proposal_our_entity`): مشخص می‌کنه سند این نسخه از پیشنهاد تحت کدوم شرکت گروه (`our_entities`) صادر می‌شه — nullable و **در سطح هر نسخه** ذخیره می‌شه (هم‌الگوی `currency_code`/`chosen_delivery_term` که قبلاً per-version بودن، نه یک فیلد ثابت در سطح `inquiries`). هنگام ساخت نسخه‌ی اول (`getOrSeedFinancial`/`getOrSeedTechnical`) پیش‌فرض روی شرکت ایرانی گروه (`short_code='پ ت'`) ست می‌شه؛ نسخه‌های بعدی (`reviseFinancial`/`reviseTechnical`) همون مقدار نسخه‌ی قبلی رو کپی می‌کنن
- ⚠️ **Fallback برای رکوردهای قدیمی‌تر از این فاز**: نسخه‌هایی که قبل از Migration `0025` ساخته شدن `our_entity_id = NULL` دارن. `ProposalService.resolveOurEntity()` در سطح اپلیکیشن (نه ستون) این حالت رو با همون شرکت پیش‌فرض (`پ ت`) پر می‌کنه، بدون اینکه چیزی رو در دیتابیس بازنویسی کنه — یعنی سند تولیدشده برای یک پیشنهاد قدیمی هم سربرگ کامل داره
- **ماژول جدید `apps/api/src/proposal-documents/`**: یک متد اسمبلی داده‌ی مشترک `ProposalService.getDocumentData(inquiryId, kind)` (نیازمند `inquiries.selection_locked_at` پرشده، وگرنه خطا — همون قانون کسب‌وکاری موجود «تولید پیشنهاد فقط بعد از قفل انتخاب نهایی») یک شیء کامل شامل خریدار/فروشنده/اقلام/جمع کل می‌سازه؛ دو رندرر مستقل ولی هم‌محتوا از همین یک خروجی مصرف می‌کنن:
  - **PDF**: `pdf-renderer.service.ts` با `puppeteer` (رندر HTML→PDF Chromium headless — تنها راه قابل‌اتکا برای تایپوگرافی فارسی/RTL و فونت Vazirmatn پروژه) — چون `puppeteer` نسخه‌ی جدید یک بسته‌ی ESM-only است و خروجی این پروژه CommonJS، از یک `import()` دینامیک واقعی (نه استاتیک، تا TypeScript آن را به `require()` تبدیل نکند) برای بارگذاری آن در Runtime استفاده می‌شه
  - **Word**: `docx-renderer.service.ts` با پکیج `docx` (ساخت برنامه‌ای فایل `.docx`، قالب مستقل از HTML)
  - قالب مشترک fa/en در `html-template.builder.ts` (دیکشنری `LABELS` دوزبانه + `bilingual()` که در نبود ترجمه به فارسی fallback می‌کنه) — پیشنهاد مالی ستون‌های «قیمت واحد»/«قیمت کل» + جمع کل رو داره؛ پیشنهاد فنی همون جدول رو بدون این دو ستون می‌سازه
  - Endpoint: `POST inquiries/:id/proposal/financial/generate` و `POST inquiries/:id/proposal/technical/generate` (`format=pdf|docx`, `lang=fa|en` در body) — پشت مجوز موجود `proposal.generate` (از قبل در کاتالوگ بود، این فاز اولین استفاده‌ی واقعی‌شه). فایل تولیدشده با `StorageService` موجود ذخیره و `fileUrl` روی همون رکورد `financial_proposals`/`technical_proposals` (فیلد `file_url` از قبل موجود) ست می‌شه — دقیقاً جای همون فیلدی که آپلود دستی هم پر می‌کنه، پس آپلود دستی برای موارد سفارشی همچنان کار می‌کنه و جایگزین نشده
  - Frontend (`ProposalTab.tsx`): کنار بخش آپلود دستی موجود، دراپ‌داون فرمت (PDF/Word) + زبان (فا/EN) + دکمه «تولید سند» اضافه شد که همون endpoint رو صدا می‌زنه و بعد از موفقیت بخش «فایل فعلی» رو رفرش می‌کنه

### اصلاحات سند PDF/Word + شرکت صادرکننده قابل‌انتخاب + رفع باگ پیش‌نمایش PDF (فاز ۳۹) ✅ 🆕

⚠️ بازخورد کاربر بعد از تست زنده‌ی فاز ۳۸: چند مورد قالب‌بندی سند + یک باگ واقعی در پیش‌نمایش فایل داخل برنامه.

- **`users.full_name_en`** (Migration `0026_bilingual_names`): نام لاتین کاربر، خودسرویس در پروفایل قابل ثبته؛ در اسناد انگلیسی به‌جای `full_name` فارسی استفاده می‌شه (برچسب سند: «کارشناس استعلام» / *Sales Expert*)
- **`our_entities.address_en`** (همون Migration `0026`): آدرس انگلیسی شرکت گروه، در فوتر و سربرگ سند نسخه‌ی انگلیسی به‌جای آدرس فارسی نشون داده می‌شه
- **شرکت صادرکننده اسناد قابل‌انتخاب**: `SaveFinancialProposalDto`/`SaveTechnicalProposalDto` حالا `ourEntityId` اختیاری می‌گیرن؛ در `ProposalTab.tsx` یک Select «شرکت صادرکننده اسناد» به فرم بافرشده‌ی هر دو پیشنهاد (مالی/فنی، مستقل از هم) اضافه شد که با دکمه‌ی «ذخیره» موجود (الگوی فاز ۳۲) پرسیست می‌شه — قبلاً این مقدار فقط یک‌بار موقع seed نسخه ۱ («پ ت» پیش‌فرض) تعیین می‌شد و هیچ‌وقت از UI قابل تغییر نبود
- **حذف فیلد «کارشناس طرف خریدار» از سند**: بازخورد کاربر («به‌جای کارشناس طرف خریدار از کارشناس استعلام استفاده شود») به‌عنوان حذف این فیلد اضافی (که فراتر از مشخصات فاز ۳۸ در نسخه‌ی اول اضافه شده بود) تفسیر شد، چون سند از قبل یک فیلد «کارشناس استعلام» معتبر در سربرگ داره؛ نمایش دوباره‌ی یک کارشناس دوم در باکس خریدار حذف شد
- **عرض ستون‌های جدول ثابت + wrap خودکار**: `table-layout: fixed` (HTML) و `TableLayoutType.FIXED` + `WidthType.PERCENTAGE` per-cell (پکیج `docx`) — متن بلند به‌جای بزرگ‌کردن ستون، چندسطری می‌شه؛ عرض هر ستون به‌صورت درصد ثابت در `format-helpers.ts`-مانند آرایه‌های `FINANCIAL_COL_WIDTHS`/`TECHNICAL_COL_WIDTHS` تعریف شده (هم در `html-template.builder.ts` هم `docx-renderer.service.ts`، باید هماهنگ نگه داشته بشن)
- **عنوان ستون «کشور سازنده» → «CO»**: هم نسخه فارسی هم انگلیسی، برای صرفه‌جویی در عرض ستون
- **نماد ارز کنار مبلغ**: تابع مشترک `formatMoney()` در `apps/api/src/proposal-documents/format-helpers.ts` — نگاشت `CURRENCY_SYMBOLS` (USD:`$`، EUR:`€`، GBP:`£`، CNY:`¥` پیشوند؛ TRY:`₺`، PLN:`zł`، AED:`د.إ` پسوند؛ IRR جدا: «ریال»/`IRR`)
- **مشخصات فنی تأمین‌کننده + نشان «معادل» در سند**: `getDocumentData()` حالا `technicalSpecs` و `isEquivalent` رو هم از baseline (که از قبل این‌ها رو از `supplier_offer_items` منتخب می‌خوند، طبق طراحی دامنه ۳) به آیتم‌های سند اضافه می‌کنه — قبلاً فقط `partNumber` منتقل می‌شد، نه مشخصات فنی کامل؛ در سند، مشخصات فنی به‌صورت یک خط توضیح زیر شرح کالا و نشان «معادل»/*Equivalent* کنار توضیح (وقتی `isEquivalent=true`) نمایش داده می‌شه
- **واحد کالا bilingual**: `translateUnit()` در `format-helpers.ts` — نگاشت ۱۵ واحد seed‌شده‌ی `measurement_units` به معادل انگلیسی (عدد→pcs، کیلوگرم→kg، ...)؛ واحد سفارشی/ناشناخته در نسخه انگلیسی به همون متن فارسی fallback می‌کنه (چون خود جدول `measurement_units` معادل انگلیسی نداره)
- **یک‌کلیک تولید+دانلود**: دکمه‌ی «تولید سند» به «دریافت فایل سند» تغییر نام داد و رفتارش با دکمه‌ی دانلود قبلی ادغام شد — کلیک، هم‌زمان تولید سرور و دانلود خودکار (`downloadFile()`) رو انجام می‌ده؛ حین تولید یک نوار پیشرفت نامعین (Indeterminate Progress Bar، انیمیشن CSS محلی در خود کامپوننت) زیر دکمه‌ها نمایش داده می‌شه؛ دکمه‌های «آپلود فایل» دستی و «فایل فعلی» (برای دسترسی مجدد بدون تولید دوباره) دست‌نخورده موندن

⚠️ **باگ واقعی کشف‌شده و رفع‌شده — پیش‌نمایش PDF داخل برنامه**: تست زنده نشون داد فایل تازه‌تولیدشده‌ی پیشنهاد اصلاً پیش‌نمایش نمی‌شد («این نوع فایل قابل پیش‌نمایش داخل برنامه نیست»)، با اینکه `FileViewer` از قبل از PDF (از طریق `<iframe>`) پشتیبانی می‌کرد. ریشه‌ی باگ: چند جای برنامه (`ProposalTab.tsx`، `OrderTab.tsx`، `EmployeesTab.tsx`، `FlexPaymentList.tsx`، ...) یک برچسب فارسی ثابت بدون پسوند به‌عنوان `fileName` پاس می‌دن (مثلاً «فایل پیشنهاد»، «فایل قرارداد») و تشخیص previewable/MIME در `FileViewer.tsx` از همون رشته پسوند استخراج می‌کرد — یعنی تشخیص «PDF هست یا نه» به‌جای مسیر واقعی فایل، به یک برچسب دلخواه UI وابسته بود. راه‌حل معماری (نه پچ تک‌تک صدازننده‌ها): `FileViewer`/`FilePreviewModal` حالا همیشه از پسوند واقعی `fileUrl` (مسیر ذخیره‌شده در Storage) استفاده می‌کنن، نه از `fileName` نمایشی؛ تابع جدید `resolveDownloadName(fileUrl, fileName)` هم پسوند واقعی رو برای نام پیشنهادی دانلود اضافه می‌کنه اگه `fileName` نداشتش — این یعنی تمام محل‌های استفاده‌ی موجود از `FileViewer` در کل برنامه (نه فقط پیشنهاد) یک‌جا درست شدن، بدون تغییر در خود آن‌ها.

### رفع باگ توزیع VAT/سایر هزینه‌ها + شماره‌گذاری قابل‌ردیابی + خروجی Excel + بارکد + ساده‌سازی UI (فاز ۴۰) ✅ 🆕

⚠️ بازخورد کاربر بعد از تست زنده‌ی مرحله «انتخاب نهایی»: تیک‌زدن توزیع VAT/سایر هزینه‌ها اثری روی قیمت‌ها نداشت + چند درخواست طراحی/فرمت جدید برای اسناد پیشنهاد.

- **رفع باگ پیش‌نمایش زنده‌ی توزیع VAT (فاز ۴۰-الف)**: ریشه‌ی باگ در دیتابیس یا فرمول سرور نبود (`computeEffectiveUnitPrice` در `selection.service.ts` از قبل درست بود) — مشکل صرفاً در UI بود: پیش از ذخیره، `SelectionTab.tsx` همیشه از مقدار `effectivePrice` قدیمیِ برگشتی از سرور استفاده می‌کرد، نه از state محلیِ چک‌باکس‌های تازه‌تیک‌خورده. راه‌حل: `SelectionService.getSelection()` حالا چهار فیلد ورودی لازم برای محاسبه رو هم برمی‌گردونه (`vatApplicable`, `vatRatePercent`, `otherCosts`, `subTotal` — همه از قبل روی `supplier_offers` وجود داشتن، فقط در پاسخ API نبودن)؛ فرانت یک تابع `computeLiveEffectivePrice()` اضافه کرد که دقیقاً همون فرمول سرور رو روی state محلی (`distributeDraft`) اجرا می‌کنه، پس با هر تیک/آنتیک چک‌باکس، «فی مؤثر» و «قیمت فروش نهایی پیش‌نمایش» بی‌درنگ (بدون نیاز به «ذخیره») به‌روز می‌شه. بدون تغییر schema.
- **شماره‌گذاری قابل‌ردیابی پیشنهاد بر مبنای شرکت گروه (فاز ۴۰-ب)**: جدول جدید `proposal_counters` (Migration `0027_proposal_counters`) — دقیقاً هم‌الگوی `letter_counters` (دامنه ۸): شمارنده‌ی اتمیک مستقل به‌ازای `(year, our_entity_id)`، جدا از شمارنده‌ی نامه‌ها تا قاطی نشن. سرویس جدید `ProposalNumberService.nextNumber()` (`apps/api/src/proposal/proposal-number.service.ts`) شماره‌ی بعدی رو داخل یک `SELECT ... FOR UPDATE` تراکنشی می‌گیره (race-condition-safe، مثل `LetterNumberService`) و فرمت `{سال}-{short_code}-{سریال ۴رقمی}` تولید می‌کنه (سال بر مبنای `our_entities.calendar_type` همون شرکت — شمسی برای ایران، میلادی برای شرکت‌های خارجی، دقیقاً هم‌قانون نامه‌ها). `getOrSeedFinancial`/`getOrSeedTechnical`/`reviseFinancial`/`reviseTechnical` در `proposal.service.ts` همگی از حالت آرایه‌ای `$transaction([...])` به حالت callback تراکنشی (`$transaction(async (tx) => {...})`) تبدیل شدن تا کوئری خام شمارنده و نوشتن Prisma هر دو در یک تراکنش اتمیک باشن. ⚠️ این شماره‌گذاری فقط برای **نسخه‌های تازه‌ساخته‌شده** اعمال می‌شه — نسخه‌های قدیمی‌تر از این فاز همون شماره‌ی قدیمی خودشون (`{internal_number}-FIN-v{n}`) رو برای همیشه حفظ می‌کنن، بدون بازنویسی گذشته‌نگر (سازگار با قاعده‌ی کلی پروژه: تاریخچه دست‌نخورده می‌مونه).
- **خروجی Excel به‌جای Word (فاز ۴۰-ج)**: پکیج `docx` حذف و `bwip-js` (بارکد، پایین‌تر) اضافه شد؛ `DocxRendererService` حذف شد و `ExcelRendererService` (`apps/api/src/proposal-documents/excel-renderer.service.ts`) با `exceljs` (که پروژه از فاز ۲۵ برای Import/Export کاتالوگ کالا از قبل داشت) جایگزینش شد — قالب Excel دقیقاً هم‌محتوای نسخه‌ی HTML/PDF (همون یک خروجی مشترک `ProposalService.getDocumentData()`) ولی با primitive های بومی ExcelJS بازسازی شد: ردیف تیتر Merge‌شده و وسط‌چین، تصاویر شناور (لوگو/بارکد) با `workbook.addImage()` + `sheet.addImage()`، عرض ستون‌های ثابت (`FINANCIAL_COL_WIDTHS`/`TECHNICAL_COL_WIDTHS`، هم‌ارز آرایه‌های فاز ۳۹ در HTML/docx سابق)، و `views:[{rightToLeft:true}]` برای راست‌چین فارسی. `ProposalDocFormat` از `"pdf"|"docx"` به `"pdf"|"xlsx"` تغییر کرد (در DTO، سرویس، و کوئری‌پارام‌های endpoint موجود — بدون تغییر مسیر endpoint).
- **طراحی سند: عنوان وسط‌چین + لوگوی بزرگ‌تر (فاز ۴۰-د)**: در `html-template.builder.ts` یک بلوک مستقل `.doc-title` (به‌جای `<h1>` داخل سربرگ قبلی) عنوان رو («پیش‌فاکتور»/*Proforma Invoice* برای مالی، «پیشنهاد فنی»/**«Technical Offer»** برای فنی — برچسب انگلیسی فنی هم در همین فاز عوض شد) بالای صفحه و وسط‌چین می‌کنه؛ اندازه‌ی لوگو در CSS از `max-height:56px` به `96px` (و عرض تا `300px`) افزایش یافت. نسخه‌ی Excel هم همین دو تصمیم رو با یک ردیف Merge‌شده‌ی تیتر (فونت ۲۰) و تصویر لوگوی شناور بزرگ‌تر (`170×68`) پیاده می‌کنه.
- **حذف آپلود دستی و «فایل فعلی» (فاز ۴۰-ه)**: بر اساس بازخورد کاربر که دیگه نیازی به آپلود دستی سند نیست، جهش mutation های `setFinancialFile`/`setTechnicalFile` (که فیلد `file_url` موجود روی `financial_proposals`/`technical_proposals` رو مستقیماً از یک فایل آپلودشده پر می‌کردن) از `proposal-api.ts` حذف شدن؛ در `ProposalTab.tsx` دکمه‌ی «آپلود فایل» و بخش «فایل فعلی» (که یک `FileViewer` مجزا نشون می‌داد) هم حذف شدن. ⚠️ endpoint های بک‌اند مربوطه (`PATCH .../financial/file`, `PATCH .../technical/file`) عمداً دست‌نخورده موندن — تصمیم محافظه‌کارانه چون حذفشون ریسک/فایده‌ی کمی داشت و صراحتاً درخواست نشده بود؛ فقط مسیر فرانت‌اند به اونا قطع شد. دکمه‌ی واحد «دریافت فایل سند» (تولید+دانلود یک‌کلیکی، از فاز ۳۹) و دکمه‌ی «تاریخچه نسخه‌ها» بدون تغییر باقی موندن.
- **تاریخچه‌ی نسخه با جزئیات کامل + دانلود مجدد (فاز ۴۰-و)**: `HistoryModal` در `ProposalTab.tsx` بازطراحی شد — هر ردیف نسخه حالا با کلیک باز/بسته می‌شه (`HistoryVersionRow` جدید) و جزئیات کامل همون نسخه رو نشون می‌ده (ترم تحویل، ارز، قیمت هر قلم یا مشخصات فنی هر قلم، شرایط پرداخت، یادداشت مذاکره). چون `useProposalHistory()` از قبل کل رکورد `FinancialProposal`/`TechnicalProposal` هر نسخه (شامل `fileUrl` خود همون نسخه، نه فقط نسخه‌ی جاری) رو برمی‌گردوند، نیازی به تغییر بک‌اند نبود — فقط UI فرانت این داده‌ی از قبل موجود رو کامل نشون نمی‌داد. اگه `fileUrl` اون نسخه‌ی خاص پر باشه (یعنی قبلاً برای همون نسخه سندی تولید شده)، دکمه‌ی «دانلود فایل این نسخه» با همون `downloadFile()` مشترک ظاهر می‌شه؛ وگرنه پیام «برای این نسخه هنوز فایلی تولید نشده است» نشون داده می‌شه — نسخه‌های قدیمی‌تر از فاز ۳۸ (که اصلاً تولید خودکار سند نداشتن) این پیام رو می‌بینن.
- **بارکد روی سند تولیدشده (فاز ۴۰-ز)**: کتابخونه‌ی جدید `bwip-js` (خالص جاوااسکریپت، بدون نیاز به کامپایل native/canvas — عمداً برای سازگاری با محیط sandbox پروژه انتخاب شد) در `apps/api/src/proposal-documents/barcode.helper.ts` — `generateBarcodePng()`/`generateBarcodeDataUri()` یک بارکد Code128 از روی `proposalNumber` همون سند می‌سازن. `ProposalDocumentsService.generate()` این بارکد رو برای هر سه فرمت (PDF/Excel، هر دو زبان) همیشه تولید می‌کنه و به رندرر مربوطه پاس می‌ده: در HTML/PDF یک `<div class="header-barcode">` با تصویر Data-URI کنار سربرگ؛ در Excel یک تصویر شناور مشابه لوگو (طرف مقابل صفحه). بدون تغییر schema — بارکد فقط از `proposalNumber` موجود مشتق می‌شه، جایی ذخیره نمی‌شه.

✅ **رفع شد (تا فاز ۴۱)**: باگ بالا (ویرایش پیشنهاد تأمین‌کننده وقتی قلمش قبلاً در «انتخاب نهایی» انتخاب شده، خطای ۵۰۰ به‌خاطر نقض FK) برطرف شده — `RfqsService.updateOffer()` دیگه با `deleteMany()`+`create()` کار نمی‌کنه؛ به‌جاش روی کلید یکتای `(offerId, inquiryItemId)` با `upsert()` سرجای همون ردیف قدیمی به‌روزرسانی می‌کنه، پس `id` (و در نتیجه FK `inquiry_items.selected_offer_item_id`) دست‌نخورده می‌مونه.

### پیوست‌های کلی + Drag & Drop + اصلاح داده‌ی سند مشتری + چند رفع باگ UX (فاز ۴۱ تا ۴۹) ✅ 🆕

⚠️ بعد از یک دور تست زنده‌ی دیگه، کاربر ۹ بازخورد مستقل داد که چند حوزه رو هم‌زمان لمس می‌کنن (پیوست فایل، داده‌ی سند مشتری، منطق نتیجه نهایی، UX خطا/اسکرول، نام‌گذاری فایل‌ها):

- **پیوست‌های کلی استعلام (فاز ۴۱)**: جدول جدید `inquiry_documents` (بالاتر مستند شد) + کامپوننت مشترک `FileDropZone` با Drag & Drop.
- **پیوست ردیف/کل آفر تأمین‌کننده با Drag & Drop (فاز ۴۲)**: ستون از قبل موجود `supplier_offer_items.datasheet_url` (که تا این فاز هیچ‌جا در DTO/UI سیم‌کشی نشده بود) حالا در `OfferItemDto`/`RfqsService.createOffer`/`updateOffer` پاس داده می‌شه؛ `RfqTab.tsx` هم برای هر ردیف قلم آفر (دیتاشیت تکی) هم برای کل آفر (چندفایلی) از `FileDropZone` استفاده می‌کنه — بدون Migration جدید.
- **نمایش ردیف/پارت‌نامبر به‌جای کد داخلی کالا در «انتخاب نهایی» (فاز ۴۳)**: `SelectionService.getSelection()` حالا `partNumber` رو هم برمی‌گردونه (از `inquiry_items.part_number` که قبلاً select نمی‌شد)؛ هدر هر کارت قلم در `SelectionTab.tsx` از «کد داخلی + شرح» به «ردیف N — PN: ... — شرح — تعداد واحد» تغییر کرد — کد داخلی کالا (`item_code`) دیگه نمایش داده نمی‌شه چون مدیریت باهاش کار نمی‌کنه.
- **⚠️ پارت‌نامبر/شرح فنی تأمین‌کننده به‌جای شرح استعلام در سند مشتری (فاز ۴۴)**: از لحظه‌ای که آفر تأمین‌کننده برای یک ردیف ثبت می‌شه، سند/پیش‌نمایش «پیشنهاد به مشتری» (هم مالی هم فنی) دیگه به شرح خودِ استعلام اشاره نمی‌کنه. `proposal.service.ts` → `getSelectionBaseline()` یک فیلد جدید `customerDescription` محاسبه می‌کنه: `details.technicalSpecs?.trim() || inquiryDescription` — یعنی شرح فنی *تأمین‌کننده* اولویت داره، و fallback به شرح استعلام **فقط** وقتی تأمین‌کننده هیچ شرح فنی‌ای وارد نکرده (برای جلوگیری از ردیف خالی در سند؛ تصمیم مهندسی صریح مستندشده). `getDocumentData()` و `getProposal()` (برای پیش‌نمایش `ProposalTab.tsx`) هر دو از این فیلد به‌جای `description` خام استفاده می‌کنن. پارت‌نامبر از قبل (فاز ۳۹) از `supplier_offer_items` می‌اومد، این فاز فقط `description` رو هم به همون منطق اضافه کرد. ⚠️ **محدوده‌ی عمدی**: فقط سند/پیش‌نمایش مشتری عوض شد — تب‌های داخلی مدیریتی (انتخاب نهایی، سفارش، PO، حمل) که برای پیگیری اجرایی به شرح خودِ استعلام ارجاع می‌دن، دست‌نخورده موندن. به همین دلیل خط تکراری «مشخصات فنی» زیر شرح کالا در رندررهای PDF/Excel (که تا فاز ۳۹ جدا از description نمایش داده می‌شد) حذف شد — چون الان description خودش همون شرح فنیه.
- **رفع مغایرت «شرکت صادرکننده اسناد» (فاز ۴۵)**: ریشه‌ی باگ، نه در سرور بلکه در ترتیب فراخوانی فرانت بود — تولید سند از مقدار *قبلاً ذخیره‌شده* روی رکورد پیشنهاد می‌خوند، نه از دراپ‌داون تازه‌انتخاب‌شده‌ای که هنوز «ذخیره» نشده بود. رفع صرفاً فرانت‌اندی: `generateFinancialDoc`/`generateTechnicalDoc` در `ProposalTab.tsx` قبل از فراخوانی endpoint تولید سند، اگه فرم هنوز ویرایش‌پذیره (`canEdit && !sentAt`)، اول خودکار `saveFinancial()`/`saveTechnical()` رو صدا می‌زنن.
- **نتیجه نهایی فقط برای ردیف‌های دارای پیشنهاد (فاز ۴۶)**: `OutcomeService.getOutcome()`/`saveOutcome()` حالا `inquiry_items` رو با `where: { selected_offer_item_id IS NOT NULL }` فیلتر می‌کنن — ردیف‌هایی که هیچ‌وقت آفری براشون انتخاب نشده (پس چیزی به مشتری پیشنهاد نشده)، اصلاً وارد فرآیند برد/باخت نمی‌شن؛ یعنی «برد کامل» فقط یعنی برد کامل *ردیف‌های دارای پیشنهاد*، نه همه‌ی ردیف‌های خام پرونده. `OutcomeTab.tsx` تعداد ردیف‌های مستثنی‌شده رو هم به کاربر نشون می‌ده.
- **پیام خطای واضح به‌جای «در حال بارگذاری» بی‌پایان (فاز ۴۷)**: چند بخش از `SettlementTab.tsx` (`DeliverySection`، `InvoiceSection`) الگوی `if (isLoading || !data) return <p>در حال بارگذاری...</p>` رو داشتن — بدون شاخه‌ی جدای `isError`، یعنی وقتی کوئری واقعاً با خطا (۴۰۳ دسترسی، یا ۴۰۰ چون مرحله‌ی قبل کامل نشده) برمی‌گشت، پیام بارگذاری برای همیشه می‌موند. الگوی درست (از قبل در `ProposalTab.tsx`/`OutcomeTab.tsx`/`OrderTab.tsx`/`POTab.tsx` بود) اینجا هم پیاده شد: `isLoading` جدا از `isError`، با پیام خطای واقعی از پاسخ سرور.
- **اسکرول خودکار به آخرین پیام در «گفتگو و لاگ فعالیت پرونده» (فاز ۴۸)**: `ActivityFeed.tsx` — چون فید از قدیم به جدید مرتبه (`inquiry_discussions` با `ORDER BY created_at ASC`)، یک `useEffect` روی تغییر تعداد/آخرین‌آیدی پیام‌ها، `scrollTop` کانتینر رو به `scrollHeight` می‌رسونه.
- **سیستم ذخیره‌سازی و نام‌گذاری فایل (فاز ۴۹)**: `StorageService.save()` یک پارامتر سوم اختیاری گرفت: `{ folderHint?, preferredBaseName? }` (Backward-compatible — بدون پارامتر، رفتار قدیمی سال/ماه دست‌نخورده می‌مونه). `folderHint` (معمولاً `inquiries.internal_number`، پاکسازی‌شده با تابع `sanitizeSegment()` برای امنیت مسیر) فایل رو به‌جای پوشه‌بندی سال/ماه، داخل یک پوشه‌ی مشترک با نام همون پرونده ذخیره می‌کنه؛ `preferredBaseName` نام فیزیکی فایل روی دیسک رو از UUID خام به `{نام-پاکسازی‌شده}-{شناسه‌کوتاه}.ext` تغییر می‌ده. `FilesController` یک مسیر دانلود دوم (`GET files/:folder/:name`) کنار مسیر قدیمی (`GET files/:year/:month/:name`) گرفت — هر دو الگو بر مبنای تعداد بخش‌های مسیر از هم تفکیک می‌شن. همه‌ی نقاط آپلود مربوط به یک استعلام مشخص (پیوست کلی، پیوست ردیف، پیوست آفر) حالا `folder=inquiry.internalNumber` پاس می‌دن؛ تولید سند پیشنهاد (`ProposalDocumentsService.generate()`) هم `preferredBaseName` رو `Commercial Offer {proposalNumber}` (مالی) یا `Technical Offer {proposalNumber}` (فنی) می‌ذاره — طبق درخواست صریح کاربر برای نام‌گذاری قابل‌تشخیص.

### inquiry_item_outcomes

⚠️ **این جدول در سطح هر قلم استعلامه، نه کل استعلام** — چون در مناقصات/استعلام‌های چندقلمی، مشتری می‌تونه بعضی اقلام رو به ما بده و بعضی رو به رقیب. پس هر `inquiry_item` نتیجه جدا (برد/باخت/لغو/در انتظار) و دلایل جدا خودشو داره.

- نقطه کلیدی تحلیل برد/باخت (مرحله ۷) — رابطه یک‌به‌یک با `inquiry_items`
- `loss_reason` به‌صورت Enum محدود شده تا گزارش‌گیری آماری روی دلایل باخت ممکن باشه؛ `expert_note` برای جزئیات کیفی اضافه
- **وضعیت کلی استعلام** (`inquiries.status`) در سطح اپلیکیشن بر مبنای جمع نتایج همه اقلامش به‌روزرسانی می‌شه: اگه همه اقلام `won` باشن → `won`؛ همه `lost` → `lost`؛ ترکیبی از هر دو → `partially_won`؛ هنوز همه اقلام نتیجه نگرفتن → `in_progress`
- مرحله ۸ (ثبت سفارش) فقط روی اقلامی که `result='won'` دارن انجام می‌شه — یعنی حتی در یک استعلام «partially_won»، بخش برنده‌شده مسیر اجرایی خودشو طی می‌کنه
- ⚠️ **تسهیل ورود داده در سطح UI**: برای جلوگیری از کندی ثبت، فرم به کارشناس فروش ۴ حالت سریع می‌ده (برد کامل/باخت کامل/لغو/ترکیبی). در حالت «برد کامل» یا «باخت کامل»، فقط یک دلیل مشترک وارد می‌شه و در ذخیره‌سازی برای همه ردیف‌های `inquiry_item_outcomes` تکرار می‌شه. در حالت «ترکیبی»، کارشناس فقط اقلام رو به دو گروه برد/باخت تقسیم می‌کنه و یک دلیل برد مشترک برای گروه برنده + یک دلیل باخت مشترک برای گروه بازنده وارد می‌کنه — نه یک دلیل جدا برای تک‌تک اقلام. **در دیتابیس همچنان هر قلم رکورد جدا داره** (چون ممکنه تحلیل آینده نیاز به گزارش دقیق‌تر در سطح قلم پیدا کنه)؛ این فقط یک میان‌بر در تجربه کاربری‌ست، نه تغییر در گرانولاریتی داده

---

## دامنه ۵: اجرای سفارش ✅

⚠️ **جدایی نقش**: «سفارش مشتری» (`orders`) در اختیار فروشه؛ «سفارش خرید/PO» (`purchase_orders`) کاملاً جدا و در اختیار بازرگانی/مدیریته و ربطی به فروش نداره — این دو در دو تب/صفحه کاملاً مجزا در UI پیاده می‌شن، نه یک فرم واحد.

### orders / order_items

- `order_items` هر قلم رو با تأمین‌کننده و قیمت خرید/فروش نهایی‌شده‌اش نگه می‌داره (کپی از تصمیمات مراحل قبل، برای ثبات تاریخی حتی اگه بعداً آفر/قیمت تغییر کنه)

### customer_payments / issued_guarantees

- `customer_payments` به‌صورت لیستی از رکوردهای پرداخت (نه اقساط منظم) — منعطف برای پیش‌پرداخت + مانده
- ⚠️ `payment_document_file_url` روی هر رکورد پرداخت — پیوست فایل سند پرداخت (فیش/رسید)، هم روی `customer_payments` هم `supplier_payments` هم `invoice_collections`
- `issued_guarantees`: ضمانت‌نامه‌هایی که **ما به مشتری** صادر می‌کنیم (پیش‌پرداخت/حسن انجام کار) — جهت این رابطه برعکس تصور اولیه بود

### purchase_orders / po_items / supplier_payments

- `purchase_orders` جدا از پرداخت تأمین‌کننده — چون تاریخ صدور PO با تاریخ(های) پرداخت فرق می‌کنه
- ⚠️ `our_entity_id` الزامیه — هر PO باید از طریق یکی از شرکت‌های گروه ما صادر بشه (معمولاً همون شرکتی که RFQ اولیه ازش رفته بود). شرکت‌های واقعی گروه: General Trading srl، Landa Controls، Pasifik Global Makina، پولاد تجهیز آپادانا
- ⚠️ نوع حمل و Incoterm از `purchase_orders` حذف شدن — این‌ها دیگه در فرآیند جدای «استعلام حمل» (`freight_rfqs`) تعیین می‌شن، نه در سطح PO
- `supplier_payments` هم مثل `customer_payments` لیستی و منعطفه (پیش‌پرداخت/هنگام تحویل/بعد از تحویل/ترکیبی)

---

## دامنه ۶: تولید، بسته‌بندی، استعلام حمل، حمل و گمرک ✅ (نسخه ۳ — نهایی)

⚠️ **کنترل دسترسی مهم**: ماژول سراسری «مدیریت بارها» (استعلام حمل + پیگیری محموله) **فقط برای بازرگانی و مدیریت** قابل مشاهده‌ست، نه فروش. در مقابل، «بسته‌بندی» و «ثبت تصاویر/تحویل انبار» چون کار روزمره کارشناس فروش روی پرونده خودشه، **داخل صفحه جزئیات همون استعلام** انجام می‌شه، نه در ماژول سراسری.

### production_tracking / production_tracking_logs

- وضعیت کلی تولید روی `production_tracking`، گزارش‌های دوره‌ای پیگیری روی جدول جدا `production_tracking_logs`
- ⚠️ فقط **سه وضعیت**: `in_production` (در حال تولید) → `ready_to_ship` (آماده حمل) → `in_transit` (در حال حمل). `in_transit` وضعیت پایانی/بسته‌ست — پیگیری بیشتر لازم نیست
- وقتی وضعیت به `ready_to_ship` تغییر می‌کنه، اطلاعات محل pickup (آدرس، تلفن محل، نام/ایمیل/تلفن شخص رابط) پرسیده می‌شه — این‌ها برای هماهنگی شرکت حمل لازمه و در `production_tracking` ذخیره می‌شن (`pickup_address`, `pickup_phone`, `pickup_contact_name`, `pickup_contact_email`, `pickup_contact_phone`)
- بسته‌بندی (`packages`) هم در همین مرحله (`ready_to_ship`) تعریف می‌شه، نه در یک وضعیت جدای «تحویل گرفته شد» که قبلاً بود

### packages / package_items — بسته‌بندی ✅ (در صفحه استعلام، نه ماژول سراسری)

⚠️ این بخش **در تب «حمل و گمرک» صفحه جزئیات هر استعلام** انجام می‌شه (توسط کارشناس فروش/بازرگانی مرتبط با اون پرونده)، نه در ماژول سراسری — چون بسته‌بندی مستقیماً به اقلام همون پرونده مربوطه:
- `packages`: هر PO می‌تونه به چند بسته تقسیم بشه؛ هر بسته ابعاد، وزن، و محل بارگیری (انبار واسط یا نزد تأمین‌کننده) داره. ⚠️ `status` پیش‌فرض `defining`ه (در حال تعریف)؛ کارشناس مربوطه در صفحه پرونده دکمه «اعلام آماده حمل» رو می‌زنه تا `ready_to_ship` بشه — **فقط بسته‌های `ready_to_ship` در ماژول سراسری «استعلام حمل» قابل مشاهده/انتخابن**، و اونجا فقط قابل انتخاب هستن (چک‌باکس)، نه قابل ویرایش
- `package_items`: محتوای هر بسته (کدوم قلم و چه تعداد)

### freight_rfqs / freight_rfq_packages / freight_offers — استعلام حمل (ماژول سراسری، فقط بازرگانی/مدیریت)

- مشابه الگوی استعلام کالا از تأمین‌کننده، ولی طرف مقابل شرکت حمل (`business_partners.partner_type = 'freight_forwarder'`) است
- `freight_rfqs.email_subject`: متن ایمیل استعلام — ⚠️ **به انگلیسی** تولید می‌شه و شامل جدولی از بسته‌ها (ابعاد، وزن، تعداد) + آدرس مبدأ بارگیری + گمرک مقصد است (نه فقط یک خط متن ساده)
- `freight_rfq_packages`: کدوم بسته‌ها (از چند PO/پرونده مختلف) در این استعلام گنجانده شدن
- `freight_offers`: قیمت/زمان حمل هر شرکت؛ انتخاب برنده توسط بازرگانی/مدیریت

### shipments — چرخه کامل وضعیت (ماژول سراسری، فقط بازرگانی/مدیریت)

بعد از انتخاب شرکت حمل، `shipments` ساخته می‌شه و یک چرخه وضعیت خطی طی می‌کنه (`shipments.stage`):

1. **`consolidating`**: تجمیع، شرکت حمل هنوز انتخاب نشده
2. **`in_transit`**: بعد از انتخاب شرکت حمل — ⚠️ این یک **مرحله/صفحه جدای مشخص** برای بارگذاری اسناد صادراتی ارسالی (Invoice, Packing List, Non-Dual-Use, POA) است، نه فقط یک تغییر وضعیت ساده
3. **`export_declared`**: دریافت و بارگذاری اظهارنامه صادرات (EX1/Bayanname) از شرکت حمل
4. **`iran_docs_sent`**: آماده‌سازی و ارسال هم‌زمان (به شرکت حمل) + بارگذاری (در سامانه) مدارک سمت ایران — لیست کامل در `import_documents`:
   ثبت سفارش، بیمه‌نامه، Invoice لگ دوم، Packing لگ دوم، **بارنامه**، **قبض انبار**، **ترخیصیه/واگذاری**، **فاکتور ریالی شرکت حمل**، **فاکتور ارزی شرکت حمل**، **گواهی بازرسی**، **گواهی مبدأ**
5. **`customs_declared`**: اظهار گمرکی مقصد؛ بارگذاری اظهارنامه گمرکی
6. **`cleared`**: ثبت هزینه‌های ترخیص در **دو بخش جدا** (`customs_duties_amount` = حقوق و عوارض گمرکی، `clearance_fees_amount` = کارمزد و سایر هزینه‌ها)، به‌علاوه بارگذاری اسناد پایانی خروج از گمرک (`weighbridge_slip_file_url` = قبض باسکول، `customs_exit_waybill_file_url` = بارنامه خروج از گمرک)

- `shipment_packages`: کدوم بسته‌ها (از چند PO/پرونده مختلف) در این محموله هستن؛ برای فهمیدن PO ها از طریق `packages.po_id` می‌شه Join گرفت

### warehouse_receipts / warehouse_receipt_items / warehouse_receipt_photos — دریافت در انبار (در صفحه استعلام، نه ماژول سراسری)

⚠️ **ثبت تصاویر و مقدار دریافتی هر کالا توسط کارشناس فروش و در صفحه جزئیات همون استعلام انجام می‌شه**، نه در ماژول سراسری «مدیریت بارها» — چون این کار روزمره فروشه، نه بازرگانی. ماژول سراسری فقط تا مرحله `cleared` (ترخیص) پیش می‌ره؛ از اونجا به بعد، هر پرونده جداگانه مسئول ثبت دریافت کالای خودشه.
- `warehouse_receipt_items` مقدار دریافتی هر قلم رو جدا نگه می‌داره (با ارجاع به `inquiry_item_id`)
- `warehouse_receipt_photos` به همین جدول وصله — تصاویر مجزا برای هر قلم

### export_documents / export_document_attachments / import_documents

- اسناد صادرات (اروپا) و واردات (ایران) در دو جدول جدا، چون ماهیت و مسئول متفاوتی دارن
- `export_document_attachments` برای مدارک آزاد/اضافی که از قبل پیش‌بینی نشدن

### shipment_documents / shipment_edit_requests — اسناد چندفایلی + قفل مرحله (فاز ۲۷) ✅

⚠️ سه تغییر مرتبط که رفتار «همیشه آزاد» قبلی ماژول محموله رو عوض کردن:

- **اسناد چندفایلی**: ۱۷ ستون تک‌فایلهٔ `*_file_url` پراکنده در `shipments`/`export_documents`/`import_documents` با جدول عمومی `shipment_documents` جایگزین شدن — هر جایگاه (`doc_key`) چند فایل هم‌زمان با آپلودکننده/زمان می‌گیره؛ آپلود جدید **اضافه** می‌شه نه جایگزین (قبلاً آپلود جدید فایل قبلی رو بی‌بازگشت پاک می‌کرد). ستون‌های قدیمی Deprecated ولی باقی موندن (الگوی افزودنی)؛ دادهٔ موجودشون در Migration `0016` منتقل شد. منطق `status='complete'` مدارک صادراتی حالا «هر ۴ جایگاه صادراتی حداقل یک فایل» از جدول جدیده.
- **قفل مرحله**: هر مرحلهٔ عبورشدهٔ چرخهٔ ۶مرحله‌ای، فیلدها و اسنادش در سطح **بک‌اند** (نگاشت فیلد→مرحله در سرویس) قفل می‌شن — قبلاً طبق تصمیم فاز ۱۱ همه‌چیز برای همیشه آزاد بود، این فاز اون تصمیم رو برگردوند. دارندهٔ پرمیژن جدید `shipping.approve_edit` (پیش‌فرض فقط مدیریت) مستقیم می‌تونه ویرایش کنه.
- **درخواست اصلاح** (`shipment_edit_requests`): کارشناس برای مرحلهٔ قفل‌شده درخواست + دلیل ثبت می‌کنه → به همهٔ دارندگان `shipping.approve_edit` اعلان با دکمه‌های تأیید/رد می‌ره — **اولین استفادهٔ واقعی از `notifications.actions`** (JSONB دامنه ۹ که تا این فاز همیشه NULL بود) → تأیید یعنی `shipments.unlocked_stage` = همون مرحله (هم‌زمان حداکثر یک مرحلهٔ باز در هر محموله؛ درخواست pending تکراری رد می‌شه) → درخواست‌دهنده اصلاح می‌کنه و «پایان اصلاح» می‌زنه (`unlocked_stage = NULL`).

---

## دامنه ۷: تحویل و تسویه ✅

### deliveries

- `customer_acceptance_date` / `customer_acceptance_status` جدا از `actual_delivery_date` — چون تایید فنی/کیفی مشتری معمولاً مدتی بعد از تحویل فیزیکی اتفاق می‌افته
- صدور فاکتور (`invoices`) باید در سطح اپلیکیشن فقط بعد از `customer_acceptance_status = 'accepted'` مجاز باشه (Business Rule، نه Constraint دیتابیس)

### invoices / invoice_items / invoice_collections

- ⚠️ **فاکتور نهایی به ریال صادر می‌شه، ولی معامله به ارز خارجی بوده** — پس نمی‌شه یک نرخ ارز ثابت برای کل فاکتور در نظر گرفت. `invoice_items` فاکتور رو به بخش‌ها (مثلاً «پیش‌پرداخت»، «مانده») می‌شکنه و برای هرکدوم نرخ ارز مجزا اعمال می‌کنه:
  - بخش پیش‌پرداخت: نرخ ارز روزی که واقعاً دریافت شده
  - بخش مانده: نرخ ارز روز تحویل/تأیید فنی مشتری
- ⚠️ **`exchange_rate_value` به‌صورت دستی توسط کارشناس مالی وارد می‌شه** — نه خودکار از جدول `exchange_rates` (که برای گزارش‌گیری/تحلیل داخلی استفاده می‌شه، نه صدور رسمی فاکتور). این یعنی `invoice_items` به `exchange_rates` وصل نیست؛ عدد مستقیماً در لحظه صدور فاکتور وارد می‌شه
- `invoices.final_amount_irr` از `SUM(invoice_items.amount_irr)` محاسبه می‌شه
- `invoice_collections` بدون فیلدهای اقساط منظم — طبق تصمیم قبلی حذف شدن؛ به‌صورت رکورد(های) پرداخت منعطف (مشابه الگوی `customer_payments` و `supplier_payments`)

---

## دامنه ۸: مکاتبات و بایگانی اسناد ✅ (نسخه ۲ — اصلاح‌شده)

### business_partners — انواع جدید

⚠️ چون نامه‌ها می‌تونن خطاب به سازمان‌ها، بانک‌ها، و شرکت‌های خدماتی هم باشن (نه فقط مشتری/تأمین‌کننده/شرکت حمل)، `business_partners.partner_type` سه مقدار جدید گرفت: `organization` (سازمان)، `bank` (بانک)، `service_company` (شرکت خدماتی)

### our_entities.short_code / calendar_type

- هر شرکت گروه ما یک کد اختصاری یکتا داره — ⚠️ **شرکت ایرانی** (پولاد تجهیز آپادانا) کد فارسی با فاصله بین حروف داره (`پ ت`)، و از **تقویم شمسی** استفاده می‌کنه؛ **شرکت‌های خارجی** (General Trading srl، Landa Controls، Pasifik Global Makina) کد لاتین دارن (مثلاً `GT`، `LC`، `PGM`) و از **تقویم میلادی** استفاده می‌کنن
- `calendar_type` مشخص می‌کنه سال داخل شماره نامه (`letter_counters.year` و `letters.letter_number`) بر چه مبنایی محاسبه بشه

### letter_counters / letters — شماره‌گذاری بر مبنای شرکت (نه جهت نامه)

⚠️ **اصلاح مهم**: برخلاف طرح اولیه (کد `IN`/`OUT`/`MEMO` برای جهت نامه)، کد داخل شماره نامه حالا **کد اختصاری شرکت صادرکننده/دریافت‌کننده** (`our_entities.short_code`) است — مثلاً `1405-پ ت-0042` برای شرکت ایرانی یا `2026-GT-0005` برای شرکت خارجی. جهت نامه (`letters.type`: دریافتی/ارسالی/داخلی) هنوز یک فیلد جداست، ولی دیگه در فرمت شماره ظاهر نمی‌شه.

- `letter_counters` حالا با `(year, our_entity_id)` کلید می‌خوره، نه `(year, letter_type)` — هر شرکت گروه، شمارنده سالانه مستقل خودش رو داره
- `letters.issuing_entity_id`: مشخص می‌کنه این نامه تحت شماره‌گذاری کدوم شرکت گروه ثبت شده (پایه‌ی گرفتن شماره از `letter_counters`)

### letters — فرستنده/گیرنده ساختاریافته (نه متن آزاد)

⚠️ **اصلاح مهم دیگه**: کاربر دیگه نمی‌تونه فرستنده/گیرنده رو دستی تایپ کنه — باید از موجودیت‌های از پیش تعریف‌شده انتخاب کنه:

- `sender_our_entity_id` / `receiver_our_entity_id`: اگه طرف، یکی از شرکت‌های خودمونه
- `sender_partner_id` / `receiver_partner_id`: اگه طرف، یک شرکت بیرونی (`business_partners`) است
- `sender_contact_id` / `receiver_contact_id`: رابط مشخص در اون شرکت بیرونی (اختیاری — می‌شه فقط سطح شرکت رو انتخاب کرد بدون مشخص کردن شخص)
- **قانون کسب‌وکاری (سطح اپلیکیشن)**: دقیقاً یکی از دو حالت «شرکت خودمون» یا «طرف بیرونی» باید برای فرستنده پر بشه، همین‌طور برای گیرنده — هرگز هر دو هم‌زمان یا هیچ‌کدام

### letters — بازطراحی جهت‌محور (فاز ۲۴) ✅

⚠️ **اصلاح مهم دیگه (بعد از بازخورد کاربر)**: قانون «دقیقاً یکی از شرکت خودمون یا طرف بیرونی» بالا برای فرستنده/گیرنده، ثابت بود برای هر سه نوع نامه. اما در عمل **جهت نامه (`type`) خودش تعیین می‌کنه کدوم طرف چیه**، پس فرم نباید از کاربر بخواد این انتخاب رو دستی تکرار کنه:

- `type='incoming'`: فرستنده همیشه `sender_partner_id` (بیرونی)، گیرنده همیشه `receiver_our_entity_id` (یکی از شرکت‌های ما) — نه دیگه Toggle انتخاب
- `type='outgoing'`: برعکس — فرستنده `sender_our_entity_id`، گیرنده `receiver_partner_id`
- `type='internal'`: فیلدهای فرستنده/گیرنده شرکتی اصلاً استفاده نمی‌شن (همه Null می‌مونن)؛ به‌جاش دو ستون جدید `internal_from_department_id`/`internal_to_department_id` (`REFERENCES departments(id)`، همون جدول واقعی دامنهٔ ۱۱/فاز ۱۸) هویت فرستنده/گیرنده رو مشخص می‌کنن — باید متفاوت باشن («از یک واحد **به یک واحد دیگر**»)
- ⚠️ فیلد قدیمی `department` (رشتهٔ آزاد، مبنای فیلتر دسترسی `correspondence.view_own_department`) برای نامهٔ داخلی **دست‌نخورده و مستقل از** دو ستون جدید بالا می‌مونه — این دو مفهوم متفاوتن (کنترل دسترسی در برابر هویت واقعی فرستنده/گیرنده)

**نامهٔ دریافتی — مسئول پیگیری جایگزین فیلد واحد:**
- ستون جدید `responsible_user_id` (`REFERENCES users(id)`) — هنگام ثبت نامهٔ دریافتی، به‌جای انتخاب واحد (`department`)، ثبت‌کننده یک همکار رو مسئول پیگیری می‌کنه؛ `department` برای این نوع نامه دیگه پر نمی‌شه
- فیلتر دسترسی (`assertVisible`/`list`) یک شرط OR جدید می‌گیره: کاربری که `responsible_user_id`شه، حتی بدون تطابق `department`، نامه رو می‌بینه — دقیقاً جایگزین همون منطق قبلی برای این نوع خاص
- ستون جدید `sender_reference_number` (شمارهٔ نامهٔ طرف فرستنده، فقط برای دریافتی معنا داره)
- تخصیص مسئول یک ردیف `letter_workflow_logs` (`action='referred'`) + یک اعلان خودکار می‌سازه — هم‌الگوی مکانیزم `refer()` دستی موجود، فقط این مسیر خودکار صداش می‌زنه

**نامهٔ ارسالی — امضاکننده(ها):**
- جدول جدید `letter_signers` (چندبه‌چند نامه↔کاربر، چون می‌تونه چند نفر هم‌زمان امضا کنن) — فقط برای `type='outgoing'` پر می‌شه

**بارگذاری تصویر نامه:**
- اجباری بودنش فقط در مرحلهٔ «ثبت رسمی» (`register()`) چک می‌شه، نه در ذخیرهٔ پیش‌نویس — چون کاربر معمولاً اول پیش‌نویس رو با اطلاعات ناقص ذخیره می‌کنه و بعد سند رو پیدا/اسکن می‌کنه

Migration: `0014_correspondence_redesign` (افزودنی، Rollback با `DROP TABLE`/`DROP COLUMN`).

### ارتباط نامه با استعلام/محموله — انتخاب نوع لینک در UI

- `related_inquiry_id` و `related_shipment_id` هر دو Nullable موندن؛ در UI کاربر اول انتخاب می‌کنه «به چی لینک کنم؟» (استعلام/محموله/سایر) و بر اساس انتخاب، فقط فیلد مربوطه رو پر می‌کنه — نیازی به تغییر ساختار دیتابیس برای این نبود، فقط منطق UI

### documents / document_tags

- هر سند می‌تونه به یک نامه وصل باشه یا کاملاً مستقل باشه (`related_letter_id` قابل NULL) — چون ممکنه یک اسکن قرارداد بدون نامه رسمی بایگانی بشه
- `ocr_text`: در صورت فعال‌سازی OCR، متن استخراج‌شده اینجا ذخیره می‌شه و مبنای «جست‌وجوی پیشرفته در متن اسناد» می‌شه
- `document_tags`: رابطه چندبه‌چند ساده برای برچسب‌گذاری آزاد

**📌 پاسخ به سوال OCR:** بله، امکان OCR سبک و رایگان وجود داره — **Tesseract OCR** (متن‌باز، رایگان، پشتیبانی از فارسی) قابل اجراست، هم روی سرور خودتون (self-hosted، بدون هزینه API) هم به‌صورت کتابخانه‌ی Node.js (`node-tesseract-ocr` یا فراخوانی مستقیم باینری). دقتش برای اسناد تایپی خوبه ولی برای دست‌نویس محدودتره. پیشنهاد: OCR به‌صورت یک Job پس‌زمینه (نه هم‌زمان با آپلود) اجرا بشه تا آپلود کاربر کند نشه، و نتیجه بعداً در `ocr_text` ذخیره بشه.

### letter_workflow_logs / letter_audit_log

- ⚠️ این دو جدول **مقاصد متفاوتی** دارن با اینکه شبیه به نظر می‌رسن:
  - `letter_workflow_logs`: مراحل کاری واقعی (ثبت، اسکن، ارجاع، پاسخ، تأیید، ارسال، بایگانی) — این‌ها اقدامات معنادار روی گردش کار نامه‌ان
  - `letter_audit_log`: صرفاً ردیابی دسترسی (چه کسی کِی نامه رو **دید** یا **ویرایش** کرد) — برای حسابرسی امنیتی، نه گردش کار
- `letter_workflow_logs.referred_to_user_id` وقتی `action='referred'` پر می‌شه — این باعث درج یک رکورد در `letter_notifications` هم می‌شه (در سطح اپلیکیشن)

### letter_reminders / letter_notifications

- `letter_reminders`: مهلت پاسخ برای نامه‌های دریافتی — می‌تونه مبنای گزارش «نامه‌های عقب‌افتاده» باشه
- `letter_notifications`: اعلان‌های ساده (مثلاً هنگام ارجاع)؛ در آینده اگه نیاز به اعلان‌های عمومی‌تر (نه فقط نامه) بود، می‌شه این رو به یک جدول اعلان سراسری تعمیم داد

### کنترل دسترسی (هماهنگ با سیستم RBAC دامنه ۱)

- یک ماژول جدید `correspondence` باید به `permissions` اضافه بشه (مثلاً `correspondence.view_own_department`, `correspondence.register`, `correspondence.refer`, `correspondence.archive` و...) — طبق الزام «هر واحد فقط نامه‌های خودش رو ببینه»، فیلتر `letters.department` در سطح اپلیکیشن بر اساس گروه دسترسی کاربر اعمال می‌شه

---

## دامنه ۹: اعلان‌ها، یادآورها (TODO)، و پیام‌رسانی سراسری ✅ 🆕

### notifications

- ⚠️ اعلان‌ها سراسری شدن (نه فقط مختص نامه) و `letter_notifications` قبلی رو جایگزین کردن
- `actions` (JSONB) دقیقاً همون قابلیت «مثل SAP» است — دکمه‌های تأیید/رد یا اقدامات جزئی مستقیماً از داخل خود اعلان، بدون نیاز به رفتن به صفحه مربوطه؛ وقتی کاربر کلیک می‌کنه، `is_actioned`/`actioned_at` پر می‌شه
- `related_entity_type`/`related_entity_id` به‌صورت عمومی (نه FK مستقیم) چون اعلان می‌تونه به هر نوع موجودیتی (نامه، استعلام، RFQ، انتخاب نهایی و...) اشاره کنه

### activities — جایگزین reminders (فاز ۱۶، بعداً «مرکز اقدامات» در فاز ۳۱)

⚠️ `reminders` بالا فقط طرح اولیه بود؛ فاز ۱۶ (Migration `0008_activities`) قبل از هر پیاده‌سازی واقعی جایگزینش کرد با یک Entity عمومی‌تر («کار») چون یک TODO سادهٔ بدون نوع/اولویت/گردش وضعیت برای این‌همه سناریوی متفاوت (تماس، پیگیری، تأیید، کار داخلی) کافی نبود. جزئیات کامل این جدول (و فاز ۱۷ که `outcome_id`/`activity_outcome_templates` رو اضافه کرد) اینجا مستند نشده — فقط دلتای فاز ۳۱ زیر آورده شده تا این سند فعلاً کاملاً بی‌ربط با کد واقعی نباشه؛ سند مرجع کامل‌تر [SPEC-PHASE-16.md](./SPEC-PHASE-16.md) و [SPEC-PHASE-17.md](./SPEC-PHASE-17.md) هستن.

**فاز ۳۱ — مرکز اقدامات (Action Center):** طبق [SPEC-PHASE-31.md](./SPEC-PHASE-31.md)، به‌جای یک Entity کاملاً جدید، همین جدول `activities` بسط پیدا کرد (الگوی «صندوق ورودی فدرال»، مثل SAP Fiori My Inbox):
- `status` مقدار `in_progress` گرفت (بین `open` و `waiting`/`scheduled`) — قبلاً هیچ راهی برای اعلام «شروعش کردم» نبود
- `waiting_reason` (متن، اختیاری) توضیح «منتظر چی/کی؟» رو وقتی `status='waiting'` نگه می‌داره؛ با خروج از این وضعیت خودکار پاک می‌شه
- در سطح اپلیکیشن، یک سرویس تجمیع Read-Only تازه (نه جدول) `activities` رو با تأییدهای پراکندهٔ دامنه ۱۱ (مرخصی/مأموریت/اضافه‌کاری/وام/درخواست عمومی)، درخواست اصلاح محموله (فاز ۲۷) و ارجاع نامه (فاز ۲۴) در یک لیست واحد نشون می‌ده — بدون جابه‌جایی دادهٔ هیچ‌کدوم از این منابع
- پرمیژن جدید `action_center.view_team` (ماژول `action_center`) — دیدن فعالیت‌های تیم، نه فقط عضویت هارد-کدشدهٔ گروه «مدیریت»

**توسعهٔ Work Management (Business Action Hub) — Migration `0035_task_core` + `0036_activity_type_mention`:**

⚠️ بعد از تأیید UX، مرکز اقدامات از یک Inbox خواندنی به یک سیستم کامل مدیریت کار تبدیل شد. تصمیم معماری کلیدی: این غنی‌سازی روی همون جدول `activities` انجام شد (نه Entity جدید) — چون `activities` قبلاً owner/creator/priority/status/outcome/follow-up chaining داشت و فقط سه چیز کم داشت: Watcher، Timeline، و Outcome ساختاریافته‌تر. جدول `activities` خودش دست‌نخورده موند.

- **`task_watchers`** 🆕 — دنبال‌کنندگان یک Task؛ Creator به‌محض ساخت Task خودکار Watcher می‌شه (طبق طراحی UX: «Creator نباید نیاز به گزارش‌گیری داشته باشه، Timeline رو می‌بینه»). افزودن/حذف خودم همیشه آزاده (Self-subscribe)؛ افزودن/حذف شخص دیگه فقط مسئول/سازنده/مدیریت.
- **`task_timeline_entries`** 🆕 — تاریخچهٔ کامل هر Task، دقیقاً هم‌الگوی `inquiry_discussions` (کامنت آزاد + لاگ خودکار سیستم با `entry_type`). `action_kind` روی ردیف‌های خودکار (`created`, `reassigned`, `outcome_recorded`, `watcher_added`, ...) + `metadata` JSONB برای جزئیات ساختاریافته (مثلاً `{outcomeLabel, nextActionAt, sameTask}`).
- **`activity_outcome_templates.effect`** 🆕 — جایگزین منطقی `requires_follow_up` (که برای سازگاری تاریخی باقی مونده، دیگه خونده نمی‌شه): `close` | `create_follow_up` | `keep_waiting`.
  ⚠️ **تصمیم مهم که رفتار قبلی فاز ۱۷ رو برعکس کرد**: طبق بازخورد صریح کاربر بعد از تأیید UX («اگر یک Task نیاز به پیگیری مجدد دارد، لزوماً نباید Task جدید ساخته شود»)، وقتی `effect='create_follow_up'` باشه **پیش‌فرض این نیست که Task جدید ساخته بشه** — همون Task با `dueAt` جدید باز می‌مونه. فقط وقتی کاربر صراحتاً در UI گزینهٔ «به‌عنوان یک کار مستقل ثبت کن» رو انتخاب کنه (`spawnNewTask=true` در `POST /activities/:id/record-outcome`)، رفتار قدیمی فاز ۱۷ (بستن + ساخت Task زنجیره‌شده) اجرا می‌شه.
  قالب‌های پیش‌فرض (`seed.ts`) با `effect` دقیق بازنویسی شدن: «بی‌پاسخ/مشغول/تماس بعداً» → `create_follow_up`، «منتظر تأمین‌کننده/منتظر مشتری» → `keep_waiting` (نه `create_follow_up` — این‌ها منتظر یه نفر دیگه‌ان، نه نیازمند زمان‌بندی خودکار)، بقیه (تأیید‌شد/برگزار‌شد/انجام‌شد/...) → `close`.
- **`activity_type` مقدار جدید `'mention'`** — منشن در گفتگوی پرونده (`inquiry_discussions.mentioned_user_id`) قراره به‌جای فقط یک Notification، یک Task واقعی (`activityType='mention'`) هم بسازه (دکمه «مشاهده و بستن» → همون `POST /activities/:id/complete`). ⚠️ **این فاز فقط زیرساخت (enum + CHECK constraint) رو آماده کرد — خود اتصال به `InquiriesService.addMessage()` هنوز سیم‌کشی نشده**، چون `ActivitiesModule` از قبل `InquiriesModule` رو import می‌کنه (برای `ActivityLogService`) و اتصال معکوس یعنی Circular Module Dependency (نیازمند `forwardRef()` یا Event Bus) — به‌عنوان یک فاز کوچک بعدی موکول شد.
- **Endpoint های جدید** روی همون `ActivitiesController`: `GET /activities/:id` (جزئیات کامل + Watcherها)، `GET /activities/:id/timeline`، `POST /activities/:id/comments`، `POST /activities/:id/reassign`، `POST/DELETE /activities/:id/watchers[/:userId]`، `POST /activities/:id/record-outcome` (endpoint غنی جدید؛ `/complete` قدیمی برای تکمیل سادهٔ بدون انتخاب Outcome — مثل چک‌باکس سریع داشبورد — دست‌نخورده موند).
- **Action Center به رجیستری Provider محور تبدیل شد** — `ActionCenterService` دیگه هیچ منطق دامنه‌ای نداره؛ فقط از یک آرایهٔ `ActionRuleProvider` (چهار Provider فعلی: `ActivityRuleProvider`, `HrApprovalRuleProvider`, `ShipmentEditRuleProvider`, `LetterReferralRuleProvider`، هرکدوم در فایل مستقل خودش) Query می‌زنه و merge/sort می‌کنه. افزودن منبع جدید یعنی یک کلاس تازه + یک خط در Factory Provider ماژول (`action-center.module.ts`) — بدون تغییر `ActionCenterService`.
- **`ActionItem.source`** 🆕 — دستهٔ کسب‌وکاری منبع (`inquiry`/`sales`/`procurement`/`finance`/`payroll`/`shipping`/`documents`/`hr`/`crm`/`system`) برای فیلتر/گروه‌بندی در UI، مستقل از `sourceType` دقیق.
- **UI**: `WorkPanel` (پنل دائمی سمت چپ، کنار Sidebar ناوبری سمت راست — قابل جمع‌شدن، Badge فوریت، ۳ تب کارهای‌من/واگذارشده‌توسط‌من/تیم، اقدامات سریع inline بدون نیاز به باز کردن Drawer) و `TaskDrawer` (جزئیات کامل بدون خروج از صفحه: Timeline، کامنت، ثبت نتیجه با شاخهٔ همین‌Task/Task-مستقل، ارجاع، Watcher). فقط در دسکتاپ (`lg` به بالا) فعاله؛ نسخهٔ موبایل به فاز بعد موکول شد.

### chat_conversations / chat_participants / chat_messages

- ⚠️ این **جدا از `inquiry_discussions`** است — `inquiry_discussions` مخصوص گفتگو+لاگ فعالیت هر پرونده استعلامه، ولی این‌ها پیام‌رسانی عمومی سازمانیه (شبیه Slack/Teams داخلی)
- `conversation_type`: مکالمه دونفره (`direct`) یا گروهی (`group`، با `group_name`)
- `chat_participants` رابطه چندبه‌چند بین مکالمه و اعضا

**فاز ۳۰ — ویرایش/حذف پیام:** `chat_messages.edited_at`/`deleted_at` اضافه شد تا خودِ فرستنده بتونه
پیام خودشو ویرایش/حذف کنه (چک مالکیت در سطح اپلیکیشن، بدون پرمیژن RBAC جدید). حذف **Soft Delete**
است — رکورد پاک نمی‌شه، فقط `deleted_at` پر می‌شه؛ فرانت به‌جای `message_text` واقعی یک تومبستون
«این پیام حذف شد» نشون می‌ده تا جریان گفت‌وگو (شمارش/ترتیب پیام‌ها) به‌هم نریزه — دقیقاً رفتار
تلگرام. ویرایش فقط `message_text`/`edited_at` رو عوض می‌کنه؛ بدون تاریخچهٔ نسخه (برخلاف
`financial_proposals`) چون این یک ابزار پیام‌رسانی غیررسمیه، نه سند تجاری قابل حسابرسی.

### broadcast_messages / broadcast_message_dismissals — پیام اعلامی مدیر در بدو ورود (فاز ۴۱-ج) ✅ 🆕

⚠️ مدیر سیستم می‌تونه یک پیام (عکس تا نصف ارتفاع صفحه + متن زیرش) برای یک کاربر خاص، یک گروه دسترسی خاص، یا همه کاربران بسازه که همون لحظه‌ی ورود کاربر هدف به سامانه نمایش داده بشه — **یک‌بار**، و بعد از بستن توسط کاربر دیگه هرگز تکرار نشه.

- **بدون فن‌اوت رکورد در لحظه ایجاد**: برخلاف `notifications` (که دقیقاً یک ردیف به‌ازای یک کاربره)، ساختن N ردیف در لحظه‌ی ایجاد پیام برای هدف «گروه» یا «همه» پرهزینه و غیرمقیاس‌پذیره. به‌جاش `broadcast_messages` خود پیام رو یک‌بار نگه می‌داره (با `target_type`: `user`/`group`/`all` + `target_user_id`/`target_group_id` اختیاری بسته به نوع)، و کوئری «پیام‌های در انتظار من» در **لحظه‌ی درخواست** (نه در لحظه‌ی ساخت) بررسی می‌شه:
  ```sql
  SELECT * FROM broadcast_messages b
  WHERE b.active = true
    AND (b.target_type = 'all'
         OR (b.target_type = 'group' AND b.target_group_id = :myGroupId)
         OR (b.target_type = 'user'  AND b.target_user_id  = :myUserId))
    AND NOT EXISTS (
      SELECT 1 FROM broadcast_message_dismissals d
      WHERE d.broadcast_message_id = b.id AND d.user_id = :myUserId
    )
  ORDER BY b.created_at ASC
  ```
- `broadcast_message_dismissals`: ثبت این‌که کدوم کاربر کدوم پیام رو بسته — کلید ترکیبی `(broadcast_message_id, user_id)`، upsert idempotent (بستن دوباره‌ی همون پیام خطا نمی‌ده، فقط بی‌اثره). همین جدوله که تضمین می‌کنه پیام دقیقاً یک‌بار نمایش داده بشه
- `active`: مدیر می‌تونه پیام رو قبل از دیده‌شدن توسط همه غیرفعال کنه؛ پیام غیرفعال دیگه در کوئری «در انتظار من» ظاهر نمی‌شه (برای کسانی که هنوز ندیده‌ان)، ولی رکورد و تاریخچه‌ی کسانی که قبلاً دیده‌ان دست‌نخورده می‌مونه
- ⚠️ **«کارمند خاص» یعنی از جدول `users`** (کاربران دارای حساب سیستم)، نه جدول HR `employees` — تصمیم صریح کاربر، چون این پیام باید به کسی که واقعاً وارد سامانه می‌شه نمایش داده بشه
- بک‌اند: `GET/POST /broadcast-messages` و `POST /broadcast-messages/:id/deactivate` پشت مجوز جدید `broadcast_messages.manage` (پیش‌فرض فقط مدیریت)؛ `GET /broadcast-messages/pending` و `POST /broadcast-messages/:id/dismiss` برای همه کاربران لاگین‌شده آزاده (بدون مجوز خاص، هم‌الگوی `notifications`)
- فرانت: `BroadcastPopupHost` در سطح `AppShell` (کنار `ChatWidget` موجود) لیست «در انتظار من» رو می‌گیره و اولین مورد رو با `BroadcastPopup` (الگوی بصری `ConfirmModal`، عریض‌تر، تصویر `height:50vh` + `object-cover`) نمایش می‌ده؛ بعد از بستن به مورد بعدی صف می‌ره

### داشبورد و کنترل دسترسی KPI ها

- ویجت‌های داشبورد (نرخ استعلام→پیشنهاد، نرخ تبدیل پیشنهاد→سفارش، ارزش سفارش‌های در جریان، تعداد/ارزش استعلام‌های منتظر بازگشایی، سفارش‌های تحویل‌شده منتظر تأیید فنی، فاکتورهای معوق) باید **قابل کنترل با گروه دسترسی** باشن — یعنی چند دسترسی جدید به ماژول `dashboard` در `permissions` اضافه می‌شه (مثلاً `dashboard.view_own_kpis` در مقابل `dashboard.view_team_kpis`) تا مشخص بشه کارشناس فقط KPI خودشو می‌بینه ولی مدیر کل تیم رو
- نرخ لحظه‌ای طلا/ارز (سامانه معاملات طلا و ارز، نرخ تبدیل جفت‌ارزها، قیمت جهانی/داخلی طلا) از **API خارجی زنده** میاد، نه از دیتابیس داخلی — نیازی به جدول جدید نیست؛ اگه نیاز به تاریخچه/کش شد، همون `exchange_rates` موجود قابل استفاده‌ست

---

## دامنه ۱۰: خزانه‌داری و برنامه‌ریزی مالی (Treasury & Financial Planning) ✅ 🆕

⚠️ **مرز مهم با حسابداری**: این دامنه عمداً **حسابداری/دفتر کل نیست** — همون‌طور که در یادداشت `our_entities` (دامنه ۱) اومده بود، یک ماژول Ledger جدا در آینده نیاز می‌شه که به `our_entities` وصل بشه. این دامنه فقط برنامه‌ریزی، پیش‌بینی نقدینگی و کنترل جریان پرداخت/دریافته — نه ثبت اسناد حسابداری.

### budget_categories / budgets / budget_lines — بودجه

- `budget_categories` درختیه (`parent_category_id`) تا هم سرفصل کلی (مثلاً «هزینه‌های اداری») هم زیرسرفصل داشته باشه؛ `category_type` مشخص می‌کنه درآمدیه یا هزینه‌ای
- `budgets` به تفکیک `our_entity_id` + `fiscal_year` — چون هر شرکت گروه بودجه مستقل خودشو داره. `UNIQUE(our_entity_id, fiscal_year)`
- ⚠️ چون شرکت ایرانی تقویم شمسی و شرکت‌های خارجی میلادی دارن (`our_entities.calendar_type`)، `fiscal_year` یک عدد ساده ذخیره می‌شه (۱۴۰۵ یا ۲۰۲۶) و تفسیرش در سطح اپلیکیشن انجام می‌شه، نه در دیتابیس
- `budget_lines` تفکیک ماهانه اختیاریه (`month` قابل NULL برای سقف سالانه بدون شکست ماهانه)

### پیش‌بینی مطالبات و پرداختنی‌ها — بدون جدول جدید

⚠️ **تصمیم معماری مهم**: «پیش‌بینی مطالبات» (Receivable Forecast) و «پیش‌بینی پرداختنی‌ها» (Payable Forecast) جدول مستقل نمی‌خوان — این دو مستقیماً از `customer_payments` و `supplier_payments` موجود (دامنه ۵، فیلترشده روی `status='unpaid'` + `due_date`) به‌صورت گزارش/Query ساخته می‌شن، چون داده از قبل در چرخه سفارش ثبت شده و تکرارش در جدول جدید باعث ناهماهنگی می‌شه.

### cash_flow_manual_entries — پیش‌بینی جریان نقدی

- Cash Flow Forecast = تجمیع پیش‌بینی مطالبات + پیش‌بینی پرداختنی‌ها (بالا) + این جدول
- این جدول فقط برای مواردی‌ست که هنوز به یک سفارش/PO رسمی تبدیل نشدن (مثلاً معامله‌ای در حال مذاکره، یا هزینه ثابت ماهانه پیش‌بینی‌شده مثل اجاره)
- `confidence_level` (قطعی/محتمل/ممکن) برای وزن‌دهی در نمودار پیش‌بینی — یک معامله «ممکن» نباید همون وزن یک ردیف «قطعی» رو در نمودار داشته باشه
- `related_entity_type`/`related_entity_id` اختیاریه، برای لینک به یک استعلام در حال مذاکره (الگوی عمومی مشابه `notifications`)

### expense_requests / payment_requests — درخواست هزینه و درخواست پرداخت

⚠️ **تفاوت این دو مرحله جداست**:
- `expense_requests`: تأییدیه **پیش از** خرج‌کردن (مثلاً «اجازه بدید ۵۰۰ یورو برای بلیط مأموریت خرج کنم»)
- `payment_requests`: اجرای واقعی **یک پرداخت مشخص** — که می‌تونه از چند منبع مختلف سرچشمه بگیره: یک `expense_request` تأییدشده، یک ردیف سررسیدشده در `supplier_payments`، بازپرداخت به مشتری، حقوق، یا قسط وام پرسنل. به همین دلیل `source_type`/`source_entity_id` عمومیه (نه FK مستقیم به یک جدول خاص)، دقیقاً مثل الگوی `related_entity_type`/`related_entity_id` در `notifications` (دامنه ۹)
- `payment_requests.payee_partner_id` (برای طرف بیرونی) یا `payee_employee_id` (برای پرسنل) — دقیقاً یکی از این دو باید پر بشه، قانون کسب‌وکاری در سطح اپلیکیشن (مشابه الگوی فرستنده/گیرنده نامه در دامنه ۸)
- ⚠️ **وابستگی رفت‌وبرگشتی با دامنه ۱۱**: `payee_employee_id` چون `employees` هنوز در این نقطه از اسکریپت تعریف نشده (HR بعد از Treasury میاد)، ستون بدون FK ساخته می‌شه و بعد از تعریف `employees` در دامنه ۱۱ با `ALTER TABLE ... ADD CONSTRAINT` تکمیل می‌شه — دقیقاً همون الگوی `inquiry_items.selected_offer_id` در دامنه ۲

### financial_calendar_events — تقویم مالی

- ⚠️ عمداً از `reminders` (دامنه ۹) جداست — `reminders` برای TODO شخصیه، ولی این جدول مخصوص رویدادهای مالی سررسیددار با دسته‌بندی مشخص (سررسید اقساط وام، انقضای ضمانت‌نامه/LC، مهلت مالیاتی، تمدید بیمه) و پشتیبانی از تکرار (`recurrence`)
- `related_entity_type`/`related_entity_id` می‌تونه به `issued_guarantees` یا `employee_loans` وصل بشه

### financial_kpis / financial_kpi_snapshots — پایه Financial Dashboard و Executive Dashboard

⚠️ خود داشبوردها (مالی/اجرایی) جدول جدا نمی‌خوان — دقیقاً طبق الگویی که در دامنه ۹ برای KPI های عمومی تعیین شد: چند دسترسی جدید به ماژول `treasury` در `permissions` اضافه می‌شه (مثلاً `treasury.view_financial_dashboard` در مقابل `treasury.view_executive_dashboard`) تا مشخص بشه کارشناس مالی فقط داشبورد بخش خودشو می‌بینه ولی مدیرعامل همه شرکت‌های گروه رو
- `financial_kpis`: کاتالوگ تعریف KPI ها (نقطه سربه‌سری نقدینگی، DSO/DPO، انحراف بودجه از واقعی و...)
- `calculation_type`: KPI های سبک (نسبت نقدینگی لحظه‌ای) به‌صورت Live Query محاسبه می‌شن؛ KPI های سنگین (روند چندماهه، مقایسه دوره‌ای) در `financial_kpi_snapshots` کش می‌شن تا داشبورد کند نشه

### ai_financial_assistant_conversations / ai_financial_assistant_messages

- ⚠️ جدا از `chat_conversations` (دامنه ۹) که برای پیام‌رسانی بین کاربرانه — این‌ها گفتگوی زبان‌طبیعی کاربر با دستیار AI روی داده‌های مالیه
- `query_context` (JSONB) روی هر پیام پاسخ AI: چه جدول‌ها/فیلترهایی برای تولید پاسخ استفاده شدن — هم برای Audit (چون داده مالی حساسه) هم برای بهبود دقت در آینده

---

## دامنه ۱۱: منابع انسانی (Human Resources) ✅ 🆕

### departments / employees — ساختار سازمانی و پرونده پرسنل

⚠️ **تصمیم معماری مهم: `employees` کاملاً جدا از `users` است.** `users` فقط برای کسانی‌ه که به سیستم لاگین می‌کنن (کارشناسان، مدیران)؛ ولی خیلی از پرسنل شرکت (نیروی انبار، راننده، نگهبان) لزوماً حساب کاربری سیستم ندارن، در حالی که باید پرونده HR داشته باشن. به همین خاطر:
- `employees` اطلاعات هویتی خودشو مستقل نگه می‌داره (نه وابسته به `users`)
- `employees.user_id` **اختیاریه** — فقط اگه اون فرد به سیستم هم دسترسی داشته باشه پر می‌شه؛ در این حالت دو رکورد (`users` و `employees`) به یک نفر اشاره می‌کنن ولی هرکدوم مسئولیت متفاوتی دارن (احراز هویت/دسترسی در برابر پرونده پرسنلی)
- `departments` درختیه (`parent_department_id`) و به `our_entity_id` وصله — چون هر شرکت گروه ساختار سازمانی مستقل خودشو داره
- `employees.direct_manager_id` خودارجاعه (self-reference) — همراه با `departments.parent_department_id`، **چارت سازمانی از همین دو ستون به‌صورت درختی محاسبه می‌شه، بدون نیاز به جدول جداگانه**
- `departments.head_employee_id` بعد از تعریف `employees` با `ALTER TABLE` اضافه می‌شه (وابستگی رفت‌وبرگشتی بین این دو جدول)
- `military_service_status` (وضعیت خدمت نظام‌وظیفه) فقط برای پرسنل مرد ایرانی معنا داره — در سطح UI شرطی نمایش داده می‌شه، در دیتابیس nullable

### employee_contracts — قرارداد

- `our_entity_id` الزامیه — قرارداد استخدام هر نفر زیر کدوم شرکت گروه ثبت می‌شه (ایران/ایتالیا/ترکیه/لهستان)، دقیقاً همون منطق `our_entity_id` که در RFQ/PO دیدیم
- `contract_type`: دائم/موقت/پروژه‌ای/آزمایشی؛ `end_date` فقط برای غیردائم پر می‌شه

### attendance_records — حضور و غیاب

- `UNIQUE(employee_id, work_date)` از ثبت تکراری روزانه جلوگیری می‌کنه
- `source` مشخص می‌کنه از دستگاه تشخیص چهره/اثرانگشت اومده یا دستی ثبت شده — برای سازگاری با سخت‌افزار احتمالی آینده

### leave_types / leave_balances / leave_requests — مرخصی

- `leave_types` انواع (استحقاقی/استعلاجی/بدون‌حقوق/ازدواج/فوت بستگان/زایمان) با سقف پیش‌فرض سالانه
- `leave_balances` مانده هرنوع مرخصی به تفکیک سال؛ `UNIQUE(employee_id, leave_type_id, year)`
- `leave_requests.attachment_file_url` برای مدارک لازم (مثلاً گواهی پزشکی استعلاجی)

### mission_requests — مأموریت

- ⚠️ بعد از تأیید مأموریت، اگه نیاز به تنخواه/بازپرداخت هزینه سفر باشه، یک رکورد در `expense_requests` (دامنه ۱۰) ساخته و از طریق `related_expense_request_id` لینک می‌شه — به‌جای تکرار منطق درخواست هزینه در دامنه HR، از همون جدول Treasury استفاده می‌کنیم

### overtime_records — اضافه‌کاری

- `rate_multiplier` ضریب طبق قانون کار (پیش‌فرض ۱.۴ برای روزهای عادی، قابل تغییر برای تعطیلات/شب‌کاری)

### employee_loans / employee_loan_installments — وام

- هر وام به چند قسط شکسته می‌شه (`employee_loan_installments`) تا در فیش حقوقی هرماه مشخص باشه دقیقاً کدوم قسط کسر شده
- `deducted_in_payslip_id` بعد از تعریف `payslips` با `ALTER TABLE` تکمیل می‌شه (همون الگوی وابستگی رفت‌وبرگشتی)

### benefit_types / employee_benefits / deduction_types / employee_deductions — مزایا و کسورات

- الگوی مشابه هم: یک جدول تعریف نوع (Catalog) + یک جدول تخصیص به هر پرسنل با بازه زمانی اعتبار (`effective_from`/`effective_to`) و پرچم تکرارشوندگی (`is_recurring`)
- `employee_deductions.related_loan_id` اختیاریه — وقتی کسر بابت قسط وامه، به رکورد `employee_loans` وصل می‌شه

### hr_requests — درخواست‌های عمومی

⚠️ مرخصی/مأموریت/وام/اضافه‌کاری هرکدوم به‌خاطر فیلدهای ساختاریافته خاص خودشون جدول تخصصی مجزا دارن (بالا اومد). `hr_requests` فقط برای درخواست‌های عمومی‌تری‌ه که جدول اختصاصی ندارن (گواهی اشتغال، پیش‌پرداخت حقوق، درخواست تجهیزات). در UI، صفحه «درخواست‌های من» این جدول رو با ۴ جدول تخصصی بالا **در سطح اپلیکیشن ترکیب (UNION)** می‌کنه تا یک اینباکس واحد نشون بده — نیازی به یک جدول چتری واحد در دیتابیس نیست.

### payroll_periods / payslips / payslip_items — فیش حقوقی

- `payroll_periods` دوره حقوقی به تفکیک `our_entity_id` + ماه/سال؛ `UNIQUE(our_entity_id, period_month, period_year)`
- `payslips` جمع کل هر پرسنل در هر دوره؛ `payslip_items` ردیف‌های تفکیکی (حقوق پایه، حق مسکن، اضافه‌کاری، بیمه، قسط وام) — همون الگوی شفافیت کامل که در `invoice_items` (دامنه ۷) دیدیم، نه فقط یک عدد نهایی

### performance_review_cycles / performance_reviews / performance_review_items — ارزیابی عملکرد

- `performance_review_items` امکان امتیازدهی چندمعیاره می‌ده (کیفیت کار، انضباط، کار تیمی...) هرکدوم با وزن مستقل (`weight_percent`) — به‌جای یک نمره خام واحد
- `reviewer_id` معمولاً سرپرست مستقیمه، ولی به یک `employee` وصله نه لزوماً `direct_manager_id` همون لحظه (چون ممکنه سرپرست عوض شده باشه ولی ارزیابی مال دوره قبل باشه)

### training_courses / training_sessions / training_enrollments — آموزش

- تفکیک سه‌سطحی: تعریف دوره (Catalog) → برگزاری واقعی (چند بار می‌شه یک دوره رو برگزار کرد) → ثبت‌نام هر پرسنل در یک برگزاری مشخص
- `training_enrollments.certificate_file_url` برای گواهی پایان دوره

---

## دامنه ۱۲: موتور حقوق و دستمزد قانون‌محور (Payroll Engine) ✅ 🆕 (فاز ۴۱)

⚠️ **درخواست صریح کاربر**: «به هیچ عنوان درصدها، اعداد، سقف‌ها، فرمول‌ها و قوانین داخل کد هاردکد نشوند» + «تمام محاسبات داخل Engine ها انجام شوند، نه Controller/Service». معماری این دامنه یک **Rule-Based Payroll Engine** واقعیه: هر عدد کسب‌وکاری (نرخ بیمه، سقف، معافیت مالیاتی، فرمول هر جزء حقوق) از دیتابیس خونده می‌شه، نسخه‌بندی‌شده، و تغییرش هیچ‌وقت نیاز به تغییر کد نداره.

⚠️ **رابطه با دامنه ۱۱ (`payroll_periods`/`payslips`/`payslip_items` قدیمی)** — **جایگزینی نرم**، نه حذف و نه اجرای موازی (تصمیم تأییدشده §۰۰-الف، گزینه ۲ از ۳ مطرح‌شده): جداول قدیمی در دیتابیس دست‌نخورده باقی موندن (برای Rollback-پذیری و داده‌ی تاریخی)، فقط در Prisma به `LegacyPayrollPeriod`/`LegacyPayslip`/`LegacyPayslipItem` تغییر نام دادن (همون `@@map` قدیمی، بدون اثر روی DB) — دقیقاً همون الگویی که پروژه قبلاً برای `shipment_documents` (جایگزین ستون‌های تک‌فایلهٔ `*_file_url`) و `distribute_vat`/`distribute_other_costs` (جایگزین `distribute_costs`) استفاده کرده بود.

### PayrollYear / PayrollRuleVersion — واحد نسخه‌بندی

- `payroll_years`: هر سال حقوقی (شمسی برای ایران، میلادی برای شرکت‌های خارجی — همون `calendar_type` که در `our_entities` دیدیم)
- `payroll_rule_versions`: **قلب معماری بدون‌هاردکد** — هر سال می‌تونه چند نسخه داشته باشه (`draft`→`active`→`superseded`)؛ هر `payroll_periods_v2` برای همیشه به یک `rule_version_id` ثابت می‌مونه، پس اگه سال بعد قانون عوض بشه، فیش حقوقی ماه‌های قبل با همون قانون قدیمی، دقیقاً و بدون تغییر کد، قابل بازمحاسبه‌ست

### PayrollRule / PayrollTaxBracket / Formula — جایی که همه‌ی اعداد زندگی می‌کنن

- `payroll_rules`: جفت‌های `code`→`value` (مثلاً `INSURANCE_RATE_EMPLOYEE`, `TAX_EXEMPTION`, `HOUSE_RATE`) — Rule Engine فقط این جدول رو می‌خونه و کش می‌کنه؛ تغییر سالانه یعنی فقط یک `UPSERT` روی این جدول
- `payroll_tax_brackets`: پله‌های مالیات پلکانی (چون ذاتاً چندردیفی‌ان، در `payroll_rules` تک‌مقداره جا نمی‌شدن) — Tax Engine مستقل از Insurance Engine، معافیت رو کم می‌کنه و پله‌به‌پله مصرف می‌کنه
- `payroll_formulas`: عبارت‌های متنی یک DSL سبک (Variables, Conditions, Math/Comparison/Boolean Operators, `IF`/`MIN`/`MAX`/`ROUND`/`FLOOR`/`CEIL`/`ABS`/`PERCENT`) — **بدون `eval`/`new Function`**؛ Formula Engine یک Lexer→Parser→AST→Evaluator دستی پیاده کرده (تست امنیتی مخصوص همین موضوع در `formula-engine.spec.ts` وجود داره)

### PayrollComponent — کاتالوگ اجزای حقوق

- `payroll_components`: کاتالوگ سراسری (BASE, HOUSE, CHILD, SENIORITY, OVERTIME, BONUS, LOAN, ADVANCE, ...) با `component_type` (`earning`/`deduction`)، `is_insurable`/`is_taxable`، و `formula_id` که به یک فرمول **از یک نسخه‌ی مشخص** وصله
- ⚠️ **نکته معماری مهم**: کد این جدول ثابته و در طول سال‌ها عوض نمی‌شه — وقتی نسخه‌ی قانون جدید میاد، ادمین `formula_id` همون کامپوننت رو به فرمول نسخه‌ی جدید بازپیوند می‌ده (نه اینکه کامپوننت جدید بسازه)؛ کدهای `INSURANCE`/`TAX` عمداً فرمول ندارن چون مقدارشون رو Insurance Engine/Tax Engine مستقل تعیین می‌کنن، نه فرمول خام
- **Dependency Engine**: ترتیب واقعی محاسبه (نه `calc_order` نمایشی) از یک Graph بین کدهای کامپوننت‌ها ساخته می‌شه (Kahn's Topological Sort)، با تشخیص Circular Dependency و مسیر کامل چرخه در پیام خطا

### EmployeePayrollProfile — عمداً باریک (Reuse over Duplication)

- `employee_payroll_profiles` فقط چیزهایی رو نگه می‌داره که جای دیگه نیستن: `children_count`, `insurance_number`, `seniority_base_date` (override دستی، پیش‌فرض = `hire_date`), `cost_center_dept_id`, `default_rule_version_id`
- حقوق پایه/ارز از `employee_contracts.base_salary`/`salary_currency`، تأهل از `employees.marital_status`، سمت از `employees.position_title` خونده می‌شه — بدون تکرار داده

### PayrollPeriod / PayrollWorkLog — دوره و کارکرد

- `payroll_periods_v2` (نام‌گذاری متفاوت از `payroll_periods` قدیمی دامنه ۱۱) هر دوره رو برای همیشه به یک `rule_version_id` قفل می‌کنه
- ⚠️ **تصمیم تأییدشده §۰۰-ب**: `payroll_work_logs` **جدول ورودی دستی موازی نیست** — به‌صورت خودکار از `attendance_records`/`leave_requests`/`overtime_records`/`mission_requests` **موجود** (دامنه ۱۱) تجمیع می‌شه؛ `source='auto_aggregated'` پیش‌فرضه و با هر اجرای مجدد بازنویسی می‌شه، مگر یک ردیف دستی (`source='manual'`) از قبل ثبت شده باشه که دیگه دست‌نخورده می‌مونه (برای موارد استثنا)

### PayrollResult / PayrollResultItem — نتیجه و گردش وضعیت

- `payroll_results` نتیجه‌ی محاسبه‌ی هر کارمند در هر دوره‌ست، با گردش وضعیت خطی **Draft → Calculated → Reviewed → Approved → Posted → Locked** (هر گذار actor + timestamp خودشو داره: `reviewed_at`/`reviewed_by`، ...)
- ⚠️ **الزام صریح کاربر «پس از Locked هیچ تغییری مجاز نیست» با دو خط دفاعی مستقل**:
  1. سطح اپلیکیشن: `PayrollWorkflowService`/`StoreResultStage` قبل از هر نوشتن، وضعیت `locked` رو چک می‌کنن
  2. سطح دیتابیس: تریگر `trg_prevent_locked_payroll_result_mutation` — حتی `UPDATE`/`DELETE` مستقیم از `psql` هم روی یک نتیجه‌ی `locked` رد می‌شه (زنده تست شد: هنگام پاک‌سازی داده‌ی تستی این فاز، همین تریگر جلوی حذف مستقیم یک رکورد Locked رو گرفت — دقیقاً رفتار مطلوب)
- `payroll_result_items` یک Snapshot کامله (`component_code` + `formula_snapshot` در لحظه‌ی محاسبه) — دقیقاً هم‌الگوی `exchange_rate_value` در `invoice_items` (دامنه ۷): حتی اگه بعداً عنوان Component یا متن فرمول عوض بشه، تاریخچه‌ی این نتیجه‌ی خاص دست‌نخورده می‌مونه. کدهای `INSURANCE`/`TAX` اینجا ذخیره نمی‌شن چون مقدارشون روی ستون‌های اختصاصی `payroll_results` (`insurance_employee_share`, `tax_amount`, ...) می‌شینه

### PayrollAuditLog

- ثبت هر تغییر (کی/کِی/چه چیزی، `old_value`/`new_value`) روی موجودیت‌های حقوق — مستقل از منطق محاسباتی؛ `entity_type`/`entity_id` عمومیه (هم‌الگوی `notifications`/`cash_flow_manual_entries`)

### معماری Engine ها (کد، نه دیتابیس — برای مرجع)

پایپ‌لاین ۱۳مرحله‌ای ثابت (Repository Pattern + Dependency Injection، بدون منطق محاسباتی در Controller/Service):
`Load Rules → Load Employee → Load WorkLog → Evaluate Components → Calculate Earnings → Calculate Insurance Base → Insurance → Calculate Taxable Income → Tax → Other Deductions → Net Salary → Employer Cost → Store Result`

- **Formula Engine**: Lexer/Parser/AST/Evaluator مستقل از NestJS، کش‌شونده به‌ازای `formulaId`/کامپوننت
- **Dependency Engine**: Graph + Topological Sort + Circular Detection، مستقل تست‌شده
- **Rule Engine**: فقط بارگذاری `RuleSet` از یک `rule_version_id`، بدون منطق محاسباتی
- **Insurance Engine / Tax Engine**: هر دو Pure Function روی یک `RuleSet`، کاملاً مستقل از هم
- **Component Evaluator**: لایه‌ی نازک روی Formula+Dependency Engine — ترتیب صحیح رو می‌گیره و فرمول‌ها رو با Context (WorkLog+Rules+Profile+نتایج قبلی) اجرا می‌کنه
- **Payroll Processor**: تنها نقطه‌ی اجرای پایپ‌لاین؛ `calculateForEmployee` (تکی) و `calculateForPeriod` (دسته‌جمعی، هر کارمند مستقل catch می‌شه)
- **دسترسی (RBAC)**: ماژول جدید `payroll_engine` در کاتالوگ دسترسی موجود (نه سیستم نقش جدا) — `payroll_engine.view/manage_config/manage_profile/manage_worklog/run_calculation/review/approve/post/lock`؛ گروه‌های پیش‌فرض جدید «منابع انسانی» (HR Manager)، «کارشناس حقوق و دستمزد» (Payroll Expert)، «بازدیدکننده» (Viewer)، و گروه موجود «مالی» با کلیدهای `approve`/`post`/`lock` بسط پیدا کرد (Finance Manager). گروه «مدیریت» (=`ALL_PERMISSION_KEYS`) معادل CEO از قبل همه‌چیز رو داره
- **تولید سند**: به‌جای زیرساخت موازی، از `PdfRendererService` (Puppeteer، موجود در `proposal-documents`) و `StorageService` مشترک استفاده می‌کنه؛ فقط قالب HTML/Excel فیش حقوقی/لیست حقوق تازه نوشته شد

Migration: `0028_payroll_engine_foundation` (۱۱ جدول جدید، کاملاً افزودنی) + `0029_payroll_result_lock_trigger` (تریگر immutability؛ Rollback با `DROP TRIGGER`/`DROP FUNCTION`، مستند در خود فایل migration).

### فرزندان با تاریخ تولد + سقف سنی حق اولاد (فاز ۴۲) ✅ 🆕

⚠️ بازخورد کاربر حین تولید یک فیش حقوقی واقعی: «فرزندان زیر ۱۸ سال حق اولاد دریافت می‌کنند» — تا این فاز، `employee_payroll_profiles.children_count` صرفاً یک عدد خام بدون تاریخ تولد بود و امکان اعمال خودکار سقف سنی (یا بازمحاسبه‌ی خودکار وقتی فرزندی ۱۸ ساله می‌شه) رو نداشت.

- جدول جدید `employee_children` (Migration `0030_employee_children`، دامنه ۱۱): `employee_id`, `full_name` (اختیاری), `birth_date`. `employee_payroll_profiles.children_count` طبق همون الگوی «جایگزینی نرم» (مثل `distribute_costs`→`distribute_vat`/`distribute_other_costs`) Deprecated شد — ستون در دیتابیس مونده (Rollback-پذیری) ولی دیگه نه خونده می‌شه نه از UI نوشته می‌شه
- سقف سنی خودش هم یک Rule جداست (`CHILD_ALLOWANCE_MAX_AGE` در `payroll_rules`، نه هاردکد ۱۸ در کد) — اگه برای یک نسخه‌ی قانون تنظیم نشه، پیش‌فرض بدون محدودیت سنیه (همه‌ی فرزندان شمرده می‌شن)، دقیقاً هم‌روح با فلسفه‌ی «zero hardcoding» کل دامنه ۱۲
- `LoadEmployeeStage` (مرحله ۲ پایپ‌لاین) حالا `CHILDREN_COUNT` رو از تعداد فرزندان زیر سقف سنی **در تاریخ شروع همون دوره** (نه تاریخ جاری) محاسبه می‌کنه — یعنی یک فیش قدیمی حتی اگه امروز فرزند ۱۸ ساله شده باشه، بازمحاسبه‌ی تاریخی درستی می‌ده. سن دقیق (نه فقط تفاضل سال) با یک تابع خالص جدا (`countEligibleChildren`) محاسبه و تست‌شده
- Frontend: بخش «فرزندان» به صفحه‌ی جزئیات پرسنل (منابع انسانی) اضافه شد (تاریخ تولد + نام اختیاری، حذف)؛ فیلد دستی «تعداد فرزندان» از تب «پروفایل حقوقی پرسنل» ماژول حقوق و دستمزد حذف شد چون دیگه محاسبه‌ش خودکاره

---

## دامنه ۱۳: گزارش‌ها و تحلیل‌ها (Reports) ✅ 🆕 (فاز ۴۳)

⚠️ **بدون هیچ جدول یا ستون جدید در دیتابیس** — این دامنه صرفاً یک لایه‌ی Query روی جداول موجود دامنه‌های ۲، ۴، ۵ و ۱۰ است؛ بنابراین در `erp-schema.sql` معادلی نداره. سه گزارش طبق درخواست کاربر («گزارش‌ها و تحلیل‌ها» در سایدبار) اضافه شد:

- **گزارش سفارشات و سود و زیان** (`reports.view_orders_pnl`): روی `orders`/`order_items` + `inquiries` (برای فیلتر مشتری/کارشناس فروش) کوئری می‌زنه؛ سود هر سفارش از `SUM(order_items.sale_price*quantity) - SUM(order_items.purchase_price*quantity)` محاسبه می‌شه
- **گزارش پرداختی‌ها و دریافتی‌ها** (`reports.view_payments`): `customer_payments` (دریافتنی) + `supplier_payments` (پرداختنی) رو نرمالایز و ترکیب می‌کنه؛ تشخیص معوق (`isOverdue`) در سطح اپلیکیشن از `due_date < امروز` + وضعیت پرداخت‌نشده محاسبه می‌شه
- **گزارش تبدیل استعلام به سفارش** (`reports.view_conversion`): روی `inquiries.status` تالی می‌گیره؛ نرخ تبدیل = `(won + partially_won) / (won + lost + partially_won) × ۱۰۰` — پرونده‌های `in_progress`/`cancelled`/`suspended` عمداً در مخرج نیستن (هنوز نتیجه نگرفتن یا اصلاً وارد رقابت نشدن)

⚠️ **تصمیم صریح کاربر — بدون تبدیل ارز**: چون هیچ زیرساخت تبدیل نرخ ارز خودکاری در پروژه وجود نداره (نگاه کنید به سوال باز #۱ پایین)، مبالغ گزارش ۱ و ۲ هرگز بین ارزهای مختلف جمع نمی‌شن — همیشه به تفکیک `currency_code` گروه‌بندی می‌شن (کلید `"نامشخص"` برای مواردی که ارز قابل تشخیص نیست، مثلاً سفارشی که هنوز پیشنهاد مالی جاری نداره). ارز هر سفارش چون مستقیماً روی `orders` ذخیره نشده، از طریق `financial_proposals` با `status='current'` همون `inquiry_id` استخراج می‌شه (همون الگوی موجود در `settlement.service.ts`).

- «گزارش نفرات» فقط به‌عنوان جایگاه در ناوبری اضافه شد (`placeholder`، بدون مجوز/بک‌اند) — طبق تصمیم صریح کاربر که فعلاً به این مرحله نیازی نیست.
- مجوزهای جدید ماژول `reports` در `permission-catalog.ts`: `reports.view_orders_pnl`/`reports.view_payments` به‌صورت پیش‌فرض به گروه «مالی»، `reports.view_conversion` به گروه‌های «فروش» و «مالی» داده شد (گروه «مدیریت» طبق قاعده‌ی همیشگی `ALL_PERMISSION_KEYS` هر سه رو داره).

---

## 🎉 جمع‌بندی کامل دیتابیس

هر ۱۲ دامنه (۸۰+ جدول) طراحی و در `erp-schema.sql` نوشته شدن (+ دامنه ۱۳ گزارش‌ها، بدون جدول جدید):

| دامنه | جداول کلیدی |
|---|---|
| ۱. پایه/پشتیبان | users, roles, business_partners, item_catalog, currencies |
| ۲. استعلام | inquiries, inquiry_items, inquiry_discussions |
| ۳. تأمین | supplier_rfqs, supplier_offers, supplier_offer_items |
| ۴. پیشنهاد مشتری | financial_proposals, technical_proposals, inquiry_outcomes |
| ۵. اجرای سفارش | orders, purchase_orders, customer/supplier_payments, issued_guarantees |
| ۶. تولید/حمل/گمرک | production_tracking, shipments, export/import_documents, warehouse_receipts |
| ۷. تحویل/تسویه | deliveries, invoices, invoice_collections |
| ۸. مکاتبات/بایگانی | letters, documents, letter_workflow_logs |
| ۹. اعلان/فعالیت/چت | notifications, activities (فاز ۱۶، جایگزین reminders)، chat_conversations، broadcast_messages (فاز ۴۱-ج) |
| ۱۰. خزانه‌داری و برنامه‌ریزی مالی | budgets, expense_requests, payment_requests, financial_calendar_events, financial_kpis |
| ۱۱. منابع انسانی | employees, employee_contracts, leave_requests, payroll_periods (Legacy)، payslips (Legacy) |
| ۱۲. موتور حقوق و دستمزد 🆕 | payroll_rule_versions, payroll_rules, payroll_formulas, payroll_components, payroll_results |

### سوالات باز باقی‌مانده (برای فاز پیاده‌سازی):
1. منبع دقیق API نرخ ارز خودکار (`exchange_rates.source`)
2. مقدار پیش‌فرض `response_due_date` برای RFQ (چند روز؟) و پیاده‌سازی Job خودکار تغییر وضعیت به `no_response`
3. آیا نیاز به Audit Log جامع (تاریخچه تغییرات) در سطح کل سیستم هست یا فقط برای جداول حساس (قیمت‌گذاری، پرداخت)؟ (نگاه کنید به `payroll_audit_log` در دامنه ۱۲ — فعلاً فقط برای موجودیت‌های حقوق پیاده شده، نه کل سیستم)
4. **(دامنه ۱۰)** آیا زنجیره تأیید `expense_requests`/`payment_requests` چندسطحیه (مثلاً بالای یک مبلغ مشخص نیاز به تأیید مدیرعامل)؟ فعلاً فقط یک `current_approver_id`/`approved_by` ساده طراحی شده — ولی اگه زنجیره چندمرحله‌ای لازم باشه، به یک جدول `approval_steps` جدا نیاز داریم
5. ~~**(دامنه ۱۱)** فرمول دقیق محاسبه اضافه‌کاری/کسورات بیمه/مالیات طبق قانون کار ایران~~ — **حل شد در دامنه ۱۲**: به‌جای فرمول ثابت در کد، نرخ‌ها/پله‌ها/فرمول‌ها همگی در `payroll_rules`/`payroll_tax_brackets`/`payroll_formulas` نسخه‌بندی‌شده ذخیره می‌شن؛ عدد دقیق قانون کار ایران در فاز پیاده‌سازی/seed اولیه توسط کارشناس منابع انسانی وارد می‌شه، نه در کد
6. **(دامنه ۱۱)** آیا حضور و غیاب قراره به یک دستگاه فیزیکی (ساعت‌زن) وصل بشه؟ اگه بله، فرمت داده ورودی از اون سیستم باید مشخص بشه — `payroll_work_logs` (دامنه ۱۲) از `attendance_records` موجود تجمیع می‌شه، پس این سوال هنوز برای خود `attendance_records` باز می‌مونه
7. **(دامنه ۱۲)** آیا `payroll_components`/فرمول‌ها یک پنل ادمین کامل (ویرایش تعاملی فرمول با پیش‌نمایش زنده) می‌خوان، یا فرم‌های فعلی (کد+عبارت متنی) برای کارشناس حقوق و دستمزد کافیه؟

---

## ضمیمه — همگام‌سازی جداول/ستون‌های عقب‌افتاده (بعد از فاز ۳۱)

⚠️ این بخش فقط به‌خاطر یک بازخورد کاربر بعد از فاز ۳۱ اضافه شد: مقایسهٔ خودکار `schema.prisma` (منبع واقعی، چون کد از قبل پیاده‌سازی و تست شده) با `erp-schema.sql`/این سند نشون داد چند جدول/ستون که در فازهای قبلی واقعاً ساخته شدن، هیچ‌وقت به این دو سند اضافه نشده بودن. همه‌شون الان در `erp-schema.sql` هم اضافه شدن؛ اینجا فقط خلاصهٔ چرایی‌شون:

- **`refresh_tokens`** (فاز ۱، Migration `0002_auth_fields`) — نشست‌های JWT چندگانه هر کاربر (چند دستگاه هم‌زمان)؛ `users` هم فیلدهای `password_hash`/`password_reset_token`/`password_reset_token_expires_at`/`requested_department` رو از همون فاز داشت که مستند نشده بود.
- **`inquiry_counters`/`rfq_counters`/`freight_rfq_counters`/`shipment_counters`** (فازهای ۳/۴/۱۱) — شمارنده‌های اتمیک سالانه برای شماره‌گذاری خودکار، دقیقاً هم‌الگوی `catalog_counters`/`letter_counters` که از قبل مستند بودن.
- **`business_partners.phones/fax/registration_date/logo_url/is_foreign`** و **`partner_contacts.extension/photo_url/notes`** (فاز ۵، Migration `0005_feedback_pack`) — بازخورد کاربر بعد از تست فاز ۴: چند شماره تلفن هم‌زمان (JSONB)، فکس، تاریخ ثبت شرکت، لوگو، پرچم داخلی/خارجی برای شرکت؛ داخلی تلفن، عکس و ملاحظات آزاد برای رابط.
- **`inquiries.deleted_at`/`deleted_by`** (فاز ۵) — حذف نرم پرونده به‌جای حذف قطعی؛ بازگردانی/حذف قطعی واقعی از طریق دسترسی `inquiry.purge`.
- **`activity_outcome_templates`** + **`activities.outcome_id`** (فاز ۱۷) — قالب‌های نتیجهٔ ساختاریافته و Auto Follow-up Engine؛ توضیح کامل در [SPEC-PHASE-17.md](./SPEC-PHASE-17.md).
- **`chat_participants.last_read_at`** (فاز ۲۹) — مبنای رسید خواندن (تیک/دوتیک مثل واتس‌اپ).

جداول دامنهٔ ۱۰ (خزانه‌داری) و `training_courses`/`training_sessions`/`training_enrollments` عمداً دست‌نخورده موندن — این‌ها در جهت مخالف Drift هستن: در `erp-schema.sql` طراحی شدن ولی هنوز هیچ‌کدوم واقعاً پیاده‌سازی نشدن (دامنهٔ آموزش هم طبق تصمیم صریح فاز ۲۱ از دامنهٔ ۱۱ حذف شد ولی SQL فایل پاک‌سازی نشده بود) — تصمیم دربارهٔ نگه‌داشتن یا حذف این طرح‌های آینده از سند، جدا از این ضمیمه است.
