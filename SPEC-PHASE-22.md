# SPEC — فاز ۲۲: زیرساخت‌های مشترک UI (تاریخ، مبلغ، پیش‌نمایش فایل)

> این فاز اولین زیرفاز از یک بسته اصلاحات فراگیر است که کاربر قبل از رفتن به فاز خزانه‌داری درخواست کرد (نامه کامل در تاریخچه گفتگو). بسته کامل به این ترتیب زیرفازبندی می‌شه:
> **فاز ۲۲ (این سند)**: زیرساخت مشترک (تاریخ/مبلغ/پیش‌نمایش فایل) — پایه فازهای بعدی
> فاز ۲۳: پروفایل (کراپ عکس، ولیدیشن موبایل، اصلاح الزام مدارک هویتی)
> فاز ۲۴: مکاتبات (بازطراحی جهت‌محور + فیلدهای جدید — نیاز به Migration)
> فاز ۲۵: کالاها (ایمپورت/اکسپورت اکسل + ضمائم سطح کالا)
> فاز ۲۶: طرف‌های تجاری (تاگل کشور) + استعلام حمل دوزبانه
> فاز ۲۷: بارها (چندفایلی/ریویژن + قفل مرحله + گردش تأیید ویرایش)
> فاز ۲۸: چت (ویجت شناور + پیوست + استیکر)

## هدف

سه کامپوننت مشترک که در سراسر برنامه استفاده می‌شن، به‌علاوه جایگزینی همه‌جای موجودشون:

1. **رفع باگ تقویم دوگانه** (`DualDateInput`) — امکان جهش مستقیم به هر سال/ماه (نه فقط قدم‌به‌قدم)
2. **کامپوننت مبلغ با جداکننده سه‌رقمی زنده** (`AmountInput`) — جایگزین همه فیلدهای مبلغی موجود
3. **کامپوننت پیش‌نمایش/دانلود فایل** (`FileViewer`) — آیکون چشم (پیش‌نمایش داخل برنامه) + آیکون دانلود، جایگزین همه لینک‌های «فقط دانلود» موجود

## تصمیمات طراحی (بر اساس پاسخ‌های تأییدشده)

### ۱. تقویم دوگانه

- عنوان متنی وسط هدر تقویم (`{ماه} {سال}`) با **دو `<select>` کشویی** جایگزین می‌شه: یکی سال، یکی ماه — هر دو بلافاصله `viewDate` رو به‌روزرسانی می‌کنن (بدون نیاز به قدم‌به‌قدم زدن دکمه‌های قبل/بعد)
- بازه سال کشویی: از `سال جاری - ۱۰۰` تا `سال جاری + ۱۰` (کافی برای تاریخ تولد پرسنل/فرم‌های گذشته‌نگر تا امروز/آینده نزدیک مثل تاریخ سررسید)
- دکمه‌های ماه قبل/بعد (Chevron) همچنان می‌مونن برای پیمایش سریع یک‌ماهه؛ کشویی‌ها برای جهش مستقیمن
- شمارش سال/ماه در حالت شمسی از `date-fns-jalali` (`getYearJ`/`newDateJ`) و در حالت میلادی از `Date` استاندارد — دقیقاً همون منطق موجود در `grid` فعلی، فقط منبع تغییر `viewDate` از کلیک روز/دکمه قبل‌وبعد به select هم اضافه می‌شه
- فایل: `apps/web/src/components/ui/DualDateInput.tsx` — تغییر داخلی، بدون تغییر Props (سازگار با همه ۳۰+ محل استفاده موجود بدون نیاز به تغییر جای دیگه)

### ۲. کامپوننت مبلغ (`AmountInput`)

- کامپوننت کنترل‌شده جدید: `value: number | null`, `onChange: (n: number | null) => void`, بقیه Props مثل `TextInput` (placeholder, disabled, ...)
- نمایش: ارقام لاتین + کاما هزارگان (`1,234,567`) — هنگام تایپ به‌صورت زنده فرمت می‌شه (نه فقط روی blur)، با حفظ موقعیت Cursor (محاسبه بر اساس تعداد رقم قبل از Cursor، نه بر اساس Index خام رشته، چون افزودن کاما جابه‌جایی ایجاد می‌کنه)
- اعشار: تا زمانی که کاربر نقطه اعشار بزنه، بخش صحیح فرمت می‌شه و بخش اعشار دست‌نخورده می‌مونه (بدون گرد کردن حین تایپ)
- `dir="ltr"`, `inputMode="decimal"` — هم‌الگوی فیلدهای مبلغی فعلی
- فایل جدید: `apps/web/src/components/ui/AmountInput.tsx`
- جایگزینی در همه ۱۷ فیلد شناسایی‌شده (RfqTab، SelectionTab، OrderTab، OutcomeTab، SettlementTab، FreightTab، ShipmentDetail ×۳، FlexPaymentList، MyRequestsSection ×۲، EmployeesTab، EmployeeHrRecordsSection ×۲) — فیلدهای تعداد/روز/ساعت/نمره (quantity، annualEntitlementDays، hours، overallScore و...) دست‌نخورده می‌مونن چون مبلغ نیستن
- توابع نمایشی تکراری `fmt()`/`toLocaleString` پراکنده در چند فایل (`FreightTab`, `OrderTab`, `ProposalTab`, `POTab`, `RfqTab`, `SelectionTab`, `SettlementTab`) به یک تابع مشترک `formatAmount()` در همون فایل کامپوننت منتقل و از اونجا Import می‌شن (حذف تکرار، نه تغییر رفتار نمایش)

### ۳. کامپوننت پیش‌نمایش فایل (`FileViewer`)

- **مرز روشن با `AuthImage` موجود**: `AuthImage` برای تصاویر همیشه‌نمایان (لوگو شرکت، عکس رابط، آواتار پروفایل/پرسنل) دست‌نخورده می‌مونه — این‌ها نیازی به کلیک ندارن. `FileViewer` برای **اسناد/فایل‌های ضمیمه‌ای** است که فعلاً فقط لینک «دانلود» دارن (طبق Survey: پیوست نامه، اسناد مرحله بار، مدارک هویتی، ضمائم قلم استعلام، فایل قرارداد سفارش، عکس‌های دریافت انبار، فایل مالی/فنی پیشنهاد، رسید تحویل، سند تسویه، سند RFQ تأمین‌کننده، فایل قرارداد پرسنل)
- دو آیکون کنار هم: چشم (`Eye` از lucide-react) = باز کردن Modal پیش‌نمایش داخل برنامه؛ دانلود (`Download`) = فراخوانی `downloadFile()` موجود (بدون تغییر)
- Modal پیش‌نمایش: فایل با همون مکانیزم `apiClient` + Blob (توکن‌دار) واکشی می‌شه (نه لینک مستقیم — چون `/files/...` نیاز به `Authorization: Bearer` داره و بدون اون ۴۰۱ می‌ده)
- ⚠️ نکته فنی مهم: بک‌اند فعلاً `Content-Type` واقعی فایل رو در پاسخ دانلود ست نمی‌کنه (فقط `Content-Disposition: attachment`)، پس مرورگر نوع Blob رو نمی‌تونه درست تشخیص بده. به‌جای تغییر رفتار Endpoint دانلود (که ریسک بی‌مورد به مسیرهای فعلی دانلود می‌زنه)، `FileViewer` نوع فایل رو از **پسوند نام فایل** (که همیشه در `fileUrl`/`fileName` موجوده) تشخیص می‌ده و هنگام ساخت Blob، `type` درست رو دستی ست می‌کنه: `new Blob([data], { type: mimeFromExtension(name) })`
- شاخه‌بندی نمایش بر اساس پسوند:
  - `.png/.jpg/.jpeg/.webp` → `<img>` داخل Modal
  - `.pdf` → `<iframe>` با Blob URL (نمایشگر PDF داخلی مرورگر)
  - `.dwg/.xlsx/.docx/.zip` → پیش‌نمایش ممکن نیست؛ پیام «این نوع فایل قابل پیش‌نمایش داخل برنامه نیست» + دکمه دانلود در همون Modal
- فایل جدید: `apps/web/src/components/ui/FileViewer.tsx` (کامپوننت `<FileViewer fileUrl fileName />` که خودش دو آیکون + Modal رو مدیریت می‌کنه — یک Import، بدون نیاز به مدیریت state پیش‌نمایش در هر صفحه)
- جایگزینی همه لینک‌های «فقط دانلود» شناسایی‌شده در Survey با `<FileViewer />`:
  `CorrespondenceDetailPage`, `ShipmentDetail` (اسناد مرحله), `ProfilePage` (مدارک هویتی), `InquiryItemsPanel`, `InquiryFormPage`, `OrderTab` (فایل قرارداد), `ProposalTab` (فایل مالی/فنی), `SettlementTab` (رسید تحویل/سایر سند), `RfqTab` (سند تأمین‌کننده)
- رفع دو گپ اضافی که Survey پیدا کرد (چون همون کامپوننت رفعشون می‌کنه، بیرون از دامنه این فاز گذاشتن منطقی نیست):
  - `EmployeesTab.tsx:398` — فایل قرارداد پرسنل الان از `AuthImage` استفاده می‌کنه که فقط `<img>` رندر می‌کنه و برای PDF/Word خراب/خالی می‌مونه → به `FileViewer` تغییر می‌کنه
  - `ShippingTab.tsx:346-349` — عکس‌های دریافت انبار اصلاً نمایش/لینک ندارن، فقط یک نشان ثابت «تصویر» → به `FileViewer` وصل می‌شه

## خارج از محدوده این فاز

- تغییر خود Endpoint دانلود در بک‌اند (`files.controller.ts`) — فقط تشخیص نوع در فرانت
- کراپ عکس پروفایل، ولیدیشن موبایل، مدارک هویتی — فاز ۲۳
- هر تغییر خاص ماژول (مکاتبات/کالاها/بارها/چت) — فازهای بعدی

## معیار پذیرش

- انتخاب سال/ماه در `DualDateInput` بلافاصله شبکه روزهای همون ماه/سال رو نشون می‌ده؛ کار می‌کنه هم در حالت شمسی هم میلادی
- تایپ در `AmountInput` جداکننده سه‌رقمی زنده نشون می‌ده و Cursor جابه‌جای غیرمنتظره نداره؛ `onChange` عدد خام (بدون کاما) برمی‌گردونه
- هر ۱۷ فیلد مبلغی شناسایی‌شده از `TextInput` خام به `AmountInput` سوییچ شدن
- کلیک آیکون چشم روی هر فایل تصویری/PDF ضمیمه‌شده، پیش‌نمایش داخل Modal نشون می‌ده بدون دانلود اجباری؛ کلیک دانلود همون رفتار قبلی رو داره
- فایل قرارداد پرسنل (PDF/Word) و عکس‌های دریافت انبار حالا قابل مشاهده/دانلودن (نه خراب/مخفی)
- تست‌های Jest برای منطق فرمت `AmountInput` (پارس/فرمت رفت‌وبرگشتی) و تشخیص نوع فایل در `FileViewer` (`mimeFromExtension`)
- Build + Lint هر دو اپ بدون خطا؛ تأیید بصری در مرورگر (تقویم، فرم مبلغی، Modal پیش‌نمایش تصویر و PDF)
- به‌روزرسانی `README.md`
