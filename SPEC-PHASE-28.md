# SPEC — فاز ۲۸: چت — ویجت شناور + پیوست فایل + استیکر

## هدف

سه بهبود روی ماژول پیام‌رسانی سراسری (`apps/api/src/chat/*`, `apps/web/src/pages/chat/*`):

1. **ویجت شناور** در همهٔ صفحات (نه فقط صفحهٔ کامل `/chat`)
2. **پیوست فایل** در پیام‌ها
3. **استیکر** — مجموعهٔ ثابت داخلی

## وضعیت فعلی (خلاصه اکتشاف)

- بک‌اند/فرانت فعلی کامل و کارآمده: مکالمهٔ دونفره/گروهی، پیام متنی، `unreadCount`، Polling (۱۵ ثانیه لیست، ۵ ثانیه پیام‌ها).
- ⚠️ **کشف مهم**: ستون `chat_messages.file_url` از طراحی اولیهٔ همین دامنه (erp-schema.sql) از قبل وجود داره و در Prisma هم مدل شده، ولی **هیچ‌جای کد** (نه DTO، نه سرویس، نه فرانت) بهش دست نمی‌زنه — کاملاً بلااستفاده مونده. یعنی پیوست فایل این فاز **به هیچ Migration نیاز نداره**، فقط وصل‌کردن زیرساخت موجوده.
- جدول `chat_messages` ستون جدای `file_name` نداره؛ `message_text` هم `NOT NULL` است.
- ویجت شناور سراسری وجود نداره؛ چت فقط از طریق منوی کناری و مسیر کامل `/chat` قابل‌دسترسیه.

## تصمیمات طراحی

### ۱. ویجت شناور — در همهٔ صفحات + حفظ صفحهٔ کامل (تصمیم کاربر)

- کامپوننت جدید `ChatWidget.tsx` در `AppShell.tsx` مونت می‌شه (هم‌تراز `NotificationBell` در Topbar، ولی خودش یک FAB گوشهٔ پایین صفحه‌ست، نه آیتم Topbar) — یعنی در تمام صفحات برنامه دیده می‌شه.
- روی خود مسیر `/chat` **مخفی می‌مونه** (چون صفحهٔ کامل همون تجربه رو با فضای بیشتر می‌ده؛ نمایش هم‌زمان هر دو گیج‌کننده‌ست).
- دکمهٔ شناور (آیکون پیام + نشان تعداد کل `unreadCount` جمع‌شدهٔ همهٔ مکالمات) با کلیک یک پنل کوچک باز می‌کنه: لیست مکالمات (فشرده) + وقتی مکالمه‌ای انتخاب بشه، همون پنل به نمای گفتگو (پیام‌ها + کادر ارسال) سوییچ می‌کنه؛ دکمهٔ «باز کردن در حالت کامل» به `/chat` لینک می‌ده.
- منطق و Hook ها (`useConversations`, `useMessages`, `useChatMutations`) عیناً با `ChatPage` مشترکن — بدون تکرار state management. بخش‌های UI مشترک (نمایش یک پیام، کادر ارسال با دکمهٔ پیوست/استیکر) به کامپوننت‌های کوچک مشترک (`MessageBubble`, `MessageComposer`) استخراج می‌شن تا هم صفحهٔ کامل هم ویجت شناور ازشون استفاده کنن.

### ۲. پیوست فایل — استفاده از ستون موجود، بدون Migration

- `SendMessageDto`: `messageText` به `@IsOptional()` تغییر می‌کنه، فیلد جدید `fileUrl?: string` اضافه می‌شه؛ اعتبارسنجی سطح سرویس: **حداقل یکی از `messageText` یا `fileUrl` باید پر باشه**.
- اگه فقط `fileUrl` ارسال بشه (بدون کپشن)، سرویس به‌صورت خودکار `messageText` رو با نام اصلی فایل (که فرانت از خود `File` قبل از آپلود استخراج و در بدنه می‌فرسته، چون جدول ستون `file_name` جدا نداره) پر می‌کنه — همون الگوی fallback که برای نداشتن ستون مجزا لازمه.
- فرانت: همون جریان دومرحله‌ای همیشگی پروژه — `uploadFile()` عمومی (`POST /files`) برای گرفتن `fileUrl`، بعد `POST /chat/conversations/:id/messages` با `{messageText?, fileUrl}`. دکمهٔ گیره (📎) کنار کادر پیام، از `input type=file` مخفی استفاده می‌کنه (بدون محدودیت پسوند جدید — همون allow-list سراسری `StorageService`).
- نمایش پیام دارای فایل: `FileViewer` موجود (پیش‌نمایش/دانلود) به‌جای حباب متنی ساده.

### ۳. استیکر — مجموعهٔ ثابت داخلی، بدون ستون جدید (تصمیم کاربر)

- یک آرایهٔ ثابت ۱۴ ایموجی پرکاربرد محیط کاری (`👍 ❤️ 😂 🙏 ✅ 🎉 👏 😢 😮 🔥 💯 🤝 ⏰ ❌`) در یک فایل مشترک فرانت (`sticker-set.ts`) تعریف می‌شه.
- ارسال استیکر یعنی همون `POST .../messages` با `messageText` = دقیقاً یکی از این ایموجی‌ها (بدون `fileUrl`) — **هیچ فیلد/ستون جدیدی لازم نیست**، از همون مسیر پیام متنی عادی رد می‌شه.
- نمایش: هر پیامی که `fileUrl` نداره و `messageText.trim()` دقیقاً برابر یکی از ۱۴ استیکره، به‌جای حباب معمولی، به‌صورت درشت (بدون پس‌زمینه/حباب، فونت-سایز ~۴۰px) رندر می‌شه — تشخیص کاملاً سمت فرانته، بک‌اند تغییری نمی‌کنه.
- دکمهٔ 😊 کنار کادر پیام یک Popover کوچک با گرید ۱۴تایی استیکرها باز می‌کنه؛ کلیک روی هرکدوم بلافاصله ارسال می‌شه (بدون تایپ، شبیه رفتار استیکر در پیام‌رسان‌های معروف).

## خارج از محدودهٔ این فاز

- استیکر/ایموجی سفارشی یا آپلودی توسط کاربر (فقط مجموعهٔ ثابت داخلی طبق تصمیم کاربر)
- واکنش (Reaction) به پیام‌های دیگران (❤️ زیر یک پیام) — چیز متفاوتی از ارسال استیکر به‌عنوان پیام مستقله
- محدودیت/اعتبارسنجی جدید نوع فایل (همون allow-list سراسری `StorageService` کافیه)
- تبدیل Polling به WebSocket/Real-time واقعی (خارج از این فاز، تصمیم قبلی پروژه)

## معیار پذیرش

- ویجت شناور در همهٔ صفحات (به‌جز خود `/chat`) دیده می‌شه، نشان تعداد کل نخوانده رو نشون می‌ده، و باز/بسته‌شدنش state مکالمهٔ انتخاب‌شده رو با صفحهٔ کامل به‌هم نمی‌ریزه (هر دو از همون Query Cache می‌خونن)
- ارسال پیام با پیوست فایل (با یا بدون کپشن) در هر دو UI (صفحهٔ کامل و ویجت) کار می‌کنه؛ گیرنده فایل رو با `FileViewer` می‌بینه
- کلیک روی هرکدوم از ۱۴ استیکر بلافاصله به‌عنوان یک پیام ارسال می‌شه و در هر دو طرف مکالمه به‌صورت درشت (نه در حباب معمولی) نمایش داده می‌شه
- تست‌های Jest بک‌اند برای اعتبارسنجی «حداقل یکی از متن/فایل» + fallback نام فایل
- Build + Lint هر دو اپ؛ E2E زنده (ارسال پیوست از ویجت شناور در یک صفحهٔ غیر از چت، ارسال استیکر، تأیید نمایش صحیح در صفحهٔ کامل)؛ تأیید بصری
- به‌روزرسانی `README.md`
