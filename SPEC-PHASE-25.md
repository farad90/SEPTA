# SPEC — فاز ۲۵: کالاها (Excel Import/Export + پیوست‌های سطح کالا)

## هدف

دو قابلیت مستقل روی ماژول کالاها (`apps/api/src/item-catalog/*`, `apps/web/src/pages/catalog/CatalogPage.tsx`):

1. درون‌ریزی/برون‌بری اکسل (Import/Export) برای کاتالوگ کالا
2. پیوست فایل در سطح هر کالا (نقشه/کاتالوگ/دیتاشیت) — چیزی که امروز اصلاً وجود نداره

## وضعیت فعلی (خلاصه اکتشاف)

- کاتالوگ کالا CRUD کامل داره ولی هیچ زیرساخت اکسل (نه کتابخونه، نه Endpoint) و هیچ جدول/Endpoint پیوستی نداره.
- الگوی موجود و اثبات‌شده برای «پیوست چندتایی به یک رکورد فرزند» دقیقاً `inquiry_item_documents` است (UUID PK، FK با `ON DELETE CASCADE`، شکل ساده `{file_url, file_name, uploaded_by, uploaded_at}`، دو Endpoint افزودن/حذف زیر پرمیژن دامنه مادر) — همین الگو برای پیوست کالا تکرار می‌شه، با یک تفاوت ساختاری: چون PK جدول `item_catalog` یک کلید طبیعی (`item_code VARCHAR(100)`) هست نه UUID، ستون FK جدول پیوست هم باید `VARCHAR(100) REFERENCES item_catalog(item_code)` باشه، نه UUID.
- `StorageService`/`POST /files`/`GET /files/...` عمومی و آماده‌ست (شامل `.xlsx` در فهرست پسوندهای مجاز)؛ کامپوننت `FileViewer` هم آماده و در ۱۳ فایل دیگه استفاده شده — کاتالوگ اولین باره که ازش استفاده می‌کنه.
- هیچ کتابخونه اکسل (`exceljs`/`xlsx`/...) در مونوریپو نصب نیست — باید اضافه بشه (فقط بک‌اند؛ Parse/Generate هر دو سمت سرور انجام می‌شه، فرانت فقط آپلود/دانلود فایل معمولی).

## تصمیمات طراحی

### ۱. Import — رفتار روی کد کالای تکراری

⚠️ تصمیم کاربر: **فقط ایجاد** — اگه ردیفی از اکسل `item_code`ای داره که از قبل در کاتالوگ هست، اون ردیف رد می‌شه (نه بروزرسانی/Upsert). دلیل: جلوگیری از بازنویسی ناخواسته دیتای موجود از طریق یک فایل اکسل که ممکنه قدیمی/نادرست باشه. اگه کاربر بخواد کالای موجود رو ویرایش کنه، از فرم ویرایش تکی استفاده می‌کنه.

### ۲. Import — مدل خطا: ثبت جزئی + گزارش

⚠️ تصمیم کاربر: Import **Atomic نیست**. هر ردیف مستقل پردازش می‌شه:
- ردیف‌های معتبر (شرح کالا و پارت‌نامبر پر، کد کالا یا خالیه یا تکراری نیست) **ثبت می‌شن**
- ردیف‌های مشکل‌دار (شرح/پارت‌نامبر خالی، یا کد کالای تکراری) **رد می‌شن** ولی مانع بقیه فایل نمی‌شن
- خروجی نهایی یک گزارش برمی‌گردونه: `{ totalRows, createdCount, failedCount, errors: [{ row, itemCode?, message }] }` — عدد `row` شمارهٔ ردیف واقعی در اکسل (برای پیدا کردنش راحت باشه، نه Index صفر‌مبنا)

### ۳. دسترسی (RBAC)

⚠️ تصمیم کاربر: کلید جدید فقط برای Import؛ بقیه از پرمیژن‌های موجود استفاده می‌کنن:
- `catalog.import` (جدید) — «درون‌ریزی اکسل کالاها»؛ چون یک عملیات نوشتاریِ دسته‌جمعی با شعاع اثر بیشتر از افزودن یک کالای تکیه، پرمیژن مجزا می‌خواد. به همون گروه‌های پیش‌فرضی داده می‌شه که امروز `catalog.manage_units` رو دارن (بازرگانی/مدیریت طبق seed فعلی).
- Export — فقط `catalog.view` (خروجی، همون دیتایی‌ست که کاربر همین الان با View می‌بینه، خطر جدیدی اضافه نمی‌کنه)
- پیوست‌ها (افزودن/حذف) — از `catalog.create` موجود استفاده می‌کنن، دقیقاً هم‌الگوی `inquiry_item_documents` که از پرمیژن `inquiry.edit` دامنهٔ مادر استفاده می‌کنه، نه یک کلید مجزا. دیدن پیوست‌های موجود برای هر کسی که `catalog.view` داره آزاده.

### ۴. ستون‌های اکسل (Import و Export مشترک)

هدرهای فارسی، تطبیق بر اساس **نام هدر** نه موقعیت ستون (مقاوم در برابر جابه‌جایی/حذف ستون توسط کاربر):

| هدر فارسی | فیلد DB | الزامی؟ | یادداشت |
|---|---|---|---|
| کد کالا | `item_code` | خیر | خالی = تولید خودکار (`ITM-NNNNNN`، همون شمارندهٔ اتمیک فعلی)؛ پر ولی تکراری = رد ردیف |
| پارت نامبر | `part_number` | بله | خالی = رد ردیف |
| شرح کالا | `item_description` | بله | خالی = رد ردیف |
| سازنده | `builder` | خیر | |
| واحد اندازه‌گیری | `default_measurement_unit` | خیر | متن آزاد ذخیره می‌شه (همون‌طور که فرم دستی هم هیچ محدودیت DB روی این فیلد نداره — فقط UI فرم از `measurement_units` به‌عنوان پیشنهاد استفاده می‌کنه)؛ auto-sync با جدول واحدها **در محدودهٔ این فاز نیست** |
| وضعیت | `status` | خیر | «فعال»/«غیرفعال» یا خالی (پیش‌فرض «فعال») — هر مقدار دیگه هم پیش‌فرض «فعال» می‌شه |

`createdBy` روی هر ردیف موفق، همون کاربر اجراکنندهٔ Import می‌شه (مثل ایجاد دستی).

Export همین ستون‌ها رو برمی‌گردونه (فارسی، همون ترتیب) تا خروجیِ خودش مستقیماً قابل استفاده به‌عنوان الگوی Import باشه.

### ۵. Endpointهای جدید

```
GET  /item-catalog/export?q=&builders=      → catalog.view — خروجی .xlsx، همون فیلترهای q/builders لیست فعلی رو اعمال می‌کنه (بدون صفحه‌بندی — کل نتایج منطبق)
GET  /item-catalog/import-template          → catalog.import — یک .xlsx مینیمال فقط با ردیف هدر + یک ردیف نمونه، برای راهنمایی کاربر
POST /item-catalog/import                   → catalog.import — multipart، فیلد "file"؛ خروجی گزارش بالا
POST /item-catalog/:code/documents          → catalog.create — بدنه {fileUrl, fileName?} (بعد از آپلود عمومی از POST /files)
DELETE /item-catalog-documents/:id          → catalog.create
```

`getByCode()` از این پس `documents` رو هم Eager-load می‌کنه (مرتب بر اساس `uploadedAt asc`، همراه نام آپلودکننده) — مشابه دقیق الگوی `inquiry_item_documents` در `inquiries.service.ts`. لیست کلی (`list()`) پیوست‌ها رو برنمی‌گردونه (سبک‌نگه‌داشتن پاسخ لیست).

### ۶. کتابخونهٔ اکسل

`exceljs` (فقط `apps/api`) — دلیل: فعال نگه‌داشته‌شده، هم Read هم Write رو پوشش می‌ده، بدون CVE شناخته‌شدهٔ باز (بر خلاف نسخه‌های قدیمی `xlsx`/SheetJS). فرانت به هیچ کتابخونهٔ اکسلی نیاز نداره — Export یک دانلود فایل معمولی از سمت سرور است (مثل الگوی `downloadFile` موجود) و Import فقط یک `<input type=file>` است که فایل رو مستقیم (نه از طریق `POST /files` عمومی) به `POST /item-catalog/import` می‌فرسته چون این یک مرحله‌ای پردازش می‌شه، نه ثبت یک `fileUrl` دائمی.

### ۷. جدول پیوست کالا (Migration جدید)

```sql
CREATE TABLE item_catalog_documents (
    id           UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    item_code    VARCHAR(100) NOT NULL REFERENCES item_catalog(item_code) ON DELETE CASCADE,
    file_url     TEXT NOT NULL,
    file_name    VARCHAR(300),
    uploaded_by  UUID REFERENCES users(id),
    uploaded_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

Migration: `0015_catalog_import_attachments` (افزودنی، Rollback با `DROP TABLE`).

### ۸. فرانت‌اند

- **لیست کالاها**: دکمهٔ «خروجی اکسل» (هر کاربر با `catalog.view`) — دانلود با فیلترهای فعلی جست‌وجو/سازنده. دکمهٔ «درون‌ریزی اکسل» (فقط `catalog.import`) → مودال با: لینک «دانلود قالب نمونه»، انتخاب فایل، دکمهٔ ارسال؛ بعد از ارسال، خلاصهٔ نتیجه (X ایجاد شد، Y رد شد) + لیست جمع‌شونده خطاها (شمارهٔ ردیف + دلیل).
- **جزئیات کالا (`ItemDetail`)**: بخش جدید «پیوست‌ها» — همون الگوی `ItemDocuments` در `InquiryItemsPanel.tsx` (آپلود عمومی از `POST /files` سپس ثبت در `POST /item-catalog/:code/documents`، نمایش با `FileViewer`، حذف). آپلود/حذف پشت `catalog.create`؛ مشاهده برای هر کاربر با دسترسی به صفحه.

## خارج از محدودهٔ این فاز

- بروزرسانی/Upsert کالای موجود از طریق اکسل (طبق تصمیم صریح کاربر، فقط ایجاد)
- همگام‌سازی خودکار واحد اندازه‌گیریِ وارد‌شده از اکسل با جدول `measurement_units`
- دسته‌بندی/طبقه‌بندی کالا (طبق تصمیم قدیمی «لیست تخت» — دست‌نخورده می‌مونه)
- اسکن ویروس/بدافزار روی فایل آپلودی (خارج از الگوی فعلی `StorageService` هم هست)
- اتصال واقعی بخش «سابقهٔ استفاده در استعلام‌ها» در `ItemDetail` (پلاسهولدر ثابت فعلی دست‌نخورده می‌مونه — نامرتبط با این فاز)

## معیار پذیرش

- آپلود یک فایل اکسل معتبر، ردیف‌های سالم رو (با کد خودکار یا دستی) ثبت می‌کنه و گزارش دقیق برمی‌گردونه
- ردیف با شرح/پارت‌نامبر خالی یا کد کالای تکراری رد می‌شه، بدون توقف بقیهٔ فایل، و در گزارش خطا با شمارهٔ ردیف واقعی دیده می‌شه
- خروجی اکسل، فیلترهای فعلی صفحهٔ لیست (جست‌وجو/سازنده) رو رعایت می‌کنه و ستون‌بندیش دقیقاً با ورودی Import یکی‌ست
- «دانلود قالب نمونه» یک فایل کوچک با هدر صحیح برمی‌گردونه
- در جزئیات کالا، آپلود/مشاهده/حذف پیوست به‌درستی کار می‌کنه و پشت پرمیژن صحیح قفله
- تست‌های Jest بک‌اند برای منطق ردیف‌به‌ردیف Import (موفق/رد‌شده)، تولید ستون‌های Export، و CRUD پیوست‌ها
- Build + Lint هر دو اپ بدون خطا؛ E2E زنده روی DB واقعی (Import یک فایل نمونه با حداقل یک ردیف خطادار عمدی، Export، پیوست)؛ تأیید بصری در مرورگر
- به‌روزرسانی `erp-schema.sql`، `erp-database-design.md`، و `README.md`
