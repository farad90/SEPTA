# SPEC — فاز ۱۵: صفحه پروفایل کاربری

> پیش‌نیاز: فاز ۱۴ ✅. مرجع بصری: `mockups/profile-page-mockup.jsx`.
> مرجع داده: `erp-schema.sql` — `users` (فیلدهای هویتی از فاز ۱ موجودن) + `user_identity_documents` (تا الان هیچ‌جا استفاده نشده بود).

## هدف فاز ۱۵

صفحه `/profile` — ویرایش اطلاعات فردی خود کاربر (نه ادمین روی کاربر دیگه) + بارگذاری مدارک هویتی الزامی (کارت ملی + صفحات شناسنامه) + نوار پیشرفت تکمیل پروفایل.

## ⚠️ تصمیمات

1. **بدون Migration جدید** — جدول `user_identity_documents` از فاز ۱ (baseline) در دیتابیس موجوده ولی تا الان مدل Prisma نداشت (طبق محدودیت شناخته‌شده فاز ۱ در README). فقط مدل Prisma جدید اضافه می‌شه، نه تغییر ساختار.
2. **داخل UsersModule، نه ماژول جدا** — چون هر دو جدول (`users`, `user_identity_documents`) از قبل تحت دامنه Users هستن؛ برخلاف اعلان/یادآور/چت (دامنه‌های کاملاً جدا در فاز ۱۴)، اینجا فقط دسترسی self-service به همون جدول اضافه می‌شه.
3. **Self-service فقط روی خود کاربر** — همه Endpoint ها از `req.user.userId` استفاده می‌کنن (نه پارامتر `:id`)، پس نیازی به کلید دسترسی RBAC جدید نیست (مثل الگوی `users/colleagues`). این جدا از `PATCH /users/:id` ادمین (که فقط `permissionGroupId`/`status` رو کنترل می‌کنه) می‌مونه.
4. **آپلود فایل** — از `FilesModule` موجود (`POST /files`) استفاده می‌شه، دقیقاً مثل الگوی مستندات پیوستی استعلام/نامه در فازهای قبل.
5. **«جایگزینی» مدرک هویتی** — چون `document_type`+`page_number` قید Unique نداره، جایگزینی در سطح اپلیکیشن پیاده می‌شه: رکورد(های) قبلی همون `document_type` (و همون `page_number` برای شناسنامه) حذف و رکورد جدید درج می‌شه — نتیجه همیشه یک تصویر برای هر اسلات.
6. **درصد تکمیل پروفایل** — کاملاً سمت فرانت محاسبه می‌شه (مثل mockup)، نه یک فیلد ذخیره‌شده در دیتابیس.
7. **نمایش عکس واقعی پروفایل** — چون `profile_photo_url` از فاز ۱ در دیتابیس هست ولی تا الان هیچ‌جا استفاده نمی‌شد، `Avatar` در Topbar (کنار منوی کاربر) بعد از این فاز عکس واقعی رو نشون می‌ده اگه ثبت شده باشه (وگرنه Fallback به حروف اول مثل قبل). سایر جاهای `Avatar` (چت، اعلان‌ها) دست نمی‌خوره — خارج از اسکوپ.
8. **رفع Gap کوچک در منوی کاربر** — Topbar واقعی فعلاً فقط «خروج از سامانه» داره؛ یک آیتم «پروفایل من» به دراپ‌داون منوی کاربر اضافه می‌شه که به `/profile` لینک می‌ده (مطابق mockup که این دکمه رو در Topbar نشون می‌ده).

## بک‌اند (داخل UsersModule)

مدل Prisma جدید: `UserIdentityDocument` (baseline، بدون Migration).

| Endpoint | توضیح |
|---|---|
| `GET /users/me` | پروفایل کامل خودم (فیلدهای هویتی + لیست مدارک) |
| `PATCH /users/me` | ویرایش `mobile, email, birthDate, address, nationalId, birthCertificateNo, profilePhotoUrl` |
| `PUT /users/me/identity-documents` | ثبت/جایگزینی یک مدرک: `{documentType, pageNumber?, fileUrl}` — منطق جایگزینی طبق تصمیم ۵ |
| `DELETE /users/me/identity-documents/:id` | حذف یک صفحه شناسنامه (برای پاک کردن صفحه اضافه‌شدهٔ اشتباهی) |

## فرانت‌اند

- **`/profile`**: پورت مستقیم mockup — کارت اطلاعات فردی (نمایش/ویرایش inline مثل الگوی سایر صفحات)، نوار هشدار تکمیل پروفایل، کارت‌های آپلود مدرک هویتی (کارت ملی + صفحات شناسنامه با دکمه «افزودن صفحه دیگر»)
- **AppShell**: آیتم «پروفایل من» در دراپ‌داون کاربر + Avatar واقعی وقتی عکس ثبت شده

## خارج از اسکوپ

- ویرایش پروفایل کاربر دیگر توسط ادمین (همون `/users` فعلی کافیه)
- نمایش عکس واقعی در چت/اعلان‌ها/سایدبار

## تست‌ها

- **Jest**: منطق جایگزینی مدرک (حذف قبلی + درج جدید)، عدم امکان دسترسی/ویرایش پروفایل کاربر دیگر از طریق این Endpoint ها
- **E2E زنده**: ویرایش پروفایل → مقادیر ذخیره می‌شن؛ آپلود کارت ملی → مدرک ثبت می‌شه؛ آپلود دوباره (جایگزینی) → فقط یک رکورد کارت ملی می‌مونه؛ افزودن صفحه ۲ شناسنامه → دو رکورد جدا با `pageNumber` درست

## Definition of Done

- [ ] مدل Prisma `UserIdentityDocument` + `generate`
- [ ] Backend: 4 Endpoint بالا در UsersModule
- [ ] Web: صفحه `/profile` + لینک «پروفایل من» در Topbar + Avatar واقعی
- [ ] Jest سبز + build/lint + E2E زنده + تست بصری + README
