# SPEC — فاز ۸: سفارش مشتری — تب ۶

> پیش‌نیاز: فاز ۷ ✅ (نتیجه نهایی). مرجع بصری: `OrderTab` در `mockups/inquiry-detail-mockup.jsx`.
> در اختیار **فروش**؛ سفارش خرید از تأمین‌کننده (PO، تب ۷) کاملاً جدا و در فاز بعد.

## هدف فاز ۸

1. ثبت اطلاعات سفارش/قرارداد (شماره سفارش، شماره/تاریخ قرارداد، تاریخ تحویل تعهدشده، فایل قرارداد)
2. اقلام سفارش = **فقط اقلامی که در تب ۵ نتیجه `won` گرفتن** — بدون افزودن/حذف دستی، از نتیجه هم‌رسانی می‌شه
3. پرداخت‌های مشتری (لیست منعطف، نه اقساط ثابت) + ضمانت‌نامه‌های صادره به مشتری
4. طبق تصمیم شما: **حداکثر یک سفارش برای هر پرونده** (بدون نسخه‌بندی)

## ⚠️ دو قانون کسب‌وکاری مهم (تأییدشده توسط شما)

1. **هم‌رسانی خودکار اقلام**: هر بار که تب باز/ذخیره می‌شه، اقلام سفارش با نتیجه فعلی تب ۵ هم‌رسانی می‌شه — قلم‌های تازه `won` اضافه، قلم‌هایی که دیگه `won` نیستن حذف می‌شن. ردیف‌های باقی‌مونده **دست‌نخورده** می‌مونن (قیمت/تعداد ثبت‌شده‌شون بازنویسی نمی‌شه) تا مغایرتی با پرداخت‌های قبلاً ثبت‌شده ایجاد نشه.
2. **منبع قیمت هنگام افزودن قلم جدید**: `purchase_price`/`sale_price` از آخرین نسخه **پذیرفته‌شده** پیشنهاد مالی (`financial_proposals` نسخه `status='current'`) خونده می‌شه؛ اگه پیشنهاد مالی اصلاً ساخته نشده (مسیر غیرمعمول چون تب ۵ بدون قید بازه)، از baseline مرحله انتخاب نهایی (فی مؤثر + `final_sale_price`) استفاده می‌شه. `supplier_id` همیشه از زنجیره آفر منتخب (`selected_offer_item → offer → rfq → supplier`) میاد.

## بک‌اند

مدل‌های Prisma جدید: `Order`, `OrderItem`, `CustomerPayment`, `IssuedGuarantee` (جدول‌ها baseline، Migration جدید لازم نیست).

### OrderModule

| Endpoint | دسترسی | توضیح |
|---|---|---|
| `GET /inquiries/:id/order` | `order.view` | اگه سفارش نساخته شده: `{ exists:false, wonItems }` پیش‌نمایش؛ اگه ساخته شده: هم‌رسانی اقلام + محاسبه `total_amount` + خروجی کامل (هدر + اقلام + پرداخت‌ها + ضمانت‌نامه‌ها) |
| `PUT /inquiries/:id/order` | `order.create` | بار اول: ساخت سفارش (رد با ۴۰۰ اگه هیچ قلم `won` نباشه)؛ بارهای بعد: فقط ویرایش هدر (شماره/قرارداد/تاریخ‌ها/فایل) |
| `POST /inquiries/:id/order/payments` | `order.manage_payments` | افزودن ردیف پرداخت مشتری |
| `PATCH /order-payments/:id` | `order.manage_payments` | ویرایش ردیف (شرح/سررسید/مبلغ/تاریخ دریافت واقعی/وضعیت/فایل سند) |
| `DELETE /order-payments/:id` | `order.manage_payments` | حذف ردیف |
| `POST /inquiries/:id/order/guarantees` | `order.manage_payments` | افزودن ضمانت‌نامه |
| `PATCH /order-guarantees/:id` | `order.manage_payments` | ویرایش (بانک/مبلغ/تاریخ صدور-انقضا/وضعیت) |
| `DELETE /order-guarantees/:id` | `order.manage_payments` | حذف |

**قوانین کسب‌وکاری:**
- `total_amount` همیشه محاسبه‌شده‌ست (`SUM(sale_price × quantity)` اقلام فعلی) — ورودی دستی نیست
- ساخت دوباره سفارش برای پرونده‌ای که از قبل داره، رد نمی‌شه بلکه فقط هدر رو آپدیت می‌کنه (طبق تصمیم «یک سفارش برای هر پرونده»)
- فایل قرارداد/سند پرداخت: آپلود دستی مثل فاز ۶ (لینک `fileUrl` از `/files`، نه تولید خودکار)
- activity فقط روی «ساخت اولیه سفارش» (نه هر sync/ویرایش جزئی)

### دسترسی

از کلیدهای موجود کاتالوگ استفاده می‌شه، چیز جدیدی لازم نیست: `order.view`، `order.create`، `order.manage_payments` (پیش‌فرض فروش کامل، بازرگانی/مدیریت هم دسترسی دارن).

## فرانت‌اند — تب ۶ (مطابق mockup)

- بنر اطلاع‌رسانی: «این مرحله فقط روی اقلام برنده اجرا می‌شه؛ سفارش خرید در تب جدا و توسط بازرگانی»
- اگه هیچ قلم `won` نباشه: پیام «هنوز قلم برنده‌ای در این پرونده ثبت نشده» به‌جای فرم
- کارت «اطلاعات سفارش و قرارداد» + آپلود فایل قرارداد
- کارت «اقلام سفارش (فقط اقلام برنده)» — فقط‌خواندنی، از GET هم‌رسانی‌شده میاد؛ جمع کل زیرش
- `FlexPaymentList` (کامپوننت جدید و قابل استفاده مجدد برای PO فاز بعد): ردیف‌های شرح/سررسید/مبلغ/وضعیت/پیوست سند + افزودن/حذف ردیف
- کارت ضمانت‌نامه‌های صادره: نوع (پیش‌پرداخت/حسن انجام کار)/مبلغ/بانک/تاریخ انقضا + افزودن
- دکمه «ثبت سفارش مشتری» (ساخت اولیه) / «ذخیره تغییرات» (ویرایش‌های بعدی)
- گیت دسترسی: بدون `order.view` پیام «دسترسی ندارید»؛ بدون `order.create` فرم هدر read-only؛ بدون `order.manage_payments` بخش پرداخت/ضمانت‌نامه read-only

## خارج از اسکوپ فاز ۸ (به‌صراحت)

- تب ۷ «سفارش خرید (PO)» — فاز ۹
- محاسبه مغایرت زمان تحویل واقعی/تعهدشده (دامنه ۷ — تحویل)
- گردش‌کار تأیید ضمانت‌نامه با بانک (فقط ثبت اطلاعات، نه یکپارچگی بانکی)

## تست‌ها

- **Jest**: رد ساخت سفارش بدون قلم `won`؛ هم‌رسانی اقلام (افزودن قلم تازه‌won، حذف قلم دیگه-won-نیست، حفظ ردیف دست‌نخورده)؛ منبع قیمت (اول از پیشنهاد مالی، fallback به Selection)؛ محاسبه `total_amount`؛ CRUD پرداخت/ضمانت‌نامه
- **E2E زنده**: استعلام دو-قلمی → یکی won یکی lost → ساخت سفارش (فقط قلم برنده) → افزودن پرداخت+ضمانت‌نامه → اصلاح نتیجه در تب ۵ (قلم دوم هم won بشه) → GET سفارش و بررسی افزوده‌شدن خودکار قلم دوم با حفظ قلم اول → پاک‌سازی

## Definition of Done

- [ ] GET با هم‌رسانی خودکار عضویت اقلام (بدون بازنویسی ردیف‌های موجود) + محاسبه total
- [ ] PUT ساخت/ویرایش هدر + CRUD پرداخت/ضمانت‌نامه
- [ ] منبع قیمت لایه‌ای (پیشنهاد مالی پذیرفته‌شده → fallback انتخاب نهایی)
- [ ] تب ۶ مطابق mockup (بنر، فرم هدر، اقلام فقط‌خواندنی، دو FlexPaymentList)
- [ ] Jest سبز + build/lint + E2E زنده + تست بصری
