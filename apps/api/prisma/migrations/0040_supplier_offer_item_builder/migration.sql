-- فاز ۵۵ — بازخورد کاربر: هنگام ثبت پیشنهاد قیمت تأمین‌کننده، برند/سازنده‌ی همون پیشنهاد هم
-- وارد بشه (ممکنه با برند اولیه‌ی ثبت‌شده روی خود استعلام فرق داشته باشه — مثلاً استعلام معادل
-- بوده و تأمین‌کننده برند دیگه‌ای پیشنهاد داده). این برند در پیشنهاد فنی/مالی به مشتری درج می‌شه.

ALTER TABLE supplier_offer_items ADD COLUMN builder VARCHAR(200);

-- Rollback:
-- ALTER TABLE supplier_offer_items DROP COLUMN builder;
