-- ============================================================
-- فاز ۳۴ — محرمانگی اطلاعات بازرگانی از دید فروش
-- ============================================================

-- نسخه‌ی عمومی (بدون نام تأمین‌کننده/مبلغ) برای ورودی‌های activity که اطلاعات
-- بازرگانی حساس دارن؛ NULL یعنی این ورودی نیازی به نسخه‌ی محدود نداره (پیام‌های
-- آزاد کاربر، یا ورودی‌هایی که از اول عمومی‌ان) و comment_text کامل برای همه نمایش داده می‌شه.
ALTER TABLE inquiry_discussions ADD COLUMN comment_text_restricted TEXT;
