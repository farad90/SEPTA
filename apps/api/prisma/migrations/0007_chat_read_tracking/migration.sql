-- ============================================================
-- فاز ۱۴ — افزودن last_read_at به chat_participants برای محاسبهٔ
-- واقعی «تعداد پیام نخوانده» هر مکالمه (schema اصلی این ستون رو نداشت)
-- ============================================================

ALTER TABLE chat_participants
    ADD COLUMN last_read_at TIMESTAMPTZ;
