services:
  postgres:
    image: postgres:16-alpine
    container_name: septa_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: septa
      POSTGRES_PASSWORD: septa_dev_password
      POSTGRES_DB: septa
    ports:
      - "5432:5432"
    volumes:
      - septa_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U septa -d septa"]
      interval: 5s
      timeout: 5s
      retries: 10

  # P1-E3-F1-T1 — Redis: cache/queue/pub-sub infrastructure for Phase 2 work;
  # not used for anything yet beyond the health check.
  redis:
    image: redis:7-alpine
    container_name: septa_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - septa_redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    container_name: septa_api
    restart: unless-stopped
    env_file:
      - apps/api/.env
    environment:
      # داخل شبکه Docker، هاست پستگرس "postgres" است، نه localhost — این مقدار
      # env_file بالا رو override می‌کنه.
      DATABASE_URL: postgresql://septa:septa_dev_password@postgres:5432/septa?schema=public
      REDIS_URL: redis://redis:6379
    ports:
      - "3000:3000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    # P1-E2-F1-T1 — curl/wget aren't installed in the api image (only
    # ca-certificates/fonts-liberation/chromium are, for Puppeteer), so the
    # check uses Node's own http module instead of shelling out to either.
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "require('http').get('http://localhost:3000/health',(r)=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))",
        ]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 20s

volumes:
  septa_postgres_data:
  septa_redis_data:
