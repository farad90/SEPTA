# فاز ۲۹ — چت: تجربهٔ تلگرام‌مانند

> این سند برخلاف روال معمول (اسپک قبل از پیاده‌سازی)، بعد از پیاده‌سازی نوشته شده — چون کاربر
> ۵ بازخورد مشخص و بدون ابهام روی فاز ۲۸ داد و به‌جای چرخهٔ اسپک/تایید مستقیم پیاده‌سازی درخواست شد.
> این سند فقط برای هم‌سویی با الگوی مستندسازی هر فاز (لینک از README) نگه داشته می‌شه.

بدون Migration — همهٔ تغییرات یا کاملاً فرانت‌اندن یا از دادهٔ از قبل موجود در اسکیما استفاده می‌کنن.

## ۱. ویجت شناور → نوار ثابت (مثل فیسبوک مسنجر)

مشکل گزارش‌شده: «آیکون چت وقتی روش کلیک می‌کنی تو صفحه جابه‌جا می‌شه». راه‌حل معماری (نه صرفاً بصری):
`ChatWidget` حالا یک ستون Flex داخل کانتینر `fixed bottom-0 left-4` است که نوار (دکمهٔ اصلی) همیشه
**آخرین فرزند** آن ستونه با ارتفاع ثابت (`h-11`)؛ پنل گفتگو فقط وقتی `open=true` باشه به‌عنوان فرزند
اول همون ستون رندر می‌شه. چون کانتینر فقط `bottom` دارد نه `top`، رشد ارتفاع محتوا همیشه به‌سمت بالا
اتفاق می‌افته و لبهٔ پایین (جایی که نوار قرار داره) هیچ‌وقت جابه‌جا نمی‌شه — نه فقط یک وصلهٔ CSS بلکه
تضمین ساختاری.

## ۲. اسکرول خودکار به آخرین پیام

هوک مشترک جدید `use-auto-scroll.ts` (استفاده در `ChatPage` و `ChatWidget`):
- با تعویض مکالمه، بی‌انیمیشن می‌ره ته لیست.
- با افزایش تعداد پیام‌ها: اگه کاربر نزدیک پایین لیست بود (کمتر از ۱۲۰px فاصله)، خودکار و نرم اسکرول
  می‌کنه؛ در غیر این‌صورت (کاربر داره تاریخچه رو بالاتر می‌خونه) به‌جای پرت‌کردنش پایین، دکمهٔ شناور
  «پیام‌های جدید ↓» ظاهر می‌شه که با کلیک اسکرول می‌کنه.

## ۳. پیکر ایموجی/استیکر بازطراحی‌شده

بازخورد: «استیکرها بی‌کیفیت‌ان، استیکرهای دیفالت اندروید/iOS رو بذار، اول باید بره تو کادر تایپ».

- کپی کردن مستقیم استیکرهای اختصاصی اپل/گوگل از نظر کپی‌رایت درست نیست — چیزی که واقعاً «پیش‌فرض
  کیبورد» رو می‌سازه، خودِ کاراکترهای یونیکد استانداردن (رندرشون به فونت سیستم‌عامل بستگی داره، نه
  چیزی که سرور بفرسته). راه‌حل: `sticker-set.ts` از ۱۴ ایموجی ثابت به ۷ دسته (لبخندها/ژست‌ها/قلب‌ها/
  حیوانات/غذا/فعالیت‌ها/نمادها، مجموعاً ~۱۸۰ ایموجی) با آیکون تب برای هر دسته گسترش پیدا کرد.
- `MessageComposer`: کلیک روی ایموجی داخل پیکر، به‌جای ارسال فوری، در موقعیت Cursor داخل کادر متن
  درج می‌شه (`insertEmoji`)؛ پاپ‌آور باز می‌مونه تا چند ایموجی پشت‌سرهم انتخاب بشه؛ ارسال واقعی فقط با
  دکمهٔ ارسال یا Enter.
- تشخیص «پیام-استیکر» (نمایش اموجی بزرگ بدون حباب چت) از تطبیق با یک لیست ثابت به یک Regex عمومی
  تغییر کرد: متن پیام باید کاملاً از ۱ تا ۳ واحد ایموجی (با پشتیبانی دنبالهٔ ZWJ و Variation Selector)
  تشکیل شده باشه — دقیقاً مثل تلگرام/واتس‌اپ/آیمسیج. این یعنی چه ایموجی از پیکر خودمون انتخاب بشه چه
  مستقیم از کیبورد سیستم‌عامل تایپ بشه، یکسان تشخیص داده می‌شه.

## ۴. پیش‌نمایش درون‌خطی پیوست (مثل تلگرام)

`FileViewer.tsx`: `FilePreviewModal` (Lightbox موجود) و `isImageFile` جدید export شدن تا حباب‌های چت
هم بتونن مستقیم ازشون استفاده کنن، بدون تکرار منطق.

`MessageBubble.tsx` → `AttachmentPreview`:
- فایل تصویری: به‌جای ردیف آیکون چشم/دانلود، خودِ تصویر با گوشهٔ گردشده و بدون پدینگ/پس‌زمینهٔ حباب
  (مثل تلگرام) درون‌خطی نمایش داده می‌شه (`AuthImage`، چون دانلود پشت JWT است)؛ کلیک = باز شدن همون
  Lightbox موجود برای بزرگ‌نمایی.
- فایل غیرتصویری: کارت سند (آیکون `FileText` + نام فایل) که کلیکش هم همون Lightbox رو باز می‌کنه
  (برای PDF پیش‌نمایش می‌کنه، برای بقیه پیشنهاد دانلود می‌ده — رفتار از قبل موجود در `FileViewer`).

## ۵. رسید خواندن (Read Receipt) — فقط مکالمهٔ دونفره

ستون `chat_participants.last_read_at` از فاز ۱۴ در اسکیما وجود داشت (برای شمارش پیام نخوانده) ولی
هیچ‌وقت در پاسخ API به فرانت افشا نمی‌شد. تنها تغییر بک‌اند این فاز:

- `chat.service.ts` (`listMine`/`getConversationDetail`): هر شرکت‌کننده در آرایهٔ `participants` حالا
  `lastReadAt` هم داره — بدون کوئری جدید، چون `include` از قبل خودِ رکورد `ChatParticipant` رو برمی‌گردوند،
  فقط map خروجی عوض شد.

فرانت (`chat-shared.tsx`): `otherParticipant()` طرف مقابل مکالمهٔ دونفره رو پیدا می‌کنه؛
`lastReadMessageId()` آخرین پیامی که من فرستادم و طرف مقابل خونده رو پیدا می‌کنه؛ `getReadStatus()`
برای هر پیام یک `{isRead, showLabel, readAt}` می‌سازه. `MessageBubble` بر این اساس:
- تیک تک (خاکستری) یا تیک دوتایی (رنگی) کنار ساعت هر پیام — مثل واتس‌اپ، روی تک‌تک پیام‌ها.
- برچسب متنی «خوانده شد HH:MM» فقط زیر **آخرین** پیام خوانده‌شده — مثل تلگرام، برای جلوگیری از
  شلوغی بصری.

⚠️ چون فقط یک `lastReadAt` در سطح مکالمه داریم (نه زمان دقیق خواندن هر پیام)، ساعت نمایش‌داده‌شده
برای همهٔ پیام‌های خوانده‌شده در یک رویداد خواندن یکسانه — تقریب معقولی با دادهٔ موجود، بدون نیاز به
جدول/ستون جدید.

⚠️ فقط برای مکالمهٔ دونفره (`conversationType='direct'`) پیاده شد — «دیده‌شده توسط N نفر» برای گروه
پیچیدگی اضافه‌ای داره که در این فاز درخواست نشده بود.

## فاصلهٔ Polling

برای حس زنده‌تر (رسید خواندن نزدیک به لحظه‌ای)، `refetchInterval` مکالمات از ۱۵ به ۸ ثانیه و پیام‌ها
از ۵ به ۳ ثانیه کم شد.

## بدون تغییر

- هیچ Migration/ستون/جدول جدیدی لازم نشد.
- منطق ارسال/دریافت پیام در بک‌اند (`chat.service.ts#sendMessage`) دست‌نخورده موند.
