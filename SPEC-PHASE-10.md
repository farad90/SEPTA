# SPEC — فاز ۱۰: پیگیری تولید + بسته‌بندی — تب ۸ (بخش اول دامنه ۶)

> پیش‌نیاز: فاز ۹ ✅ (PO). مرجع بصری: بخش اول `ShippingTab` در `mockups/inquiry-detail-mockup.jsx`.
> در اختیار **فروش و بازرگانی هر دو** (طبق تصمیم شما).

## هدف فاز ۱۰

1. پیگیری وضعیت تولید نزد هر تأمین‌کننده — **در سطح هر PO** (نه هر تأمین‌کننده به‌صورت کلی): `in_production` → `ready_to_ship` → `in_transit` + یادداشت‌های پیگیری دوره‌ای
2. وقتی وضعیت `ready_to_ship` شد: اطلاعات محل pickup (آدرس/تلفن/شخص رابط) + تعریف بسته‌بندی (ابعاد/وزن) برای همون PO

## ⚠️ دو محدودیت اسکوپ (تأییدشده توسط شما)

1. **«دریافت کالا در انبار» (`warehouse_receipts`) در این فاز نیست.** این جدول در اسکیما به `shipment_id` الزامی وصله که فقط در ماژول سراسری «مدیریت بارها» (فاز بعدی) ساخته می‌شه — پس با پیاده‌سازی الان امکان نداره. آخر تب ۸ mockup («دریافت کالا در انبار») به یک فاز بعدی موکول می‌شه.
2. **ماژول سراسری «مدیریت بارها»** (استعلام حمل + پیگیری محموله/گمرک، مرجع بصری `shipment-management-mockup.jsx`) هم یک فاز جداست. کارت «وضعیت محموله (فقط‌خواندنی)» در این mockup هم در این فاز نیست — چون به `shipments` وصله که هنوز وجود نداره.
3. **`package_items`** (اینکه کدوم قلم دقیقاً در کدوم بسته‌ست) در این فاز پیاده نمی‌شه — mockup برای این بخش هیچ UI ای نداره (فقط ابعاد/وزن/محل pickup هر بسته رو می‌گیره، بدون تخصیص قلم). اگه بعداً لازم شد، به‌عنوان بهبود جدا اضافه می‌شه.

## بک‌اند

مدل‌های Prisma جدید: `ProductionTracking`, `ProductionTrackingLog`, `Package` (جدول‌ها baseline، Migration جدید لازم نیست؛ `package_items` این فاز نیست).

### ShippingModule

| Endpoint | دسترسی | توضیح |
|---|---|---|
| `GET /inquiries/:id/production-tracking` | `shipping.view` | آرایه‌ای به‌ازای هر PO این پرونده — وضعیت تولید (خودکار seed با `in_production` در اولین GET)، یادداشت‌های پیگیری، بسته‌ها |
| `PATCH /inquiries/:id/production-tracking/:poId` | `shipping.record_packaging` | تغییر وضعیت + `estimated_ready_date` + اطلاعات pickup (فقط وقتی `ready_to_ship`) |
| `POST /inquiries/:id/production-tracking/:poId/logs` | `shipping.record_packaging` | افزودن یادداشت پیگیری (تاریخ + متن + پیوست اختیاری) |
| `POST /inquiries/:id/production-tracking/:poId/packages` | `shipping.record_packaging` | افزودن بسته جدید (شماره خودکار «بسته N») |
| `PATCH /packages/:id` | `shipping.record_packaging` | ویرایش ابعاد/وزن/محل pickup، یا اعلام «آماده حمل» (`status → ready_to_ship`) |
| `DELETE /packages/:id` | `shipping.record_packaging` | حذف بسته — فقط وقتی هنوز `defining`ه |

**قوانین کسب‌وکاری:**
- هر PO دقیقاً یک `production_tracking` داره (بدون Constraint دیتابیسی، مثل الگوی «یک سفارش برای هر پرونده» در فاز ۸ — در سطح اپلیکیشن با seed خودکار تضمین می‌شه)
- تغییر وضعیت آزادانه‌ست (بدون قفل جهت — قابل برگشت برای اصلاح، هم‌سو با روح پروژه)
- بعد از `ready_to_ship` شدن یک بسته، دیگه قابل ویرایش/حذف نیست (فقط نمایش)
- یادداشت‌های پیگیری فقط افزودنی‌ان (بدون ویرایش/حذف — مطابق mockup)
- شماره بسته خودکار: «بسته ۱»، «بسته ۲»، ... بر اساس تعداد بسته‌های موجود همون PO + ۱

### دسترسی
از کلیدهای موجود استفاده می‌شه: `shipping.view`، `shipping.record_packaging`. طبق تصمیم شما، این دو کلید به **گروه پیش‌فرض فروش هم اضافه می‌شن** (الان فقط بازرگانی داشت) — اصلاح در `permission-catalog.ts` + reseed.

## فرانت‌اند — تب ۸ (بخش اول، مطابق mockup)

- کارت جدا برای هر PO این پرونده: نام تأمین‌کننده + dropdown وضعیت (رنگ بر اساس وضعیت)
- لیست یادداشت‌های پیگیری + فرم افزودن (فقط وقتی `in_production`)
- وقتی `ready_to_ship`: فرم اطلاعات pickup + لیست بسته‌ها (هرکدوم ابعاد/وزن قابل‌ویرایش تا قبل از اعلام آماده + دکمه «اعلام آماده حمل») + «افزودن بسته»
- وقتی `in_transit`: پیام «کالا در حال حمله — دیگر نیازی به پیگیری نیست»
- گیت دسترسی: بدون `shipping.view` پیام «دسترسی ندارید»؛ بدون `shipping.record_packaging` فرم‌ها read-only

## خارج از اسکوپ فاز ۱۰ (به‌صراحت)

- «دریافت کالا در انبار» (`warehouse_receipts`) — فاز بعد از ساخته‌شدن ماژول محموله‌ها
- ماژول سراسری «مدیریت بارها» (استعلام حمل + پیگیری محموله/گمرک) — فاز جدا (مرجع: `shipment-management-mockup.jsx`)
- `package_items` (تخصیص قلم به بسته) — بدون UI در mockup، بعداً در صورت نیاز

## تست‌ها

- **Jest**: seed خودکار تولید نسخه اولیه هر PO؛ رد ویرایش/حذف بسته بعد از `ready_to_ship`؛ شماره‌گذاری خودکار بسته؛ افزودن یادداشت پیگیری
- **E2E زنده**: استعلام با یک PO → GET (seed خودکار `in_production`) → افزودن یادداشت پیگیری → تغییر وضعیت به `ready_to_ship` + ثبت pickup → افزودن دو بسته → اعلام آماده حمل یکی → تلاش ویرایش بسته آماده‌شده (باید رد بشه) → پاک‌سازی

## Definition of Done

- [ ] GET با seed خودکار هر PO + یادداشت‌ها + بسته‌ها
- [ ] PATCH وضعیت/pickup + POST یادداشت + CRUD بسته با قفل بعد از آماده‌سازی
- [ ] `shipping.view`/`shipping.record_packaging` به فروش هم اضافه شد (seed)
- [ ] تب ۸ (بخش اول) مطابق mockup — کارت هر PO، شرطی‌بودن بخش‌ها بر اساس وضعیت
- [ ] Jest سبز + build/lint + E2E زنده + تست بصری
