# SPEC — فاز ۲۳: پروفایل کاربری (کراپ عکس، ولیدیشن موبایل، اصلاح الزام مدارک هویتی)

> دومین زیرفاز از بستهٔ اصلاحات فراگیر قبل از دامنهٔ خزانه‌داری (بعد از فاز ۲۲ — زیرساخت مشترک UI).

## هدف

سه اصلاح مستقل در `apps/web/src/pages/profile/ProfilePage.tsx` + یک اصلاح کوچک بک‌اند:

1. امکان Crop عکس پروفایل بلافاصله بعد از انتخاب فایل، قبل از آپلود نهایی
2. ولیدیشن شماره موبایل (باید با `09` شروع بشه و ۱۱ رقم باشه) — هم فرانت هم بک‌اند
3. در بخش مدارک هویتی، فقط «شناسنامه — صفحه ۱» الزامی باشه؛ کارت ملی دیگه الزامی نیست

## تصمیمات طراحی

### ۱. کراپ عکس پروفایل

- هیچ کتابخونه Crop در پروژه موجود نیست؛ چون پیاده‌سازی درگ/زوم/کراپ روی Canvas از صفر پرریسک و پرحجمه، کتابخونهٔ سبک `react-easy-crop` (بدون Dependency اضافه، ~۵kB gzip) اضافه می‌شه
- جریان: انتخاب فایل از `<input type="file">` → به‌جای آپلود فوری، فایل به‌صورت Object URL در یک Modal کراپ (دایره‌ای/مربعی ۱:۱ — هم‌راستا با نمایش گرد فعلی `ProfileAvatar` که `rounded-full`ه) باز می‌شه → کاربر درگ/زوم می‌کنه → دکمه «تأیید و آپلود» عکس نهایی رو با Canvas می‌بره، به `Blob` تبدیل می‌کنه، در `File` (با پسوند `.jpg`) می‌پیچه، و از همون `uploadFile()` موجود عبور می‌ده — **بدون نیاز به تغییر بک‌اند** (بررسی شد: `FilesController`/`StorageService` فقط پسوند و حجم فایل رو چک می‌کنن، نه اینکه فایل اصلی باشه یا Re-encode شده)
- دکمه «انصراف» در Modal کراپ = بستن بدون آپلود (فایل انتخاب‌شده دور ریخته می‌شه)
- کامپوننت جدید: `apps/web/src/components/ui/ImageCropModal.tsx` (قابل استفادهٔ مجدد — فقط پروفایل الان نیازش داره، ولی مثل `FileViewer`/`AmountInput` به‌صورت مستقل ساخته می‌شه)

### ۲. ولیدیشن شماره موبایل

- ⚠️ الگوی دقیقاً موجود در `CreateUserDto` (`@Matches(/^09\d{9}$/)`) الان روی مسیر خودسرویس پروفایل (`UpdateProfileDto.mobile`, فقط `@IsString()`) اعمال نشده — این یه Gap واقعیه، نه فقط یه فیچر جدید
- بک‌اند: همون Decorator عیناً به `UpdateProfileDto.mobile` اضافه می‌شه (فقط وقتی مقدار غیرخالیه؛ `@IsOptional()` می‌مونه چون موبایل اجباری نیست)
- فرانت: فیلد موبایل در فرم ویرایش پروفایل (`ProfilePage.tsx`) موقع تایپ/Blur با همون Regex چک می‌شه؛ اگه پر باشه ولی نامعتبر، پیام خطای هم‌الگوی `DualDateInput` (متن قرمز زیر فیلد) نشون داده می‌شه و دکمه «ذخیره» غیرفعال می‌مونه

### ۳. اصلاح الزام مدارک هویتی

- الزام مدارک هویتی صرفاً یه مفهوم نمایشی/UI در `ProfilePage.tsx`ه (بک‌اند هیچ اجباری نداره) — پس این اصلاح کاملاً Frontend-only است
- `nationalIdCardDoc` از محاسبهٔ درصد تکمیل پروفایل و از برچسب «الزامی» حذف می‌شه؛ بخش کارت ملی همچنان قابل بارگذاریه ولی بدون برچسب الزام (رنگ خنثی به‌جای زرد هشدار وقتی خالیه)
- فقط `pageSlots` صفحهٔ ۱ شناسنامه (`birth_certificate_page`, `page_number=1`) در محاسبهٔ تکمیل و برچسب «الزامی» می‌مونه؛ صفحات بعدی شناسنامه (۲+) همچنان اختیاری و قابل افزودن هستن (بدون تغییر)
- عنوان بخش از «مدارک هویتی (الزامی)» به «مدارک هویتی» تغییر می‌کنه (چون کل بخش دیگه یکدست الزامی نیست)، و «شناسنامه — صفحه ۱» زیرش برچسب «الزامی» می‌گیره

### ۴. حذف عکس پروفایل + کراپ/حذف لوگوی شرکت (افزودهٔ بعدی — درخواست کاربر)

- **حذف عکس پروفایل**: آیکون سطل زبالهٔ کوچک کنار دکمهٔ دوربین (فقط وقتی `profile.profilePhotoUrl` مقدار داره) — با کلیک، `profilePhotoUrl: null` به `PATCH /users/me` ارسال می‌شه. چون `UpdateProfileDto.mobile`ی قبلی فقط `@IsString()` داشت (نه Nullable)، تایپ `profilePhotoUrl` هم در DTO هم در `UpdateProfileBody` فرانت به `string | null` عریض شد؛ `class-validator`ی `@IsOptional()` مقدار `null` رو هم رد نمی‌کنه، پس بدون تغییر منطق بک‌اند کار می‌کنه.
- **لوگوی شرکت (`apps/web/src/pages/partners/PartnersPage.tsx`)**: همون `ImageCropModal` با `cropShape="rect"` (نه `"round"`) چون لوگو در جعبهٔ `rounded-md` مربعی نمایش داده می‌شه نه دایره‌ای — برای همین Prop اختیاری `cropShape`/`title` به `ImageCropModal` اضافه شد (پیش‌فرض `"round"`/«برش عکس پروفایل» برای عکس پروفایل، override به `"rect"`/«برش لوگوی شرکت» برای لوگو). دکمهٔ حذف لوگو از قبل وجود داشت (`set("logoUrl", "")` روی حالت محلی فرم، قبل از Submit) — فقط کراپ قبل از آپلود اضافه شد.

## خارج از محدوده این فاز

- تغییر بک‌اند برای اعمال اجباری مدارک هویتی (همچنان مفهوم UI می‌مونه، طبق طراحی فعلی)
- کراپ برای عکس رابطین (`partner_contacts.photo_url`) — همون الگو در دسترسه ولی این فاز پیاده‌ش نکرد

## معیار پذیرش

- انتخاب فایل عکس پروفایل بلافاصله Modal کراپ باز می‌کنه؛ تأیید کراپ عکس بریده‌شده رو آپلود و ذخیره می‌کنه (نه فایل اصلی خام)
- شماره موبایل نامعتبر (مثلاً شروع با غیر ۰۹ یا طول اشتباه) هم در فرانت جلوی ذخیره رو می‌گیره هم در بک‌اند رد می‌شه (۴۰۰)
- در بخش مدارک هویتی، فقط عدم بارگذاری «شناسنامه — صفحه ۱» در نوار پیشرفت/برچسب الزامی تأثیر می‌ذاره؛ نبود کارت ملی دیگه پیشرفت رو کم نمی‌کنه
- آیکون حذف کنار عکس پروفایل (فقط وقتی عکس موجوده) با یک کلیک عکس رو پاک می‌کنه و آواتار به حروف اول برمی‌گرده
- در فرم شرکت‌ها، انتخاب فایل لوگو همون Modal کراپ رو با شکل مربعی (`cropShape="rect"`) و عنوان «برش لوگوی شرکت» باز می‌کنه؛ دکمهٔ حذف لوگوی موجود هم دست‌نخورده کار می‌کنه
- تست‌های Jest بک‌اند برای Regex جدید موبایل روی `UpdateProfileDto`
- Build + Lint هر دو اپ بدون خطا؛ تأیید بصری در مرورگر (کراپ عکس واقعی، خطای موبایل نامعتبر، برچسب‌های جدید مدارک هویتی)
- به‌روزرسانی `README.md`
