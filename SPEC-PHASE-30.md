# فاز ۳۰ — چت: قابلیت‌های قدرتمند (بازخورد کاربر روی فاز ۲۹)

> این سند هم مثل فاز ۲۹ بعد از پیاده‌سازی نوشته شده — کاربر ۶ درخواست مشخص داد و مستقیم
> پیاده‌سازی شد، بدون چرخهٔ اسپک/تایید. برای هم‌سویی با الگوی مستندسازی هر فاز نگه داشته می‌شه.

## ۱. ویرایش/حذف پیام توسط فرستنده (شامل Migration)

Migration `0017_chat_message_edit_delete`: دو ستون `edited_at`/`deleted_at` روی `chat_messages`.

- حذف **Soft Delete** است (مثل تلگرام) — رکورد پاک نمی‌شه، فرانت به‌جای متن واقعی یک
  تومبستون «این پیام حذف شد» نشون می‌ده تا جریان گفت‌وگو به‌هم نریزه.
- ویرایش فقط `message_text`/`edited_at` رو عوض می‌کنه؛ بدون تاریخچهٔ نسخه (پیام‌رسانی غیررسمیه،
  نه سند تجاری قابل حسابرسی مثل `financial_proposals`).
- چک مالکیت (`assertOwnMessage`) در سطح اپلیکیشن — بدون کلید RBAC جدید، چون این «مالکیتِ
  خود پیام» است نه یک دسترسی سازمانی.
- بک‌اند: `PATCH /chat/messages/:id`، `DELETE /chat/messages/:id`.
- فرانت: هاور روی پیام‌های خودم دو آیکون کوچک (ویرایش/حذف) نشون می‌ده؛ ویرایش پیام رو به یک
  کادر متن درون‌خطی تبدیل می‌کنه؛ حذف از همون `ConfirmModal` مشترک پروژه استفاده می‌کنه.

## ۲. ساخت مکالمهٔ جدید از حالت ویجت (غیرتمام‌صفحه)

`NewGroupModal`/`NewDirectModal` از `ChatPage.tsx` به فایل مشترک `NewConversationModals.tsx`
استخراج شدن؛ `ChatWidget` حالا دو آیکون (پیام مستقیم جدید / گروه جدید) در هدر لیست داره که
همون مودال‌های موجود رو باز می‌کنه — بدون تکرار کد.

## ۳. Drag & Drop فایل/عکس

`MessageComposer` یک متد `addFiles` روی `ref` (`useImperativeHandle`) expose می‌کنه؛ والد
(`ChatPage`/`ChatWidget`) کل ناحیهٔ پیام‌ها رو Drop-Zone می‌کنه (`onDragEnter/Over/Leave/Drop`)
و در drop، فایل‌ها رو به `composerRef.current.addFiles(...)` می‌فرسته. یک Overlay با پس‌زمینهٔ
نیمه‌شفاف و border نقطه‌چین حین کشیدن فایل روی ناحیه نشون داده می‌شه.

## ۴. کپشن مثل تلگرام (شامل رفع یک باگ واقعی)

فایل انتخاب‌شده (چه با دکمهٔ 📎 چه Drag & Drop) بلافاصله آپلود/ارسال نمی‌شه — اول در یک نوار
پیش‌نمایش (thumbnail برای تصویر، آیکون+نام برای بقیه، با دکمهٔ × حذف) بالای کادر متن می‌شینه؛
کادر متن به «افزودن توضیح (اختیاری)...» تبدیل می‌شه؛ فقط با زدن دکمهٔ ارسال واقعاً آپلود و
ارسال می‌شه. چند فایل هم‌زمان قابل قبوله (مثل آلبوم تلگرام، هرکدوم پیام جدا با همون کپشن مشترک،
چون مدل دیتابیس یک‌پیوست-در-هر-پیام است، بدون نیاز به گروه‌بندی بصری اضافه).

⚠️ **باگ کشف‌شده حین تست زنده و رفع‌شده در همین فاز**: تشخیص «آیا این پیوست تصویره؟» قبلاً
از پسوند `messageText` انجام می‌شد (`MessageBubble.tsx`) — درست کار می‌کرد چون تا فاز ۲۹
`messageText` همیشه دقیقاً همون نام فایل بود. حالا که کپشن دلخواه ممکنه، وقتی کاربر یک عکس رو
با کپشن دلخواه (نه اسم فایل) می‌فرسته، تشخیص تصویر اشتباه می‌شد و به‌جای پیش‌نمایش درون‌خطی،
کارت سند نشون داده می‌شد. رفع شد: تشخیص حالا از پسوند خودِ `fileUrl` انجام می‌شه (که همیشه
پسوند واقعی فایل رو حفظ می‌کنه، فارغ از کپشن) — و کپشن حالا همیشه به‌صورت یک خط متن زیر تصویر
هم نمایش داده می‌شه (قبلاً برای تصاویر اصلاً جایی نشون داده نمی‌شد).

## ۵. جست‌وجو در پیام‌ها

کاملاً سمت کلاینت (بدون تغییر بک‌اند) — چون پیام‌های هر مکالمه از قبل بدون صفحه‌بندی کامل
لود می‌شن. هوک مشترک `use-message-search.ts`: فیلتر substring روی `messageText` (پیام‌های
حذف‌شده مستثنا)، شمارندهٔ N/M، پیمایش بعدی/قبلی، اسکرول‌خودکار+هایلایت پیام فعلی — دقیقاً
مثل Ctrl+F داخل یک چت در تلگرام دسکتاپ/وب. آیکون جست‌وجو در هدر مکالمهٔ بازشده (هم صفحهٔ کامل
هم ویجت) قرار داره.

## ۶. استیکر با گرافیک باکیفیت‌تر (Twemoji محلی)

بازخورد: «استیکرها با گرافیک بهتر در حد تلگرام/واتس‌اپ». فونت ایموجی پیش‌فرض ویندوز
(Segoe UI Emoji) نسبت به گلیف‌های اپل/اندروید/تلگرام قدیمی و کم‌کیفیت به‌نظر می‌رسه؛ کپی
مستقیم استیکرهای اختصاصی اپل/گوگل/تلگرام هم از نظر کپی‌رایت درست نیست.

راه‌حل: پکیج `twemoji-svg` (MIT + CC-BY-4.0، گرافیک استاندارد Twitter/X که دیسکورد/اسلک هم
استفاده می‌کنن) موقتاً نصب شد، فقط SVGهای ۱۳۴ ایموجی مجموعهٔ فاز ۲۹ (`sticker-set.ts`) با یک
اسکریپت یک‌بارمصرف به `public/twemoji/` کپی و نگاشت ایموجی→نام‌فایل در
`emoji-codepoints.generated.ts` تولید شد؛ خودِ پکیج `twemoji-svg` بعدش حذف شد (فقط منبع
تولید دارایی بود، در Runtime لازم نیست). کامپوننت جدید `EmojiImage.tsx` هر ایموجی رو به‌صورت
`<img>` از فایل محلی رندر می‌کنه؛ اگه ایموجی خارج از مجموعهٔ ۱۳۴تایی باشه (مثلاً پیست مستقیم
از کیبورد سیستم‌عامل) به‌صورت امن به رندر متنی معمولی برمی‌گرده. استفاده در پیکر ایموجی
(`MessageComposer`، هم آیکون تب‌ها هم گرید) و در نمایش استیکر بزرگ (`MessageBubble`).

## بدون تغییر

- منطق ارسال پیام معمولی (`ChatService#sendMessage`) دست‌نخورده موند.
- ساختار Read Receipt فاز ۲۹ دست‌نخورده موند؛ فقط پیام‌های ویرایش‌شده/حذف‌نشده در محاسباتش شرکت می‌کنن.
