# SPEC — فاز ۹: سفارش خرید (PO) — تب ۷

> پیش‌نیاز: فاز ۸ ✅ (سفارش مشتری). مرجع بصری: `POTab` در `mockups/inquiry-detail-mockup.jsx`.
> در اختیار **بازرگانی/مدیریت** (نه فروش). هر خرید باید از طریق یکی از **شرکت‌های گروه ما** صادر بشه.

## هدف فاز ۹

1. به‌ازای هر **تأمین‌کننده متمایز** بین اقلام سفارش مشتری (فاز ۸)، یک PO مستقل — با `our_entity` (شرکت خریدار گروه)، شماره PO، تاریخ صدور خودش
2. اقلام هر PO = زیرمجموعه اقلام سفارش مشتری که تأمین‌کننده‌شون همونه — هم‌رسانی خودکار، دقیقاً مثل الگوی فاز ۸
3. پرداخت به تأمین‌کننده (لیست منعطف، با `FlexPaymentList` فاز ۸ + فیلد اضافه «روش پرداخت»)
4. طبق تصمیم شما (هم‌سو با فاز ۶): هر PO **مستقل صادر/ذخیره می‌شه** — نه یک دکمه ترکیبی برای همه تأمین‌کننده‌ها با هم

## بک‌اند

مدل‌های Prisma جدید: `PurchaseOrder`, `PoItem`, `SupplierPayment` (باseline، Migration جدید لازم نیست).

### PoModule

| Endpoint | دسترسی | توضیح |
|---|---|---|
| `GET /inquiries/:id/purchase-orders` | `po.view` | آرایه‌ای از گروه‌های «هر تأمین‌کننده»: اگه PO ساخته نشده `{exists:false, previewItems, defaultOurEntityId}`؛ اگه ساخته شده، اقلام هم‌رسانی‌شده + خروجی کامل |
| `PUT /inquiries/:id/purchase-orders/:supplierId` | `po.create` | ساخت اولیه یا ویرایش هدر PO همون تأمین‌کننده (شماره/`our_entity`/تاریخ صدور/تحویل) |
| `POST /inquiries/:id/purchase-orders/:supplierId/payments` | `po.manage_payments` | افزودن ردیف پرداخت (ردیف خالی مجاز، مثل فاز ۸) |
| `PATCH /po-payments/:id` | `po.manage_payments` | ویرایش ردیف |
| `DELETE /po-payments/:id` | `po.manage_payments` | حذف ردیف |

**قوانین کسب‌وکاری:**
- پیش‌شرط: سفارش مشتری (فاز ۸) باید برای این پرونده ساخته شده باشه و حداقل یک قلمش این تأمین‌کننده رو داشته باشه — وگرنه ۴۰۰
- هم‌رسانی اقلام PO: دقیقاً الگوی فاز ۸ — اقلام سفارش مشتریِ این تأمین‌کننده که در PO نیستن اضافه می‌شن، اونایی که دیگه نیستن حذف می‌شن، ردیف‌های موجود دست‌نخورده می‌مونن
- `price`/`quantity` هر ردیف PO مستقیماً از `order_items` همون قلم کپی می‌شه (بدون واسطه — قیمت خرید همونیه که فاز ۸ قبلاً snapshot کرده)
- `total_amount` خودکار = `SUM(price×quantity)` اقلام فعلی PO
- `currency_code`: از ارز غالب آفرهای منتخب اقلام همین تأمین‌کننده مشتق می‌شه (همون قاعده فاز ۶)
- `our_entity_id` پیش‌فرض = همون شرکت گروهی که آخرین RFQ این پرونده به این تأمین‌کننده از طریقش ارسال شده؛ قابل تغییر دستی (قانون نرم، نه Constraint)
- `po_number` پیش‌فرض خودکار (`PO-{شماره سفارش}-{سه‌حرف اول نام تأمین‌کننده}`)، قابل بازنویسی دستی
- activity فقط روی ساخت اولیه هر PO (نه هر ویرایش/sync)

### دسترسی
از کلیدهای موجود کاتالوگ استفاده می‌شه: `po.view`، `po.create`، `po.manage_payments` (پیش‌فرض به بازرگانی/مدیریت).

## فرانت‌اند — تب ۷ (مطابق mockup)

- بنر «این بخش مربوط به بازرگانی/مدیریت است، هر PO باید از طریق شرکت‌های گروه صادر بشه»
- به‌ازای هر تأمین‌کننده یک کارت مستقل: نام/آیکون + مبلغ کل، انتخاب `our_entity` (پیش‌فرض پرشده)، شماره/تاریخ PO، لیست اقلام فقط‌خواندنی، `FlexPaymentList` («پرداخت به {تأمین‌کننده}»، ۳ وضعیت + فیلد روش پرداخت) و دکمه «صدور PO»/«ذخیره تغییرات» **مستقل همون کارت**
- `FlexPaymentList` با یک prop اختیاری جدید (`showPaymentMethod`) برای فیلد روش پرداخت گسترش پیدا می‌کنه — بدون شکستن استفاده فعلیش در تب ۶
- گیت: اگه سفارش مشتری هنوز ساخته نشده یا قلم برنده‌ای نداره → پیام راهنما به‌جای کارت‌ها
- گیت دسترسی مشابه تب ۶

## خارج از اسکوپ فاز ۹ (به‌صراحت)

- تب ۸ «حمل و گمرک» (تولید/بسته‌بندی/استعلام حمل/شیپینگ) — فاز بعدی
- Incoterm/نوع حمل روی PO — طبق طراحی اسکیما این‌ها دیگه در فرآیند جدای «استعلام حمل» تعیین می‌شن، نه سطح PO

## تست‌ها

- **Jest**: رد ساخت PO بدون سفارش مشتری/بدون قلم این تأمین‌کننده؛ هم‌رسانی اقلام (حفظ ردیف موجود)؛ `our_entity` پیش‌فرض از RFQ؛ `currency_code` غالب؛ CRUD پرداخت با ردیف خالی
- **E2E زنده**: سفارش با دو تأمین‌کننده مختلف → صدور PO مستقل هرکدوم → پرداخت به یکی → اصلاح نتیجه که قلم سومی به یکی از تأمین‌کننده‌ها اضافه کنه → بررسی sync خودکار بدون دست‌خوردن پرداخت ثبت‌شده → پاک‌سازی

## Definition of Done

- [ ] GET/PUT هر PO مستقل با هم‌رسانی خودکار اقلام + محاسبه total/currency
- [ ] CRUD پرداخت با ردیف خالی مجاز (طبق اصلاح فاز ۸)
- [ ] تب ۷ مطابق mockup (کارت مستقل هر تأمین‌کننده، دکمه صدور مستقل)
- [ ] Jest سبز + build/lint + E2E زنده + تست بصری
