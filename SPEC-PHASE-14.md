# SPEC — فاز ۱۴: اعلان‌ها، یادآورها (TODO) و پیام‌رسانی سراسری (دامنه ۹)

> پیش‌نیاز: فاز ۱۳ ✅. مرجع بصری: بخش اعلان‌ها در `mockups/app-shell-mockup.jsx`، ویجت TODO در `mockups/dashboard-mockup.jsx`، و `mockups/chat-messaging-mockup.jsx`.
> طبق تصمیم شما: **کل دامنه ۹** (اعلان‌ها + یادآورها + پیام‌رسانی) — بدون داشبورد کامل (KPI/گزارش، فاز جدا).

## هدف فاز ۱۴

1. **اعلان‌ها**: زنگولهٔ سراسری در Topbar با Dropdown اعلان‌های کاربر لاگین‌شده
2. **یادآورها (TODO شخصی)**: لیست کارهای خودم با افزودن/تیک‌زدن — در مسیر `/` (جایگزین Placeholder فعلی داشبورد، فقط همین ویجت، نه KPI ها)
3. **پیام‌رسانی سراسری**: مکالمهٔ دونفره/گروهی، جدا از گفتگوی هر پرونده (`inquiry_discussions`)

## ⚠️ تصمیمات و محدودیت‌های اسکوپ

1. **بدون WebSocket** — کل پروژه تا الان فقط REST + React Query بوده؛ اعلان‌ها/پیام‌ها با **Polling** (`refetchInterval` در React Query) به‌روزرسانی می‌شن، نه زیرساخت جدید Real-time.
2. **محل ویجت یادآورها**: طبق mockup داشبورد، این ویجت بخشی از یک داشبورد کامل‌تره؛ چون این فاز شامل KPI/گزارش نیست، فقط همین ویجت جایگزین Placeholder مسیر `/` می‌شه (بدون کارت‌های آماری).
3. **محدودهٔ واقعی اتصال اعلان‌ها به رویدادهای موجود** — طبق مثال‌های خود کامنت schema (`type`: mention, letter_referral, approval_request, rfq_overdue, reminder_due)، فقط ۴ نقطهٔ واقعی و از‌قبل‌موجود سیم‌کشی می‌شن (بدون دست‌کاری گسترده در ماژول‌های قبلی):
   - **mention**: وقتی در گفتگوی پرونده (`inquiry_discussions`, فاز ۳) کسی Mention می‌شه
   - **letter_referral**: وقتی نامه ارجاع داده می‌شه (`CorrespondenceService.refer`, فاز ۱۳)
   - **rfq_overdue**: همون Job شبانهٔ موجود (`RfqsScheduler`, فاز ۴) که وضعیت RFQ رو به «بدون پاسخ» تغییر می‌ده
   - **reminder_due**: یک Cron جدید و کوچیک، هر ۱۵ دقیقه یادآورهای سررسیدشدهٔ بی‌اعلان رو پیدا و اعلان می‌ده
   - **approval_request** در schema هست ولی هیچ گردش‌کار تأیید واقعی‌ای در پروژه وجود نداره تا بهش وصل بشه — این تایپ در این فاز صادر نمی‌شه.
4. **اعلان‌های «قابل‌اقدام» (دکمه تأیید/رد)**: mockup یک نمونهٔ «تأیید تخفیف» نشون می‌ده که به هیچ گردش‌کار واقعی پروژه وصل نیست (چنین چیزی هیچ‌وقت ساخته نشده). فیلدهای `actions`/`is_actioned`/`actioned_at` در دیتابیس و یک endpoint عمومی «ثبت اقدام» پیاده می‌شن (برای استفادهٔ آینده)، ولی هیچ‌کدوم از ۴ محرک بالا فعلاً دکمهٔ اقدام واقعی تولید نمی‌کنن (`actions: null`).
5. **یادآور فقط برای خودم در این فاز** — طبق UI mockup (فقط «یادآور جدید برای خودم»)؛ بک‌اند از `ownerUserId` اختیاری پشتیبانی می‌کنه (برای گسترش بعدی «مدیر برای زیردست یادآور بذاره») ولی UI این فاز فقط self-create داره.
6. **بدون پیوست فایل در چت** — schema فیلد `file_url` داره ولی mockup دکمهٔ پیوست نداره؛ خارج از اسکوپ.
7. **رفع یک Gap در mockup چت**: هیچ دکمهٔ «پیام مستقیم جدید» وجود نداره (فقط «گروه جدید»)؛ یک دکمهٔ کوچیک برای شروع مکالمهٔ دونفره با یکی از همکاران اضافه می‌شه (والا امکان استفادهٔ واقعی از قابلیت نیست).
8. **Migration جدید کوچیک**: `chat_participants` برای محاسبهٔ واقعی «تعداد نخوانده» به یک ستون `last_read_at` نیاز داره که در schema فعلی نیست — طبق الگوی افزودن جداول جدید در فازهای قبل (rfq_counters و...)، یک migration افزایشی (نه تغییر جدول موجود، فقط افزودن ستون Nullable) اضافه می‌شه.
9. **بدون دسترسی RBAC جدید** — اعلان‌ها/یادآورها/چت مربوط به خود کاربره (نه دادهٔ سازمانی محرمانه)؛ مثل الگوی `users/colleagues`، endpoint ها برای هر کاربر لاگین‌شده باز می‌مونن، بدون کلید دسترسی جدید.

## بک‌اند

مدل‌های Prisma جدید: `Notification`, `Reminder`, `ChatConversation`, `ChatParticipant`, `ChatMessage` (baseline). Migration جدید: `chat_participants.last_read_at`.

### NotificationsService (Cross-Cutting، مثل ActivityLogService)
سرویس داخلی `create()` که از ماژول‌های دیگه (Inquiries mention، Correspondence refer، Rfqs cron، Reminders cron) صدا زده می‌شه.

| Endpoint | توضیح |
|---|---|
| `GET /notifications` | ۵۰ اعلان اخیر کاربر لاگین‌شده |
| `POST /notifications/:id/read` | علامت خوانده‌شده |
| `POST /notifications/mark-all-read` | همه رو خوانده کن |
| `POST /notifications/:id/action` | ثبت اقدام عمومی (`is_actioned=true`) |

### RemindersModule

| Endpoint | توضیح |
|---|---|
| `GET /reminders` | یادآورهای من (owner = کاربر لاگین‌شده)، مرتب بر اساس سررسید |
| `POST /reminders` | افزودن (پیش‌فرض owner=خودم) |
| `PATCH /reminders/:id` | تغییر وضعیت (تیک زدن/برگردوندن) |
| Cron `RemindersScheduler` | هر ۱۵ دقیقه: یادآورهای سررسیدشدهٔ بدون اعلان قبلی → اعلان `reminder_due` |

### ChatModule

| Endpoint | توضیح |
|---|---|
| `GET /chat/conversations` | مکالمه‌های من + آخرین پیام + تعداد نخوانده (`last_read_at`) |
| `POST /chat/conversations` | ساخت مستقیم (idempotent — اگه بین همین دو نفر از قبل هست، همون برگردونده می‌شه) یا گروهی |
| `GET /chat/conversations/:id/messages` | پیام‌های یک مکالمه |
| `POST /chat/conversations/:id/messages` | ارسال پیام متنی |
| `POST /chat/conversations/:id/read` | به‌روزرسانی `last_read_at` |

## فرانت‌اند

- **NotificationBell**: کامپوننت در Topbar (کنار آواتار کاربر)، Polling هر ۳۰ ثانیه، Dropdown با لیست + دکمه «همه رو خوانده کن»
- **مسیر `/`**: جایگزین Placeholder → فقط ویجت «یادآورهای من» (چک‌باکس + افزودن سریع)
- **`/chat`**: صفحهٔ دوستونه (لیست مکالمه‌ها + پنجرهٔ گفتگو) طبق mockup، با دکمهٔ اضافه‌شدهٔ «پیام مستقیم جدید» + «گروه جدید»؛ Polling پیام‌های مکالمهٔ باز هر ۵ ثانیه

## خارج از اسکوپ فاز ۱۴ (به‌صراحت)

- داشبورد کامل (KPI ها، نمودارها، نرخ ارز/طلا) — فاز جدا
- پیوست فایل در چت
- گردش‌کار تأیید/رد واقعی روی اعلان‌های قابل‌اقدام
- افزودن/حذف عضو گروه بعد از ساخت، یا ترک گروه

## تست‌ها

- **Jest**: عدم تکرار اعلان `reminder_due` برای یک یادآور در چند اجرای Cron؛ idempotent بودن ساخت مکالمهٔ مستقیم تکراری؛ محاسبهٔ تعداد نخوانده بر اساس `last_read_at`
- **E2E زنده**: Mention در گفتگوی یک پرونده → اعلان دریافت می‌شه؛ ارجاع نامه → اعلان دریافت می‌شه؛ ساخت یادآور با سررسید گذشته + اجرای دستی Cron → اعلان `reminder_due`؛ ساخت مکالمهٔ مستقیم دوباره با همون نفر → همون مکالمهٔ قبلی برمی‌گرده؛ ارسال پیام + خوانده‌شدن → شمارندهٔ نخوانده صفر می‌شه

## Definition of Done

- [ ] Migration `chat_participants.last_read_at` + مدل‌های Prisma دامنه ۹
- [ ] NotificationsService + ۳ نقطهٔ اتصال واقعی + Cron یادآور
- [ ] RemindersModule کامل
- [ ] ChatModule کامل با شمارندهٔ نخوانده صحیح
- [ ] NotificationBell در Topbar + صفحهٔ `/` (ویجت یادآور) + صفحهٔ `/chat`
- [ ] Jest سبز + build/lint + E2E زنده + تست بصری + README
