# SPEC — فاز ۲۴: بازطراحی مکاتبات و بایگانی اسناد (دامنه ۸)

> سومین زیرفاز از بستهٔ اصلاحات فراگیر قبل از دامنهٔ خزانه‌داری (بعد از فاز ۲۲ و ۲۳).
> برخلاف فازهای ۲۲/۲۳، این فاز نیاز به Migration واقعی داره (اولین تغییر شِمای مکاتبات از زمان ساخت اولیهٔ ماژول در «فاز ۱۳»، که کلاً داخل `0001_baseline` بیکار شده بود).

## هدف

بازطراحی فرم ثبت نامه بر مبنای **جهت نامه** (دریافتی/ارسالی/داخلی) — چون فیلدهای هرکدوم واقعاً متفاوتن، نه یک فرم یکسان با گزینه‌های اضافه:

1. جهت نامه اول پرسیده می‌شه؛ بقیهٔ فرم بر اساسش تغییر می‌کنه
2. نامهٔ دریافتی: فرستنده حتماً بیرونیه، گیرنده حتماً یکی از شرکت‌های ماست (بدون Toggle انتخاب) + شمارهٔ نامهٔ فرستنده + مسئول پیگیری (جایگزین فیلد واحد)
3. نامهٔ ارسالی: فرستنده حتماً یکی از شرکت‌های ماست، گیرنده حتماً بیرونیه (بدون Toggle) + امضاکننده(های) نامه
4. نامهٔ داخلی: فرستنده/گیرنده دیگه شرکت نیستن — بخش سازمانی HR واقعی به بخش سازمانی دیگه
5. بارگذاری تصویر نامه اجباری در مرحلهٔ «ثبت رسمی» (نه در پیش‌نویس)

## تصمیمات طراحی (بر اساس پاسخ‌های تأییدشده)

### ۱. حذف Toggle «یکی از شرکت‌های ما / طرف بیرونی»

- `PartySelector` یک Prop اختیاری جدید می‌گیره: `fixedKind?: "our_entity" | "partner"` — وقتی ست بشه، دکمه‌های سوییچ حذف می‌شن و فقط همون یک حالت (Select شرکت‌های گروه یا جستجوی طرف بیرونی) نمایش داده می‌شه. کامپوننت فعلی دست‌نخورده می‌مونه، فقط این حالت جدید بهش اضافه می‌شه
- در فرم ثبت نامه: `type=incoming` → فرستنده با `fixedKind="partner"`، گیرنده با `fixedKind="our_entity"`؛ `type=outgoing` → برعکس؛ `type=internal` → اصلاً `PartySelector` رندر نمی‌شه (به بخش ۴ نگاه کن)
- ⚠️ این محدودیت الان فقط UI بود؛ سرویس بک‌اند هم به‌روزرسانی می‌شه تا **بر اساس `type`** فیلدهای فرستنده/گیرنده رو Validate کنه (نه فقط «دقیقاً یکی پر باشه» فعلی، بلکه «کدوم یکی، بسته به جهت نامه»)

### ۲. نامهٔ دریافتی — مسئول پیگیری + شمارهٔ نامهٔ فرستنده

- ⚠️ ستون جدید `letters.responsible_user_id` (نه استخراج از `letter_workflow_logs`، چون هم برای فیلتر دسترسی لازمه هم برای نمایش سریع — استخراج از لاگ کند و شکننده‌ست)
- **جایگزین فیلد `department`** فقط برای نامه‌های دریافتی: هنگام ثبت نامهٔ دریافتی، `department` دیگه نمایش/دریافت نمی‌شه؛ به‌جاش یک Select اجباری «مسئول پیگیری» از **همهٔ کاربران سیستم** (همون Endpoint موجود `GET /users/colleagues` که برای Mention در گفتگوی پرونده استفاده می‌شه — بدون فیلتر دپارتمان)
- ستون جدید `letters.sender_reference_number` (اجباری فقط برای دریافتی) — شمارهٔ نامه‌ای که طرف فرستنده روی نامه‌اش گذاشته
- بعد از ثبت، اگه `responsible_user_id` با ثبت‌کننده فرق داشته باشه: یک اعلان (`notifications.create`, type=`letter_responsible_assigned`) + یک ردیف `letter_workflow_logs` (`action='referred'`, `referred_to_user_id`, یادداشت خودکار) — دقیقاً هم‌الگوی مکانیزم `refer()` موجود، فقط برای این مسیر خودکار صدا زده می‌شه
- **فیلتر دسترسی** (`assertVisible`/`list`) یک شرط OR جدید می‌گیره: `letter.responsibleUserId === کاربر جاری` — چون `department` دیگه برای این نوع نامه پر نمی‌شه، این جایگزین دقیق همون منطق قبلیه

### ۳. نامهٔ ارسالی — امضاکننده(ها)

- جدول جدید `letter_signers` (رابطهٔ چندبه‌چند نامه↔کاربر) — چون می‌تونه چند نفر هم‌زمان امضاکننده باشن
- در فرم: چک‌باکس چندانتخابی از همون لیست کاربران (`GET /users/colleagues`)، حداقل یک نفر اجباری برای `type=outgoing`
- فیلد `department` برای نامهٔ ارسالی **دست‌نخورده می‌مونه** (این تصمیم فقط مخصوص دریافتی بود)

### ۴. نامهٔ داخلی — بخش به بخش (نه شرکت به شرکت)

- ⚠️ طبق پاسخ تأییدشده: به جدول واقعی `departments` (دامنهٔ ۱۱، فاز ۱۸) وصل می‌شه، نه لیست قدیمی هاردکد
- دو ستون جدید: `letters.internal_from_department_id` و `letters.internal_to_department_id`، هردو `REFERENCES departments(id)`؛ باید متفاوت باشن («از یک واحد **به یک واحد دیگر**»)
- در فرم: هیچ `PartySelector`ی رندر نمی‌شه؛ به‌جاش دو `Select` از بخش‌های سازمانی (فیلترشده بر اساس `our_entity` انتخاب‌شده در `issuingEntityId`، با استفاده از Endpoint موجود دپارتمان‌های HR)
- ⚠️ **فیلد قدیمی `department` (رشتهٔ آزاد، برای کنترل دسترسی `view_own_department`) برای نامهٔ داخلی همچنان نگه داشته می‌شه و مستقل از دو ستون جدیده** — این دو مفهوم متفاوتن (کنترل دسترسی در برابر هویت واقعی فرستنده/گیرنده) و ادغامشون خطر رگرسیون در فیلتر دسترسی فعلی داره؛ در سطح UI هم جدا از هم نمایش داده می‌شن

### ۵. الزام بارگذاری تصویر نامه

- ⚠️ طبق پاسخ تأییدشده: فقط در مرحلهٔ «ثبت رسمی» (`register()`) اجباریه، نه در ذخیرهٔ پیش‌نویس
- `register()` قبل از تولید شماره، چک می‌کنه که حداقل یک ردیف در `documents` با `related_letter_id` همین نامه وجود داشته باشه؛ اگه نه، خطای ۴۰۰ («برای ثبت رسمی نامه، حداقل یک تصویر/سند باید بارگذاری شده باشه»)
- ⚠️ برای اینکه این مسیر عملاً قابل استفاده باشه، یک قدم آپلود فایل (اختیاری، نه اجباری) به‌خودِ فرم «نامهٔ جدید» هم اضافه می‌شه (الان فقط بعد از ساخت نامه، از صفحهٔ جزئیات ممکنه) — کاربر می‌تونه هم‌زمان با ثبت پیش‌نویس فایل رو انتخاب کنه؛ بعد از ساخت موفق نامه، آپلود و اتصال سند به‌صورت خودکار (پشت‌سرهم، نه Atomic با خودِ Create) انجام می‌شه

## اسکیمای دیتابیس (Migration جدید `0014_correspondence_redesign`)

```sql
ALTER TABLE letters
    ADD COLUMN responsible_user_id          UUID REFERENCES users(id),
    ADD COLUMN sender_reference_number      VARCHAR(100),
    ADD COLUMN internal_from_department_id  UUID REFERENCES departments(id),
    ADD COLUMN internal_to_department_id    UUID REFERENCES departments(id);

CREATE TABLE letter_signers (
    letter_id  UUID NOT NULL REFERENCES letters(id) ON DELETE CASCADE,
    user_id    UUID NOT NULL REFERENCES users(id),
    PRIMARY KEY (letter_id, user_id)
);
```

Rollback: `DROP TABLE letter_signers;` + `ALTER TABLE letters DROP COLUMN responsible_user_id, DROP COLUMN sender_reference_number, DROP COLUMN internal_from_department_id, DROP COLUMN internal_to_department_id;`

## تغییرات بک‌اند

- `CreateLetterDto`: افزودن `senderReferenceNumber?`, `responsibleUserId?`, `signerUserIds?: string[]`, `internalFromDepartmentId?`, `internalToDepartmentId?` — همه اختیاری در سطح DTO (چون اجباری‌بودنشون به `type` بستگی داره)، Validate واقعی در سرویس
- `correspondence.service.ts`:
  - `assertPartyValid` جایگزین می‌شه با منطق آگاه از `type`: `incoming` → فرستنده باید `partnerId` داشته باشه/گیرنده `ourEntityId`؛ `outgoing` → برعکس؛ `internal` → هیچ‌کدوم از فیلدهای فرستنده/گیرنده شرکتی پذیرفته نمی‌شه، به‌جاش دو فیلد دپارتمان اجباری و متفاوت
  - `create()`: برای `incoming` علاوه بر ولیدیشن، `department` رو نادیده می‌گیره (همیشه `null`)؛ اگه `responsibleUserId` با `userId` سازنده فرق داشت، نوتیفیکیشن + لاگ گردش کار خودکار بزنه؛ برای `outgoing` ردیف‌های `letter_signers` رو داخل همون Transaction بسازه
  - `register()`: گارد جدید چک وجود حداقل یک `documents` مرتبط
  - `assertVisible`/`list()`: شرط OR جدید `responsibleUserId === userId`

## Frontend

- `PartySelector.tsx`: افزودن Prop `fixedKind`
- `NewLetterForm` (`CorrespondenceListPage.tsx`): انتخاب `type` به بالای فرم منتقل می‌شه؛ بقیهٔ فیلدها به‌صورت شرطی رندر می‌شن (سه شاخهٔ کاملاً جدا برای دریافتی/ارسالی/داخلی)؛ افزودن قدم آپلود فایل اختیاری
- صفحهٔ جزئیات: نمایش «مسئول پیگیری»/«شمارهٔ نامهٔ فرستنده» برای دریافتی، «امضاکنندگان» برای ارسالی، «از بخش/به بخش» برای داخلی؛ دکمهٔ «ثبت رسمی» پیام خطای بک‌اند (نبود سند) رو نشون می‌ده

## خارج از محدودهٔ این فاز

- تغییر مکانیزم فعلی `refer()` (ارجاع دستی نامه به یک همکار در هر مرحله) — دست‌نخورده و مستقل از `responsible_user_id` باقی می‌مونه
- تغییر فیلد `department` برای نامه‌های ارسالی/داخلی یا مهاجرت کل مکانیزم کنترل دسترسی به دپارتمان‌های واقعی HR
- تولید خودکار PDF/Word نامه — همچنان فقط آپلود دستی تصویر/اسکن

## معیار پذیرش

- انتخاب نوع نامه در بالای فرم، سه فرم کاملاً متفاوت (فیلد فرستنده/گیرنده متفاوت) رو نشون می‌ده
- ثبت نامهٔ دریافتی بدون انتخاب مسئول پیگیری یا بدون شمارهٔ نامهٔ فرستنده رد می‌شه؛ فرستنده اجباراً بیرونی، گیرنده اجباراً یکی از شرکت‌های ماست
- ثبت نامهٔ ارسالی بدون حداقل یک امضاکننده رد می‌شه؛ فرستنده اجباراً یکی از شرکت‌های ماست، گیرنده اجباراً بیرونیه
- ثبت نامهٔ داخلی بدون بخش مبدأ/مقصد متفاوت رد می‌شه؛ هیچ فیلد شرکتی (فرستنده/گیرنده) پذیرفته نمی‌شه
- «ثبت رسمی» روی نامه‌ای بدون سند پیوست‌شده رد می‌شه با پیام خطای واضح؛ بعد از آپلود حداقل یک سند، ثبت رسمی موفق می‌شه
- کاربری که `responsible_user_id` یک نامهٔ دریافتیه (بدون داشتن `view_all`) می‌تونه اون نامه رو ببینه، حتی بدون تطابق دپارتمان
- تست‌های Jest برای: ولیدیشن آگاه از `type` در `create()`، گارد سند در `register()`، فیلتر دسترسی جدید بر مبنای `responsibleUserId`
- Migration `0014_correspondence_redesign` دیپلوی می‌شه + مدل‌های Prisma به‌روز می‌شن
- Build + Lint هر دو اپ بدون خطا؛ E2E زنده روی DB واقعی (هر سه نوع نامه) + تأیید بصری + به‌روزرسانی `README.md`
