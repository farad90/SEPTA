# SPEC — فاز ۲۶: اصلاح تاگل کشور شرکت‌ها + ایمیل دوزبانه استعلام حمل

## هدف

دو اصلاح مستقل و بدون ارتباط به هم:

1. رفع باگ عدم هماهنگی تاگل «شرکت خارجی» با فیلد کشور در فرم شرکت‌ها (`apps/web/src/pages/partners/PartnersPage.tsx`)
2. تبدیل ایمیل استعلام حمل (`apps/api/src/freight/freight-rfq-email.builder.ts`) از فقط‌انگلیسی به دوزبانه (فارسی + انگلیسی)

## وضعیت فعلی (خلاصه اکتشاف)

### بخش ۱ — باگ تاگل کشور

- تاگل `isForeign` و فیلد `country` دو منبع مستقل‌ان که با هم هماهنگ نمی‌مونن: بعد از کلیک اولیه روی چک‌باکس، کاربر می‌تونه از دراپ‌داون هر کشوری رو (از جمله ایران در حالت خارجی، یا آلمان در حالت داخلی) انتخاب کنه — هیچ قفلی جلوش رو نمی‌گیره.
- نتیجه: همون کشور «ایران» ممکنه یک‌بار به فارسی (`"ایران"`) و یک‌بار به انگلیسی (`"Iran"`) ذخیره بشه، که هم نمایش رو ناهماهنگ می‌کنه هم جست‌وجوی متنی (`contains`) رو می‌شکنه.
- طبق کامنت صریح `erp-schema.sql` (بالای `province`)، **هر ۷ فیلد** `province`/`city`/`postal_code`/`phone`/`email`/`national_id`/`registration_number` مخصوص شرکت‌های داخلی‌ان — ولی فرم فعلی فقط ۳ تای اول (استان/شهر/کدپستی) رو شرطی نمایش می‌ده؛ تلفن/ایمیل/شناسه ملی/شماره ثبت همیشه نمایش داده می‌شن، چه شرکت داخلی باشه چه خارجی.

### بخش ۲ — ایمیل استعلام حمل

- `freight-rfq-email.builder.ts` عمداً و طبق طراحی قبلی (`erp-database-design.md:216`) کاملاً انگلیسی تولید می‌شه. این فاز اون تصمیم رو عوض می‌کنه، نه یه باگ رو رفع می‌کنه.
- تنها الگوی مشابه موجود (`apps/api/src/rfqs/rfq-email.builder.ts`, ایمیل استعلام کالا از تأمین‌کننده، فاز ۴) هم انگلیسیه — طبق تصمیم کاربر، **دست‌نخورده می‌مونه**، این فاز فقط ایمیل استعلام حمل رو عوض می‌کنه.

## تصمیمات طراحی

### ۱. رفع باگ تاگل — فقط فرانت‌اند (تصمیم کاربر)

⚠️ بدون تغییر DTO/سرویس بک‌اند یا اسکریپت اصلاح داده‌های قدیمی — فقط `PartnersPage.tsx` اصلاح می‌شه تا از این پس داده جدید همیشه سازگار ثبت بشه:

- **حالت داخلی** (`isForeign=false`): فیلد کشور دیگه یک `<Select>` نیست — یه نمایش ثابت/غیرقابل‌ویرایش «ایران» (مثلاً `<Select disabled>` تک‌گزینه‌ای یا یک `ViewField`‌مانند با پس‌زمینه خاکستری) چون تنها گزینه ممکنه. مقدار `form.country` همیشه دقیقاً `"ایران"` می‌مونه، بدون امکان تغییر دستی.
- **حالت خارجی** (`isForeign=true`): `<Select>` معمولی می‌مونه ولی گزینهٔ ایران (`{fa:"ایران", en:"Iran"}`) از لیست `COUNTRIES` حذف می‌شه — یه شرکت «خارجی» دیگه هرگز نمی‌تونه کشورش ایران ثبت بشه. چون این حالت تنها جاییه که Select واقعی رندر می‌شه، نیازی به شرط‌گذاری fa/en روی خود گزینه‌ها نیست — همیشه انگلیسی نمایش داده می‌شه (`dir="ltr"`).
- **گسترش فیلدهای مخصوص داخلی از ۳ به هر ۷ فیلد**: `province`/`city`/`postalCode`/`phone`/`email`/`nationalId`/`registrationNumber` همه با یک شرط واحد (`!form.isForeign`) نمایش/مخفی می‌شن، هم در فرم ایجاد/ویرایش هم در نمای فقط‌خواندنی جزئیات. `toBody()` هم همین ۷ فیلد رو برای شرکت خارجی `undefined` می‌فرسته (نه فقط ۳ تا مثل الان).
- داده‌های قدیمی احتمالاً ناسازگار (اگه از قبل موردی مثل `isForeign=false` با `country="آلمان"` در دیتابیس باشه) در این فاز اصلاح نمی‌شن — فقط جلوی ثبت جدید ناسازگار گرفته می‌شه.

### ۲. ایمیل دوزبانه استعلام حمل

⚠️ تصمیم کاربر: **یک ایمیل واحد، بلوک فارسی سپس بلوک انگلیسی** (نه دو ایمیل جدا، نه فقط بخشی از متن ترجمه بشه) — چون مخاطب استعلام حمل (`freight_forwarder`) می‌تونه داخلی یا خارجی باشه و از قبل زبان مشخصی براش ثبت نشده.

- `buildFreightRfqEmailHtml()`: محتوای فعلی انگلیسی (سلام/درخواست بهترین پیشنهاد/جدول بسته‌ها/گمرک مقصد/امضا) دست‌نخورده به‌عنوان بلوک دوم می‌مونه؛ یک بلوک فارسی معادل (همون اطلاعات: سلام + درخواست ارائهٔ بهترین پیشنهاد قیمت/زمان حمل با مرجع شماره استعلام + جدول بسته‌ها با هدرهای فارسی + گمرک مقصد + امضا با نام کارشناس) قبلش اضافه می‌شه، با یک `<hr/>` جداکننده بین دو بلوک. بلوک فارسی `dir="rtl"`، بلوک انگلیسی `dir="ltr"` (مثل قبل).
- `buildFreightRfqEmailText()` (نسخه متنی fallback برای وقتی SMTP تنظیم نشده) هم همین ساختار دوبلوکی رو با یک خط جداکنندهٔ متنی (`------------------------------`) می‌گیره.
- `freight_rfqs.email_subject` **دست‌نخورده** می‌مونه — همچنان متن آزادیه که کارشناس بازرگانی وارد می‌کنه (یا پیش‌فرض `rfqNumber`)؛ این فاز موضوع رو دوزبانه نمی‌کنه، فقط بدنهٔ ایمیل رو.
- `escapeHtml()`/`dims()` (کمکی‌های موجود) بدون تغییر، هم برای بلوک فارسی هم انگلیسی استفاده می‌شن (فقط برچسب‌های ستون جدول برای بلوک فارسی جدا تعریف می‌شن).
- ایمیل استعلام کالا از تأمین‌کننده (`apps/api/src/rfqs/rfq-email.builder.ts`, فاز ۴) طبق تصمیم کاربر **خارج از محدودهٔ این فاز** — دست‌نخورده، فقط انگلیسی می‌مونه.

## خارج از محدودهٔ این فاز

- هرگونه تغییر DTO/Service بک‌اند یا Migration برای شرکت‌ها (فقط فرانت‌اند)
- اصلاح داده‌های قدیمی ناسازگار کشور/isForeign که از قبل در دیتابیس ثبت شدن
- دوزبانه‌سازی ایمیل استعلام کالا از تأمین‌کننده (`rfq-email.builder.ts`)
- تغییر `freight_rfqs.email_subject` به فرمت دوزبانه

## معیار پذیرش

- در فرم ثبت/ویرایش شرکت: حالت داخلی، کشور به‌صورت ثابت «ایران» نمایش داده می‌شه و قابل تغییر نیست؛ حالت خارجی، دراپ‌داون کشور گزینهٔ ایران رو نداره
- هر ۷ فیلد اختصاصی داخلی (استان/شهر/کدپستی/تلفن/ایمیل/شناسه ملی/شماره ثبت) هم در فرم هم در نمای جزئیات فقط برای شرکت داخلی نمایش داده می‌شه
- پیش‌نمایش/ارسال ایمیل استعلام حمل شامل دو بلوک کامل (فارسی سپس انگلیسی، هرکدوم با جهت متن صحیح) با محتوای معادل است؛ نسخهٔ متنی fallback هم همین ساختار رو داره
- Build + Lint هر دو اپ بدون خطا؛ تست‌های Jest موجود برای freight (اگه `email builder` تست مستقیم داره) به‌روزرسانی می‌شن؛ E2E زنده (ثبت یک شرکت داخلی و یک خارجی، پیش‌نمایش ایمیل استعلام حمل)؛ تأیید بصری در مرورگر
- به‌روزرسانی `README.md`
