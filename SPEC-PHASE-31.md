# فاز ۳۱ — مرکز اقدامات (Action Center) — تحلیل معماری + پیاده‌سازی

> ✅ کاربر این تحلیل رو با «تاییده» (شامل پذیرش صریح توصیه‌های من روی هر سه سؤال باز پایین سند)
> تأیید کرد. نسخهٔ MVP دقیقاً طبق «مرحلهٔ ۷ — جمع‌بندی معمار» پیاده‌سازی شد — جزئیات واقعی پیاده‌سازی
> (نام فایل‌ها، تصمیمات ریز) در بخش «ضمیمهٔ پیاده‌سازی» انتهای سند.

## پیش از هر چیز: چه چیزی از قبل در سپتا وجود داره؟

قبل از طراحی هر Entity جدید، ساختار فعلی سیستم بررسی شد (کد واقعی، نه فقط اسناد — چون `erp-schema.sql` برای دامنه‌های ۱۱ به بعد به‌روز نیست). نتیجه: **بخش قابل‌توجهی از چیزی که درخواست شده از قبل وجود داره، ولی پراکنده و کم‌دیده‌ست.**

| سیستم موجود | کجا | چی داره | محدودیت فعلی |
|---|---|---|---|
| **`activities`** (فاز ۱۶/۱۷) | `apps/api/src/activities/` | `activityType` (call/email/meeting/follow_up/reminder/**approval**/internal_task)، `status` (open/scheduled/waiting/overdue/completed/cancelled)، `priority` (low/normal/high/**urgent**)، `assignedToUserId`/`createdByUserId`، `dueAt`/`scheduledAt`/`completedAt`، `relatedEntityType`/`relatedEntityId` (عمومی در دیتابیس)، `outcomeId`+`outcomeNote`، `followUpOfActivityId` (زنجیرهٔ پیگیری خودکار)، Cron هر ۱۵ دقیقه برای Overdue | فقط UI آن یک **ویجت کوچک در داشبورد** («فعالیت‌های من») + یک تب داخل پروندهٔ Inquiry است — هیچ صفحهٔ سراسری در سایدبار ندارد. سرویس فقط `relatedEntityType='inquiry'` را می‌پذیرد (بقیهٔ ماژول‌ها را رد می‌کند) با اینکه ستون دیتابیس عمومی است. دسترسی «دیدن کارهای تیم» فقط با یک شرط سخت‌کدشده (`groupName === 'مدیریت'`) است، نه یک کلید RBAC واقعی. `activityType='approval'` در enum تعریف شده ولی **هیچ‌کجای سیستم امروز واقعاً این نوع را نمی‌سازد** — یک Gap واقعی.
| **`notifications`** (فاز ۱۴/۲۷) | `apps/api/src/notifications/` | جریان اعلان Cross-cutting؛ `relatedEntityType/Id` عمومی؛ `actions` (JSONB دکمه‌های تأیید/رد — فقط یک استفادهٔ واقعی: درخواست اصلاح محموله) | یک صف اعلان ساده است، نه یک سیستم Task با چرخهٔ عمر/Deadline. کاملاً از `activities` جداست — دو سیستم موازی که با هم صحبت نمی‌کنن (فقط Cron مربوط به Overdue فعالیت‌ها یک Notification هم می‌سازه).
| **`TeamApprovalsWidget`** (فاز ۱۹/۲۰) | `apps/web/src/pages/dashboard/` | یک ویجت داشبورد که **۵ منبع تأیید HR کاملاً جدا** (`leave_requests`، `employee_loans`، `mission_requests`، `overtime_records`، `hr_requests`) را با ۵ Hook/Type مجزا در یک UI مشترک نشان می‌دهد | این دقیقاً همان مشکلیه که «مرکز اقدامات» باید حلش کنه: تأییدها الان در ۵ جدول کاملاً مجزا با منطق تأیید کاملاً متفاوت (مثلاً تأیید وام → تولید خودکار جدول اقساط) پخش شدن، و فقط در سطح UI با کد دستی کنار هم چیده شدن.
| **`shipment_edit_requests`** (فاز ۲۷) | دامنه ۶ | یک الگوی «درخواست → تأیید/رد از *داخل* اعلان» (`notifications.actions`) — تنها استفادهٔ واقعی JSONB actions | فقط برای محموله‌ها؛ جدول جدا، بدون هیچ ارتباطی به `activities`.
| **`letters.responsible_user_id`** (فاز ۲۴) | دامنه ۸ | ارجاع نامهٔ دریافتی به یک کارشناس مشخص + لاگ `letter_workflow_logs` + اعلان خودکار | فقط از صفحهٔ مکاتبات قابل پیگیریه؛ هیچ‌جای دیگه‌ای «نامه‌های ارجاع‌شده به من» را جمع نمی‌بینی.
| وضعیت‌های «منتظر» ضمنی | همه‌جا | مثلاً `supplier_rfqs.status='awaiting_response'`, `freight_rfqs.status='awaiting_response'`, `inquiries.selection_locked_at IS NULL`, `deliveries.customer_acceptance_status='pending'` | هیچ‌کدوم رکورد Action واقعی ندارن؛ فقط با گشتن دستی در لیست هر ماژول قابل کشفن.

**نتیجهٔ این بررسی، مبنای کل پیشنهاد پایینه:** سپتا به یک Entity کاملاً تازه برای «کار» نیاز ندارد — `activities` همین الان ستون‌ستون همان چیزیه که خواسته شده (assignedTo، dueAt، priority، status، outcome، follow-up chaining، relatedEntity عمومی، Overdue Cron). چیزی که واقعاً غایبه یک **رابط سراسری + یک لایهٔ یکپارچه‌سازی** روی بالای `activities` + سیستم‌های تأیید پراکندهٔ موجودست، نه یک جدول موازی جدید.

---

## معماری پیشنهادی: الگوی «صندوق ورودی فدرال» (Federated Inbox)

دقیقاً همان الگویی که SAP Fiori **My Inbox** و Oracle **BPM Worklist** استفاده می‌کنن: این ماژول‌ها خودشون صاحب دادهٔ Workflowهای مختلف نیستن — فقط از سیستم‌های تخصصی مختلف (که هرکدوم منطق کسب‌وکاری خودشونو دارن) Query می‌زنن و در یک رابط یکپارچه با دکمهٔ اقدام مستقیم (Deep-link به همون رکورد اصلی) نشون می‌دن.

**چرا این الگو، نه «مهاجرت همه‌چیز به یک جدول Action واحد»؟**
- تأیید وام باعث تولید خودکار جدول اقساط می‌شه؛ تأیید درخواست اصلاح محموله باعث باز شدن قفل مرحله می‌شه؛ تأیید مرخصی باعث Upsert در `attendance_records` می‌شه. این منطق‌های بسیار خاص هر کدوم را نمی‌شه بدون از دست دادن دقت به یک جدول عمومی «Action» تقلیل داد.
- مهاجرت دادهٔ حیاتی مالی/HR/محموله به یک جدول جدید ریسک بالا و بدون ارزش واقعی داره — دادهٔ فعلی درست کار می‌کنه.
- این دقیقاً همون قاعده‌ای‌ست که خودِ سند طراحی سپتا (`erp-database-design.md`) قبلاً برای پیش‌بینی جریان نقدی هم به کار برده: «پیش‌بینی مطالبات جدول جدا نمی‌خواد — از `customer_payments`/`supplier_payments` موجود Query می‌شه». همون منطق اینجا هم صادقه.

پس دو لایه:

**لایهٔ ۱ — هستهٔ Action (دادهٔ واقعاً صاحب‌دار):** گسترش `activities` موجود (نه جایگزینی) برای Task شخصی/واگذارشده، Follow-up، Reminder، و Approvalهای *جدید* (آنهایی که امروز اصلاً جایی ثبت نمی‌شن).

**لایهٔ ۲ — تجمیع‌گر Read-Only:** یک سرویس جدید (`ActionCenterService`) که در لحظه از `activities` + ۵ منبع تأیید HR + `shipment_edit_requests` + ارجاعات نامه Query می‌زنه، همه رو به یک شکل استاندارد (`ActionItem`، فقط یک DTO در سطح اپلیکیشن، **نه جدول جدید**) تبدیل می‌کنه، و به کاربر یک لیست واحد با دکمهٔ «برو به همون‌جا» یا (در مواردی مثل الگوی موجود Shipment Edit Request) دکمهٔ Quick Action مستقیم نشون می‌ده.

---

## نام ماژول

بررسی استانداردهای صنعتی که کاربر اشاره کرد:

| سیستم | نام | تمرکز |
|---|---|---|
| SAP Fiori | My Inbox | تأیید Workflow |
| Dynamics 365 | Activities | تماس/ایمیل/وظیفه/قرار ملاقات — دقیقاً معادل الگوی فعلی `activities` سپتا |
| Salesforce | Tasks & Activities | چک‌لیست کارها |
| Oracle | BPM Worklist | تأیید Workflow |

**نام فارسی نهایی پیشنهادی: «مرکز اقدامات»** (دقیقاً همون پیشنهاد اولیهٔ شما) — چون کلمهٔ «اقدام» هم Task هم Approval هم Follow-up رو پوشش می‌ده؛ «کار» فقط Task معنی می‌ده، «وظیفه» بار رسمی/بروکراتیک بیشتری داره.

**نام فنی داخلی (route/کلید پرمیژن): `action_center`** — نه `task` (با `activityType='internal_task'` موجود تداخل اسمی داره) و نه `inbox`/`my_inbox` (بار معنایی «پیام» داره، با چت/اعلان اشتباه می‌شه).

بررسی و رد شد: **«مرکز کار» / Work Center** — در ERP صنعتی (SAP PP، Oracle MES) این اصطلاح یعنی *ایستگاه تولید/ماشین‌آلات*؛ سپتا اصلاً ماژول تولید ندارد، استفاده از این اسم گمراه‌کننده است. **«کارتابل من»** — اصطلاح رایج اتوماسیون اداری ایرانیه ولی بار معنایی بروکراسی کاغذی داره که با فضای مدرن سپتا هم‌خوانی نداره.

---

## مرحلهٔ ۱: انواع آیتم‌ها

نکتهٔ معماری مهم قبل از جدول: از ۸ مورد پیشنهادی شما، فقط **۴ تا واقعاً «نوع» (Type) با رفتار UI متفاوت** هستن؛ بقیه **محورهای عمود بر نوع** هستن (هر Typeی می‌تونه این ویژگی‌ها رو داشته باشه)، نه Type مجزا:

- **Task / Assigned Task** → این دو یک Typeاند (`internal_task`)؛ تمایزشون فقط این‌ه که `assignedToUserId` با `createdByUserId` یکیه یا نه — نیازی به فیلد/Type جدید نیست، فقط یک محاسبهٔ نمایشی.
- **Overdue Actions** → Type نیست؛ یک وضعیت زمانیه که هر Typeی (Task/Follow-up/Approval/Reminder) می‌تونه بهش برسه وقتی `dueAt` بگذره. باید Cross-cutting باشه، نه یک ستون Status جدا (توضیح کامل در مرحلهٔ ۲).
- **Workflow Generated Actions** → Type نیست؛ یک «منبع ایجاد» (سیستم/Cron در برابر کاربر) است که می‌تونه روی هر Typeی (اغلب Task یا Approval) اعمال بشه.
- **Waiting For Others** → همون `status='waiting'` موجوده؛ پیشنهاد بهبود: یک فیلد متنی «منتظر چی/کی؟» بهش اضافه بشه (امروز نداره).

جدول کامل (۴ نوع اصلی + آنچه از پیشنهاد شما در هرکدوم جا می‌گیره):

### ۱. Task (شخصی/واگذارشده)
| | |
|---|---|
| هدف | کار مشخصی که باید انجام بشه، بدون نیاز به تأیید کسی |
| سازنده | کاربر (برای خودش) یا مدیر/همکار (واگذاری به دیگری) |
| نمونهٔ سپتا | «ارسال درخواست قیمت به تأمین‌کننده»، «بررسی قیمت دریافتی» |
| نیاز به انجام یا فقط اطلاع | نیاز به انجام |
| نیاز به تأیید | خیر |
| Deadline | اختیاری |
| اولویت/رنگ | همهٔ ۴ سطح (urgent/high/normal/low) معتبره |
| وضعیت فعلی | ✅ کاملاً پیاده‌شده (`activityType='internal_task'`) — فقط UI سراسری نداره |

### ۲. Follow-up (پیگیری)
| | |
|---|---|
| هدف | یادآوری برای چک‌کردن پیشرفت یک موضوع باز |
| سازنده | معمولاً خودکار (Auto Follow-up Engine فاز ۱۷) یا دستی |
| نمونهٔ سپتا | «پیگیری پرداخت مشتری»، «پیگیری قیمت از تأمین‌کننده بی‌پاسخ» |
| نیاز به انجام یا فقط اطلاع | نیاز به انجام |
| نیاز به تأیید | خیر |
| Deadline | معمولاً بله (اغلب خودکار از outcome قبلی محاسبه می‌شه) |
| اولویت/رنگ | معمولاً normal، مگر دستی بالاتر تعیین بشه |
| وضعیت فعلی | ✅ کاملاً پیاده‌شده و حتی زنجیره‌ای (`followUpOfActivityId`) — فقط UI سراسری نداره |

### ۳. Approval (تأیید)
| | |
|---|---|
| هدف | تصمیم دوگزینه‌ای (تأیید/رد) روی یک درخواست |
| سازنده | همیشه سیستم/Workflow (وقتی یک رکورد وارد وضعیت «در انتظار تأیید» می‌شه) |
| نمونهٔ سپتا | تأیید مرخصی/وام/مأموریت/اضافه‌کاری/درخواست عمومی HR (۵ مورد موجود در `TeamApprovalsWidget`)، تأیید درخواست اصلاح مرحلهٔ محموله (`shipment_edit_requests`) |
| نیاز به انجام یا فقط اطلاع | نیاز به انجام (اقدام دوگزینه‌ای) |
| نیاز به تأیید | بله — این خودش یعنی تأیید |
| Deadline | معمولاً ندارد امروز (می‌تونه در فاز بعد اضافه بشه) |
| اولویت/رنگ | معمولاً high (چون معطل‌کردن دیگران) |
| وضعیت فعلی | ⚠️ **پراکنده در ۶ جدول جدا** (leave/loan/mission/overtime/hr_requests/shipment_edit_requests)؛ `activityType='approval'` در `activities` تعریف شده ولی **هیچ‌کجا استفاده نمی‌شه** — باید یا لایهٔ تجمیع (پیشنهاد اصلی من) یا در فاز بعد بعضی از این تأییدها واقعاً Activity واقعی هم بسازن |

### ۴. Reminder (یادآوری)
| | |
|---|---|
| هدف | یادآوری صرف، بدون پیامد کاری پیچیده |
| سازنده | کاربر برای خودش |
| نمونهٔ سپتا | «تماس با مشتری فردا ساعت ۱۰» |
| نیاز به انجام یا فقط اطلاع | نیاز به انجام (ولی سبک‌وزن) |
| نیاز به تأیید | خیر |
| Deadline | همیشه (وگرنه معنی نداره) |
| اولویت/رنگ | معمولاً normal/low |
| وضعیت فعلی | ✅ کاملاً پیاده‌شده (`activityType='reminder'`) |

### محورهای عمود (نه Type مجزا)
| محور | مقادیر | پیاده‌سازی |
|---|---|---|
| منبع ایجاد | خودم / واگذارشده / سیستم (Workflow) | مشتق از `assignedToUserId` vs `createdByUserId`؛ برای سیستم یک User سیستمی یا فلگ `isSystemGenerated` |
| وضعیت زمانی | به‌موقع / امروز / این هفته / سررسیدگذشته | محاسبه از `dueAt`، نه ستون جدا |
| نیاز به تأیید یا نه | بله/خیر | مشتق از Type (فقط Approval بله‌ست) |

**یک مورد جدید که در بررسی کد کشف شد و در پیشنهاد اولیهٔ شما نبود:** `inquiry_discussions.mentioned_user_id` (دامنه ۲، «اشاره/Mention» در گفتگوی پرونده) از قبل در دیتابیس هست ولی **هیچ Notification/Action واقعی برایش ساخته نمی‌شه** — یک Gap واقعی که ارزش داره در فاز ۱ یا ۲ به «مرکز اقدامات» وصل بشه (وقتی کسی منشن می‌شه → یک Notification/Action سبک).

---

## مرحلهٔ ۲: چرخهٔ عمر

وضعیت‌های فعلی `activities`: `open → scheduled/waiting → overdue (خودکار) → completed/cancelled`

مقایسه با پیشنهاد شما (Open/In Progress/Waiting/Deferred/Completed/Cancelled):

| وضعیت پیشنهادی شما | معادل فعلی | تصمیم |
|---|---|---|
| Open | `open` | بدون تغییر |
| In Progress | **ندارد** | ⚠️ باید اضافه بشه — الان هیچ راهی نیست که کاربر بگه «شروعش کردم» |
| Waiting | `waiting` | بدون تغییر، ولی فیلد «منتظر چی/کی؟» اضافه بشه |
| Deferred | `scheduled` | همون مفهومه، فقط اسم را در UI به «موکول‌شده» تغییر بدیم یا خودِ enum را rename کنیم (تصمیم جزئی، تأثیری بر معماری نداره) |
| Completed | `completed` | بدون تغییر |
| Cancelled | `cancelled` | بدون تغییر |

**⚠️ نکتهٔ معماری مهم دربارهٔ Overdue:** امروز `overdue` یک **وضعیت واقعی enum** است — یعنی وقتی Cron یک فعالیت `waiting` را Overdue می‌کنه، اطلاعات «این فعالیت داشت منتظر چی بود» گم می‌شه (چون status از `waiting` به `overdue` عوض شده، نه اینکه یک Flag جدا کنارش اضافه بشه). پیشنهاد: Overdue را از یک وضعیت enum به یک **Flag محاسبه‌شده** (`dueAt < now() AND status NOT IN (completed, cancelled)`) تبدیل کنیم که مستقل از وضعیت واقعی (Open/In Progress/Waiting/Deferred) نمایش داده بشه — یعنی یک فعالیت می‌تونه هم‌زمان «Waiting» و «Overdue» باشه. این یک تغییر واقعی نسبت به رفتار فعلی Cron است و باید با دقت در فاز اجرا انجام بشه (سازگاری با کد فعلی که `status='overdue'` را جای دیگه چک می‌کنه باید بررسی بشه).

چرخهٔ عمر نهایی پیشنهادی:

```
   ┌─────────┐     ┌──────────────┐     ┌─────────┐     ┌──────────┐
   │  Open   │ ──▶ │ In Progress  │ ──▶ │ Waiting │ ──▶ │ Deferred │
   └─────────┘     └──────────────┘     └─────────┘     └──────────┘
        │                  │                  │                │
        └──────────────────┴──────────────────┴────────────────┘
                                   │
                     ┌─────────────┴─────────────┐
                     ▼                           ▼
              ┌─────────────┐            ┌─────────────┐
              │  Completed  │            │  Cancelled  │
              └─────────────┘            └─────────────┘

   [Overdue] = یک Badge مستقل که روی هر وضعیت غیرنهایی (همه به‌جز Completed/Cancelled)
               وقتی dueAt گذشته باشه ظاهر می‌شه — نه یک جعبهٔ جدا در این نمودار
```

---

## مرحلهٔ ۳: اولویت‌بندی

enum فعلی (`low/normal/high/urgent`) دقیقاً منطبق با پیشنهاد شماست، فقط اسم «urgent» را در UI فارسی «بحرانی/فوری» ترجمه می‌کنیم — نیازی به تغییر دیتابیس نیست.

| اولویت | رنگ | برچسب فارسی |
|---|---|---|
| urgent | 🔴 قرمز | بحرانی |
| high | 🟠 نارنجی | مهم |
| normal | 🔵 آبی (یا خنثی) | عادی |
| low | ⚪ خاکستری | کم‌اهمیت |

**مرتب‌سازی پیشنهادی:** Overdue همیشه اول (فارغ از اولویت) ← بعد اولویت نزولی ← بعد Deadline صعودی ← موارد بدون Deadline آخر.

**تشخیص خودکار Overdue:** `dueAt < now() AND status NOT IN (completed, cancelled)` — دقیقاً منطق فعلی Cron (هر ۱۵ دقیقه)، فقط خروجی‌اش یک Flag باشه نه تغییر enum status (توضیح در مرحلهٔ ۲).

---

## مرحلهٔ ۴: ارتباط با ماژول‌های سپتا

`relatedEntityType`/`relatedEntityId` از قبل در جدول `activities` عمومی است؛ فقط سرویس (`assertEntityExists`/`assertCanViewEntity` در `activities.service.ts`) امروز صرفاً `'inquiry'` را می‌پذیرد. فهرست رسمی انواع پیشنهادی برای گسترش (فاز ۲، نه فاز ۱):

| relatedEntityType | ماژول | نمونه |
|---|---|---|
| `inquiry` | استعلام | ✅ از قبل پیاده‌شده |
| `business_partner` | مشتری/تأمین‌کننده | «پیگیری پرداخت مشتری ABC» |
| `item_catalog` | کالا | «تکمیل مشخصات فنی کالای X» |
| `order` | سفارش | «پیگیری تحویل سفارش #123» |
| `purchase_order` | خرید | «تأیید PO #456» |
| `shipment` | محموله | «پیگیری ترخیص محمولهٔ #789» |
| `letter` | مکاتبات | «پاسخ به نامهٔ #1405-پ ت-۰۰۴۲» |
| `employee` | منابع انسانی | «تکمیل مدارک استخدامی» |

برای مثال شما («دریافت قیمت موتور Siemens» → Related: Inquiry #2458، Supplier: Siemens) — پیشنهاد: به‌جای فیلد دومِ «Related» در خودِ Activity، این اطلاعات از طریق **Join با خودِ Inquiry** (که خودش `buyer_id` و از طریق `rfq_items`/`supplier_rfqs` تأمین‌کننده‌های مرتبط را دارد) مشتق بشه، نه یک ستون دوم روی Activity — چون هر Related Entity ثانویه یک ساختار متفاوت داره و اضافه‌کردن فیلد عمومی دوم دوباره ما رو به سمت افزونگی می‌بره. اگر در عمل موارد زیادی پیدا شد که واقعاً به یک Context دوم مستقل (نه مشتق‌شدنی) نیاز دارن، آن‌وقت به‌عنوان یک بهبود مشخص در فاز بعد بررسی می‌شه.

---

## مرحلهٔ ۵: رابط کاربری

**نمای اصلی:** «مرکز اقدامات» به‌عنوان یک **صفحهٔ سطح‌بالا در سایدبار** (نه فقط ویجت داشبورد کنونی) — با یک Badge تعداد (Overdue + امروز)، دقیقاً هم‌الگوی زنگولهٔ اعلان‌ها (`NotificationBell`) موجود.

**کارت‌های خلاصهٔ بالای صفحه:**
- 🔴 سررسیدگذشته
- 🟢 امروز
- 🔵 این هفته
- ⚪ بدون تاریخ / آینده

**فیلترهای جانبی:** نوع (Task/Follow-up/Approval/Reminder) · منبع (خودم/واگذارشده/سیستم) · ماژول مرتبط · اولویت.

**Viewها:**
| View | تصمیم | دلیل |
|---|---|---|
| **Table/List** | ✅ نسخهٔ اول | سریع‌ترین برای پیاده‌سازی؛ دقیقاً هم‌الگوی جدول‌های موجود سپتا (لیست استعلام‌ها و غیره) |
| **Kanban** (ستون = وضعیت) | فاز بعد | ارزش بالا برای کسی که چند کار همزمان داره، ولی Drag&Drop پیچیدگی UI/تست قابل‌توجهی اضافه می‌کنه |
| **Calendar** (بر اساس dueAt) | فاز بعد یا بعدتر | نیاز به کتابخانهٔ تقویم جدید (هزینهٔ اضافه، پروژه فعلاً کتابخانهٔ تقویم نداره)؛ ارزشش زمانی بالاست که تعداد Actionهای Deadlineدار زیاد بشه |

---

## مرحلهٔ ۶: دیتامدل

### گسترش `activities` (نه جدول جدید) — Migration در فاز اجرا
- `status`: افزودن `in_progress` به enum دستی؛ تبدیل `overdue` به Flag محاسبه‌شده (جزئیات دقیق در فاز اجرا با توجه به سازگاری با کد فعلی Cron مشخص می‌شه).
- `waiting_reason` (TEXT, nullable) — توضیح «منتظر چی/کی؟» وقتی status=waiting.
- گسترش `relatedEntityType` پشتیبانی‌شده در سرویس (نه دیتابیس، چون ستون از قبل عمومیه) به لیست مرحلهٔ ۴.

### چیزهایی که **نیازی به جدول/ستون جدید ندارن**:
- **کامنت روی Activity** — برای Activityهای متصل به Inquiry از `inquiry_discussions` موجود (که همین الان هم `metadata` JSONB برای همچین مواردی داره) استفاده می‌شه؛ برای Activityهای بدون Related Entity (Task شخصی)، کامنت در فاز ۱ لازم نیست (over-engineering برای یک Task شخصی ساده).
- **پیوست روی Activity** — همون استدلال بالا؛ اگر پرونده‌محوره، از سیستم پیوست همون پرونده استفاده می‌شه.
- **تاریخچه/History** — برای Activityهای متصل به Inquiry از `ActivityLogService` موجود (دامنه ۲) استفاده می‌شه که همین الان هم `activities.service.ts` ازش استفاده می‌کنه؛ برای Task شخصی، Audit Trail جدا لازم نیست.
- **جدول‌های تجمیع (HR approvals/shipment edit requests/correspondence)** — هیچ جدول دیتابیس جدیدی لازم ندارن؛ فقط در یک Service جدید (`ActionCenterService`, بدون جدول) Query یکپارچه می‌شن.

### Entity واقعاً جدید در فاز ۱: هیچ‌کدام
تنها یک **سرویس اپلیکیشنی جدید** (`ActionCenterService`) که Read-Only از منابع موجود Query می‌زنه و آن‌ها را به یک شکل استاندارد (`ActionItem` DTO، نه جدول) برمی‌گرداند.

---

## مرحلهٔ ۷: جمع‌بندی معمار

### نام نهایی
**«مرکز اقدامات»** (فارسی) / `action_center` (فنی) — تأیید پیشنهاد اولیهٔ شما.

### نسخهٔ اول (MVP) — چه چیزی باید باشد
1. گسترش `activities`: افزودن وضعیت `in_progress`، فیلد `waiting_reason`، تبدیل Overdue به Flag مستقل از status.
2. صفحهٔ سراسری «مرکز اقدامات» در سایدبار (Table View + کارت‌های خلاصه + فیلتر نوع/اولویت/منبع/ماژول).
3. `ActionCenterService` (تجمیع‌گر Read-Only): `activities` + ۵ منبع تأیید HR موجود + `shipment_edit_requests` + ارجاعات نامه، همه در یک لیست با دکمهٔ Deep-link (و در مواردی که الگوی موجود اجازه می‌ده، مثل Shipment Edit Request، دکمهٔ Quick Action مستقیم).
4. Badge تعداد در سایدبار (Overdue + امروز)، هم‌الگوی `NotificationBell` موجود.
5. اتصال `mentioned_user_id` (Gap کشف‌شده در دامنه ۲) به یک Action/Notification سبک.

### چیزهایی که برای نسخه‌های بعد نگه می‌داریم
- Kanban View و Calendar View.
- گسترش رسمی `relatedEntityType` به همهٔ ماژول‌های مرحلهٔ ۴ (نیاز به Generic کردن `assertEntityExists`/`assertCanViewEntity` در سرویس).
- تعریف Workflow Triggerهای خودکار بیشتر (باید با احتیاط اضافه بشن — اگر بی‌دقت زیاد بشن، مرکز اقدامات پر از Noise می‌شه و کاربرد خودش رو از دست می‌ده).
- گزارش/آنالیز عملکرد (درصد به‌موقع تکمیل‌شدن، میانگین زمان Overdue).
- تنظیم شخصی این‌که کدام دسته‌ها اعلان/Push بگیرن.

### سؤالات باز — نیاز به تصمیم شما قبل از نوشتن SPEC نهایی
1. **دکمهٔ اقدام در لیست تجمیع‌شده:** برای منابع تأیید HR/محموله، آیا فقط «برو به همون صفحه» (Deep-link) کافیه، یا باید مثل Shipment Edit Request دکمهٔ تأیید/رد مستقیم داخل خودِ مرکز اقدامات هم باشه؟ (پیشنهاد من: هردو، جایی که الگوی موجود اجازه می‌ده.)
2. **دسترسی «دیدن کارهای تیم»:** امروز فقط با عضویت در گروه هارد-کدشدهٔ «مدیریت» است. آیا باید یک کلید RBAC واقعی (`action_center.view_team`) اضافه بشه تا مدیر هر واحد (نه فقط مدیریت کل) بتونه کارهای تیم خودش رو ببینه؟ (پیشنهاد من: بله، این یک ضعف شناخته‌شدهٔ فعلیه.)
3. **مثال «Workflow Generated Action» شما (Inquiry → دریافت قیمت، Deadline ۴۸ساعته):** آیا این تریگر باید روی «ایجاد Inquiry» باشه یا روی «ارسال RFQ» (چون در لحظهٔ ایجاد Inquiry هنوز مشخص نیست قراره به کدام تأمین‌کننده RFQ بره)؟ من پیشنهاد می‌کنم روی ارسال RFQ باشه؛ اگر مثال دیگری مدنظرتونه بفرمایید.

---

منتظر «تاییده» یا اصلاحات شما هستم — تا آن زمان کد/Migration/API/Frontend نمی‌نویسم.

---

## ضمیمهٔ پیاده‌سازی (بعد از «تاییده»)

- **Migration `0018_action_center`**: `activities.status` چک‌لیست `in_progress` گرفت + ستون جدید `waiting_reason`. تصمیم عملی: تبدیل کامل Overdue از وضعیت enum به Flag مستقل (که در مرحلهٔ ۲ سند به‌عنوان بهبود مطلوب مطرح شده بود) به فاز بعد موکول شد — چون رفتار فعلی Cron/فیلترها به `status='overdue'` وابسته‌ست و تغییرش نیاز به بازبینی دقیق‌تر داره؛ در عوض `isOverdue` به‌عنوان یک فیلد محاسبه‌شده در خروجی `ActionItem` (نه در دیتابیس) اضافه شد — همون اثر عملی، بدون ریسک شکستن رفتار موجود.
- **`ActivitiesService`**: `canViewTeamActivities()` جایگزین چک صرفِ گروه «مدیریت» شد — حالا دارندهٔ `action_center.view_team` هم می‌تونه فعالیت‌های تیم رو ببینه (پاسخ به سؤال باز #۲). `update()` وقتی وضعیت از `waiting` خارج بشه `waitingReason` رو خودکار پاک می‌کنه.
- **`ActionCenterModule`** (جدید، Read-Only): `ActionCenterService.getForUser()` از چهار منبع Query می‌زنه — `ActivitiesService.list()`، ۵ سرویس `pendingApproval()` موجود دامنهٔ HR (فقط اگه کاربر پروندهٔ پرسنلی وصل داشته باشه، وگرنه silently خالی — کاربرهای بدون پروندهٔ HR مثل ادمین seed نباید خطا بگیرن)، `prisma.shipmentEditRequest` (فقط با `shipping.approve_edit`)، `prisma.letter` (ارجاع‌شده به من، غیرآرشیوشده). همه به یک `ActionItem` استاندارد (نه جدول) تبدیل و با اولویت Overdue→Priority→Deadline مرتب می‌شن. اقدام واقعی (تکمیل/تأیید/رد) همیشه از endpoint اصلی همون ماژول انجام می‌شه (`actions[].path` در پاسخ) — این سرویس هیچ نوشتاری روی داده ندارد.
- **`GET /action-center?scope=mine|team`**: بدون کلید RBAC اجباری برای حالت شخصی؛ حالت `team` فقط داخل سرویس (نه Guard) مشروط به مجوز باز می‌شه.
- **RFQ-send Trigger**: `RfqsService.create()` بعد از ارسال موفق RFQ یک Activity نوع `follow_up` با `dueAt = responseDueDate` (نه عدد ثابت) برای فرستنده می‌سازه — پاسخ به سؤال باز #۳؛ روی «ارسال RFQ» نه «ایجاد Inquiry»، چون تا لحظهٔ ارسال معلوم نیست قراره کدوم تأمین‌کننده مقصد باشه.
- **پرمیژن جدید**: `action_center.view_team` (ماژول `action_center` در کاتالوگ) — پیش‌فرض فقط گروه «مدیریت» (از طریق `ALL_PERMISSION_KEYS`) دارتش، قابل تخصیص دستی به گروه‌های سفارشی.
- **Frontend**: `/action-center` صفحهٔ سراسری (کارت‌های خلاصه + فیلتر نوع + جدول با دکمهٔ اقدام سریع) — اولین آیتم گروه «کار روزمره» در سایدبار، بدون `requiredPermission` (مثل چت/داشبورد، برای همه قابل‌مشاهده چون هرکسی می‌تونه کار شخصی داشته باشه)؛ نشان‌گر عددی سایدبار (Overdue+امروز) هر ۶۰ ثانیه Poll می‌شه.
- **تست‌ها**: ۵ تست جدید `action-center.service.spec.ts` (نگاشت فعالیت‌ها، حذف کاربر بدون پروندهٔ HR، مسیرهای Quick Action تأیید/رد HR، محدودیت `shipping.approve_edit`، فیلتر نامهٔ آرشیوشده) + ۲ تست جدید در `activities.service.spec.ts` (پرمیژن `view_team`، پاک‌شدن `waitingReason`) + Mock جدید در `rfqs.service.spec.ts`؛ کل مجموعهٔ بک‌اند (۳۷ Suite، ۳۰۰ تست) سبز.
- **E2E زنده**: یک Activity با سررسید گذشته از داشبورد ساخته شد، در `/action-center` با شمارندهٔ «سررسیدگذشته: ۱» و برچسب قرمز تاریخ دیده شد، دکمهٔ «تکمیل» زده شد و بلافاصله از لیست خارج و شمارنده صفر شد؛ سایدبار آیتم «مرکز اقدامات» را با آیکون/برچسب/نشان‌گر صحیح نشون داد.
