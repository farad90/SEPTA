# SPEC — فاز ۷: نتیجه نهایی (برد/باخت) — تب ۵

> پیش‌نیاز: فاز ۶ ✅. مرجع بصری: `OutcomeTab` در `mockups/inquiry-detail-mockup.jsx`.
> جدول: `inquiry_item_outcomes` (baseline، در سطح هر قلم — نه کل استعلام).

## هدف فاز ۷

1. ثبت سریع نتیجه با ۴ حالت میان‌بر (طبق mockup): **برد کامل** / **باخت کامل** / **لغو شده** / **ترکیبی** — بدون نیاز به دلیل جدا برای تک‌تک اقلام
2. طبق تصمیم شما: هر بار ثبت، **همه اقلام پرونده هم‌زمان** آپدیت می‌شن (نه ذخیره جزئی/تدریجی)؛ در حالت ترکیبی هر قلم بین برد/باخت جابجا می‌شه
3. بعد از ثبت، `inquiries.status` به‌صورت خودکار محاسبه می‌شه: همه `won`→`won`، همه `lost`→`lost`، همه `cancelled`→`cancelled`، ترکیبی از برد/باخت→`partially_won`
4. تب همیشه باز است (بدون قفل/پیش‌شرط از تب‌های قبل — طبق تصمیم شما)
5. «اصلاح نتیجه»: فرم با آخرین مقادیر ذخیره‌شده از سرور پر می‌شه (نه ریست به حالت اولیه mockup)

## بک‌اند

Prisma model جدید: `InquiryItemOutcome` (PK = `inquiry_item_id`، جدولش در baseline هست؛ Migration جدید لازم نیست).

### OutcomeModule

| Endpoint | دسترسی | توضیح |
|---|---|---|
| `GET /inquiries/:id/outcome` | `outcome.record` | اقلام پرونده + نتیجه فعلی هر قلم (یا `null` اگه هنوز تصمیمی ثبت نشده) + `status` فعلی پرونده |
| `PUT /inquiries/:id/outcome` | `outcome.record` | ثبت/بازنویسی نتیجه همه اقلام یک‌جا؛ `inquiries.status` رو دوباره محاسبه می‌کنه؛ activity با tag=`stage_completed` |

**DTO ورودی:**
```
mode: 'won_all' | 'lost_all' | 'cancelled' | 'mixed'
decisionDate: string
winReason?: string            // فقط won_all/mixed
lossReason?: enum شش‌گانه      // فقط lost_all/mixed — نگاشت از برچسب فارسی mockup به enum انگلیسی CHECK دیتابیس
competitorName?: string
competitorPrice?: number
note?: string                 // به expert_note همه ردیف‌های همین ذخیره کپی می‌شه
itemResults?: Record<inquiryItemId, 'won'|'lost'>   // الزامی فقط برای mode='mixed'؛ باید همه اقلام رو پوشش بده
```

**قوانین کسب‌وکاری:**
- `mode='mixed'` بدون پوشش کامل همه اقلام در `itemResults` رد می‌شه (۴۰۰)
- نگاشت `lossReason` mockup («قیمت بالاتر از رقیب» و...) به enum دیتابیس (`higher_price` و...) در DTO/سرویس انجام می‌شه؛ در UI برچسب فارسی نشون داده می‌شه
- هر ذخیره، `inquiry_item_outcomes` همه اقلام رو upsert می‌کنه (نه فقط جدیدها) — یعنی نتیجه قبلی همیشه با ذخیره جدید بازنویسی می‌شه
- بعد از upsert، `inquiries.status` مستقیماً بر اساس نتایج جدید ست می‌شه (won/lost/cancelled/partially_won)
- activity یک رکورد خلاصه («نتیجه پرونده ثبت شد: برد کامل» یا مشابه) — نه یک رکورد به‌ازای هر قلم

### دسترسی

از کلید موجود کاتالوگ استفاده می‌شه، چیز جدیدی لازم نیست: `outcome.record` (همین الان پیش‌فرض به فروش/مدیریت داده شده). چون کلید `.view` جدایی برای این ماژول تعریف نشده، کل تب (نمایش + ثبت) با همین یک کلید گیت می‌شه.

## فرانت‌اند — تب ۵ (مطابق mockup)

- اگه هیچ‌کدوم از اقلام هنوز نتیجه ندارن (`outcome: null` روی همه): نمایش فرم انتخاب حالت (۴ دکمه بزرگ)
- اگه همه اقلام از قبل نتیجه دارن: نمایش خلاصه (`مشابه mockup: "نتیجه ثبت شد: ..."`) + دکمه «اصلاح نتیجه» که برمی‌گرده به فرم، با مقادیر پرشده از سرور (mode/reason/note/itemResults استنتاج‌شده از داده موجود)
- حالت ترکیبی: لیست اقلام با دکمه toggle برد/باخت (پیش‌فرض «برد»)
- بعد از ثبت موفق برد کامل/ترکیبی: پیام اطلاع‌رسانی «تب سفارش برای N قلم برنده‌شده در فازهای بعدی فعال می‌شه» — ⚠️ **تب ۶ همچنان قفل می‌مونه** (ماژول سفارش هنوز ساخته نشده)، این فقط پیام اطلاع‌رسانیه نه باز شدن واقعی تب
- گیت دسترسی: بدون `outcome.record` پیام «دسترسی ندارید» (مثل بقیه تب‌ها)

## خارج از اسکوپ فاز ۷ (به‌صراحت)

- تب ۶ «سفارش مشتری» (دامنه ۵ اسکیما) — فاز بعدی
- گزارش تحلیلی برد/باخت (نرخ تبدیل، دلایل پرتکرار باخت) — فاز گزارش‌ها

## تست‌ها

- **Jest**: رد `mixed` بدون پوشش کامل اقلام، نگاشت درست `lossReason`، محاسبه صحیح `inquiries.status` برای هر ۴ حالت (won/lost/cancelled/mixed→partially_won)، بازنویسی نتیجه قبلی روی ذخیره دوم، activity log
- **E2E زنده**: یک استعلام دو-قلمی → ثبت «ترکیبی» (قلم ۱ برد با دلیل، قلم ۲ باخت با دلیل+رقیب) → بررسی `status=partially_won` → اصلاح به «برد کامل» → بررسی بازنویسی هر دو قلم + `status=won` → پاک‌سازی

## Definition of Done

- [ ] GET/PUT outcome با upsert همه اقلام + محاسبه `inquiries.status`
- [ ] نگاشت لیبل فارسی dropdown دلیل باخت به enum دیتابیس
- [ ] تب ۵ مطابق mockup (۴ دکمه حالت، فرم شرطی، خلاصه/اصلاح، toggle ترکیبی)
- [ ] Jest سبز + build/lint + E2E زنده + تست بصری
