# SPEC — فاز ۵: انتخاب نهایی و قیمت‌گذاری (تب ۳)

> پیش‌نیاز: فازهای ۱–۴ ✅. مرجع بصری: `SelectionTab` در `mockups/inquiry-detail-mockup.jsx`.
> این مرحله **مخصوص مدیریت/مدیر فروشه** (design doc) — کارشناس بازرگانی فقط آفر ثبت می‌کنه؛ اینجا مدیریت مقایسه و تصمیم می‌گیره.

## هدف فاز ۵

1. **مقایسه آفرها به تفکیک هر قلم**: همه آفرهای دریافتی برای هر قلم کنار هم (تأمین‌کننده، PN، فی، زمان تحویل) با انتخاب radio در سطح قلم
2. **توزیع هزینه‌ها** (`supplier_offers.distribute_costs`): چک‌باکس روی هر آفر — VAT + سایر هزینه‌ها به نسبت بین اقلام همون آفر توزیع و «فی مؤثر» نمایش داده می‌شه (مبنای Markup)
3. **قیمت‌گذاری هر قلم**: درصد حاشیه سود (`markup_percent`) + قیمت فروش نهایی (`final_sale_price`) — خودکار محاسبه، قابل اصلاح دستی؛ + یادداشت معیار انتخاب (`selection_notes`)
4. **گزینه‌های ترم تحویل** (`inquiry_delivery_options`): چند Incoterm هم‌زمان (EXW/CPT/DDP/CIF/FOB) هرکدوم با هزینه اضافه و زمان تحویل + نمایش «ارزش کل» زنده هر ترم
5. **یادداشت مدیر برای فروش** (`manager_note_to_sales`) — بعد از قفل به کارشناس فروش نشون داده می‌شه (تب ۴، فاز ۶)
6. **قفل/بازگشایی مرحله** (`selection_locked_at/by`) — پیش‌شرط تولید پیشنهاد در فاز ۶؛ بعد از قفل، ثبت/ویرایش آفر (فاز ۴) هم مسدوده (از قبل پیاده شده)

## ⚠️ دو انحراف حساب‌شده از mockup (نیاز به نگاه شما)

1. **فرمول توزیع هزینه**: mockup برای سادگی، هزینه اضافی رو به نسبت «فی واحد» توزیع می‌کنه و مقدار (تعداد) رو نادیده می‌گیره — با تعدادهای متفاوت، عدد مالی غلط می‌شه (سهم هزینه در ضرب نهایی چندبرابر حساب می‌شه). پیاده‌سازی واقعی: توزیع به نسبت **ارزش ردیف** (فی × تعداد)، بعد تقسیم سهم بر تعداد برای «فی مؤثر»:
   `فی مؤثر = فی + (ارزش ردیف ÷ جمع ارزش اقلام آفر × (VAT + سایر هزینه‌ها)) ÷ تعداد`
   با تعداد برابر، نتیجه با mockup یکیه؛ با تعداد نابرابر، این نسخه درسته.
2. **چند ارز**: آفرهای منتخب اقلام مختلف ممکنه ارز متفاوت داشته باشن؛ mockup همه رو EUR فرض کرده. «ارزش کل» هر ترم **به تفکیک ارز** جمع و نمایش داده می‌شه (بدون تبدیل نرخ — تبدیل کار فاز گزارش‌هاست). هزینه اضافه ترم به ارز غالب (پرتکرارترین ارز اقلام منتخب) ثبت می‌شه.

## بک‌اند

مدل Prisma جدید: `InquiryDeliveryOption` (جدولش در baseline هست) + relation به `Inquiry`. Migration جدید لازم نیست.

### SelectionModule

| Endpoint | دسترسی | توضیح |
|---|---|---|
| `GET /inquiries/:id/selection` | `selection.view` | تجمیعی: اقلام + آفرهای هر قلم (با فی مؤثر محاسبه‌شده) + انتخاب/markup/final فعلی + delivery options + وضعیت قفل + manager note |
| `PATCH /inquiries/:id/selection` | `selection.select_offer` | ذخیره batch: انتخاب آفر هر قلم + `selection_notes` + `distribute_costs` هر آفر؛ فیلدهای markup/final فقط با `selection.set_markup` |
| `PUT /inquiries/:id/delivery-options` | `selection.set_markup` | جایگزینی کامل لیست ترم‌ها (`UNIQUE(inquiry_id, delivery_term)`) |
| `POST /inquiries/:id/selection/lock` | `selection.lock` | + ذخیره `manager_note_to_sales`؛ activity «انتخاب نهایی قفل شد» با tag=`approval` |
| `POST /inquiries/:id/selection/unlock` | `selection.lock` | بازگشایی برای اصلاح؛ activity |

**قوانین کسب‌وکاری:**
- `selected_offer_item_id` باید متعلق به آفریِ از RFQهای همین پرونده و مربوط به همون قلم باشه
- بعد از قفل، همه‌ی endpointهای بالا جز unlock مسدودن (۴۰۰ با پیام واضح)
- **شرط قفل**: هر قلمی که حداقل یک آفر قیمت‌دار داره باید آفر منتخب + markup داشته باشه؛ اقلام بدون هیچ آفر («قابل قیمت‌گذاری نیست» در mockup) مانع قفل نیستن ولی در پاسخ قفل به‌عنوان هشدار برمی‌گردن
- `final_sale_price` در صورت عدم ورود دستی = فی مؤثر × (۱ + markup٪) — سمت سرور محاسبه و ذخیره می‌شه (اسنپ‌شات پایه که فاز ۶ کپی می‌کنه و دیگه تغییر نمی‌کنه)
- activity فقط برای اقدامات مهم: قفل/بازگشایی (نه هر ذخیره جزئی)

## فرانت‌اند — تب ۳ (مطابق mockup)

- بنر «این مرحله فقط در اختیار مدیریت/مدیر فروش است»
- کارت هر قلم: radioهای آفر (تأمین‌کننده/PN/فی/تحویل)، چک‌باکس توزیع هزینه با نمایش «فی مؤثر» accent، ردیف markup + قیمت فروش نهایی (قابل اصلاح دستی) + یادداشت معیار انتخاب (بازشونده)
- قلم بدون آفر: پیام قرمز «هنوز هیچ پیشنهاد قیمتی دریافت نشده»
- کارت ترم‌های تحویل: ردیف‌های term/extraCost/days با افزودن/حذف + «ارزش کل» زنده (به تفکیک ارز در حالت چندارزی)
- کارت یادداشت مدیر + دکمه «تأیید و قفل انتخاب نهایی» / «بازگشایی برای اصلاح» + پیام سبز بعد از قفل
- حالت قفل: همه ورودی‌ها disabled (الگوی mockup)
- دکمه ذخیره تغییرات (batch) جدا از قفل — قفل خودش آخرین تغییرات رو هم ذخیره می‌کنه
- گیت دسترسی: بدون `selection.view` تب قفل می‌مونه؛ بدون `select_offer`/`set_markup`/`lock` بخش مربوطه read-only
- تب ۳ در نوار مراحل باز می‌شه

## خارج از اسکوپ فاز ۵ (به‌صراحت)

- تب ۴ (تولید پیشنهاد مالی/فنی از این داده‌ها) — فاز ۶
- تبدیل ارز برای جمع چندارزی (فاز گزارش‌ها؛ `exchange_rates` آماده‌ست)
- سقف عددی دسترسی (`limit_value` مثل سقف مبلغ قرارداد) — ساختارش هست، enforcement با اولین نیاز واقعی

## تست‌ها

- **Jest**: فرمول فی مؤثر (توزیع بر اساس ارزش ردیف، تقسیم بر تعداد)، اعتبار selected_offer_item (آفر پرونده دیگه/قلم دیگه رد بشه)، شرط قفل (قلم آفردار بدون انتخاب → رد)، مسدودیت بعد از قفل، unlock
- **E2E زنده**: سناریوی کامل — دو آفر برای یک قلم → انتخاب + توزیع هزینه + markup → ترم‌ها → قفل → تلاش ثبت آفر جدید (باید ۴۰۰ بده — قانون فاز ۴) → unlock → پاک‌سازی

## Definition of Done

- [ ] GET/PATCH selection با فی مؤثر سمت سرور و گیت دسترسی فیلدی
- [ ] delivery options CRUD با UNIQUE ترم
- [ ] قفل با شرط کامل‌بودن + activity؛ بازگشایی
- [ ] بعد از قفل: selection و آفرهای فاز ۴ هر دو مسدود
- [ ] تب ۳ مطابق mockup (توزیع هزینه زنده، ارزش کل هر ترم، حالت قفل)
- [ ] Jest سبز + build/lint + E2E زنده + تست بصری
